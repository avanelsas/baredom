name: release-angular

on:
  push:
    tags:
      - 'angular-v*'

permissions:
  contents: write

jobs:
  release:
    name: Build & Publish Angular Adapter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - uses: DeLaGuardo/setup-clojure@13
        with:
          cli: 'latest'

      - name: Setup Babashka
        run: |
          curl -sLO https://raw.githubusercontent.com/babashka/babashka/master/install
          chmod +x install
          sudo ./install --dir /usr/local/bin
          bb --version

      - run: npm ci

      - name: Build core library
        run: npm run build

      - name: Derive version from tag
        id: version
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#angular-v}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Verify version matches package.json
        run: |
          PKG_VERSION=$(node -p "require('./adapters/angular/package.json').version")
          if [ "$PKG_VERSION" != "${{ steps.version.outputs.version }}" ]; then
            echo "Tag version (${{ steps.version.outputs.version }}) does not match package.json ($PKG_VERSION)"
            exit 1
          fi

      - name: Generate Angular directives
        run: bb scripts/generate_angular.bb

      - name: Check generated files are up to date
        run: git diff --exit-code adapters/angular/src/ adapters/angular/package.json

      - name: Install adapter dependencies
        run: cd adapters/angular && npm install

      - name: Build adapter
        run: cd adapters/angular && npx ngc

      - name: Publish to npm
        run: |
          npm config set //registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}
          npm publish --workspace adapters/angular --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: "@vanelsas/baredom-angular v${{ steps.version.outputs.version }}"
          body: |
            Angular 17+ directives for BareDOM web components.

            ```bash
            npm install @vanelsas/baredom-angular
            ```
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
