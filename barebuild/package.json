{
  "name": "@vanelsas/barebuild",
  "version": "0.5.0",
  "description": "Server-resource runtime for BareDOM — drives presentational web components from server state without client-side business logic. Reads and writes.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "Alexander van Elsas",
  "repository": {
    "type": "git",
    "url": "https://github.com/avanelsas/baredom.git",
    "directory": "barebuild"
  },
  "scripts": {
    "compile": "npx shadow-cljs compile lib",
    "build": "npx shadow-cljs release lib",
    "test": "npx shadow-cljs compile test && node out/node-tests.cjs",
    "test:browser": "npx shadow-cljs compile test-karma && npx karma start karma.conf.cjs --single-run",
    "test:server": "bb run test:server",
    "server": "bb run server"
  },
  "exports": {},
  "devDependencies": {
    "karma": "^6.4.4",
    "karma-chrome-launcher": "^3.2.0",
    "karma-cljs-test": "^0.1.0",
    "shadow-cljs": "^3.4.11"
  }
}
