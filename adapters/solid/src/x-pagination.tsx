// x-pagination.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup, createEffect } from "solid-js";
import type { JSX } from "solid-js";
import type { XPagination as XPaginationElement } from "@vanelsas/baredom/x-pagination";
import { init } from "@vanelsas/baredom/x-pagination";

init();

export interface XPaginationProps {
  page?: number;
  totalPages?: number;
  siblingCount?: number;
  boundaryCount?: number;
  size?: string;
  disabled?: boolean;
  label?: string;
  defaultPage?: number;
  onPageChangeRequest?: (e: CustomEvent<{ page: number; previousPage: number }>) => void;
  onPageChange?: (e: CustomEvent<{ page: number }>) => void;
  ref?: XPaginationElement | ((el: XPaginationElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XPagination(props: XPaginationProps): JSX.Element {
  const [local, others] = splitProps(props, ["page", "defaultPage", "onPageChangeRequest", "onPageChange", "ref", "children"]);
  let el!: XPaginationElement;

  const setRef = (r: XPaginationElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el || local.page !== undefined || local.defaultPage === undefined) return;
    el.setAttribute("page", String(local.defaultPage));
  });

  createEffect(() => {
    if (!el) return;
    const v = local.page;
    if (v === undefined) return;
    el.setAttribute("page", String(v));
  });

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => {
        if (local.page !== undefined) e.preventDefault();
        local.onPageChangeRequest?.(e as CustomEvent<{ page: number; previousPage: number }>);
      };
      el.addEventListener("page-change-request", handler);
      onCleanup(() => el.removeEventListener("page-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onPageChange?.(e as CustomEvent<{ page: number }>);
      el.addEventListener("page-change", handler);
      onCleanup(() => el.removeEventListener("page-change", handler));
    }
  });

  return (
    <x-pagination ref={setRef} {...others}>
      {local.children}
    </x-pagination>
  );
}
