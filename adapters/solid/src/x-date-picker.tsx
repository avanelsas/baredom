// x-date-picker.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XDatePicker as XDatePickerElement } from "@vanelsas/baredom/x-date-picker";
import { init } from "@vanelsas/baredom/x-date-picker";

init();

export interface XDatePickerProps {
  open?: boolean;
  disabled?: boolean;
  name?: string;
  value?: string;
  mode?: string;
  start?: string;
  readOnly?: boolean;
  end?: string;
  required?: boolean;
  onInput?: (e: CustomEvent<{ value: string; mode: string }>) => void;
  onChangeRequest?: (e: CustomEvent<{ value: string; mode: string; reason: string }>) => void;
  onChange?: (e: CustomEvent<{ value: string; mode: string; reason: string }>) => void;
  ref?: XDatePickerElement | ((el: XDatePickerElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XDatePicker(props: XDatePickerProps): JSX.Element {
  const [local, others] = splitProps(props, ["onInput", "onChangeRequest", "onChange", "ref", "children"]);
  let el!: XDatePickerElement;

  const setRef = (r: XDatePickerElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onInput?.(e as CustomEvent<{ value: string; mode: string }>);
      el.addEventListener("x-date-picker-input", handler);
      onCleanup(() => el.removeEventListener("x-date-picker-input", handler));
    }
    {
      const handler = (e: Event) => local.onChangeRequest?.(e as CustomEvent<{ value: string; mode: string; reason: string }>);
      el.addEventListener("x-date-picker-change-request", handler);
      onCleanup(() => el.removeEventListener("x-date-picker-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ value: string; mode: string; reason: string }>);
      el.addEventListener("x-date-picker-change", handler);
      onCleanup(() => el.removeEventListener("x-date-picker-change", handler));
    }
  });

  return (
    <x-date-picker ref={setRef} {...others}>
      {local.children}
    </x-date-picker>
  );
}
