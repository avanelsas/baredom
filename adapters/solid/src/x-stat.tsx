// x-stat.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XStat as XStatElement } from "@vanelsas/baredom/x-stat";
import { init } from "@vanelsas/baredom/x-stat";

init();

export interface XStatProps {
  trend?: string;
  align?: string;
  variant?: string;
  value?: string;
  hint?: string;
  size?: string;
  loading?: boolean;
  label?: string;
  emphasis?: string;
  ref?: XStatElement | ((el: XStatElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XStat(props: XStatProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XStatElement;

  const setRef = (r: XStatElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-stat ref={setRef} {...others}>
      {local.children}
    </x-stat>
  );
}
