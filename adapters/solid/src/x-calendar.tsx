// x-calendar.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XCalendar as XCalendarElement } from "@vanelsas/baredom/x-calendar";
import { init } from "@vanelsas/baredom/x-calendar";

init();

export interface XCalendarProps {
  min?: string;
  disabled?: boolean;
  locale?: string;
  autoSwap?: boolean;
  value?: string;
  mode?: string;
  start?: string;
  month?: string;
  showWeekNumbers?: boolean;
  disabledDates?: string;
  max?: string;
  firstDayOfWeek?: string;
  end?: string;
  rangeAllowSameDay?: boolean;
  onChange?: (e: CustomEvent<{ value: string; start: string; end: string; mode: string }>) => void;
  onNavigate?: (e: CustomEvent<{ month: string }>) => void;
  ref?: XCalendarElement | ((el: XCalendarElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XCalendar(props: XCalendarProps): JSX.Element {
  const [local, others] = splitProps(props, ["onChange", "onNavigate", "ref", "children"]);
  let el!: XCalendarElement;

  const setRef = (r: XCalendarElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ value: string; start: string; end: string; mode: string }>);
      el.addEventListener("x-calendar-change", handler);
      onCleanup(() => el.removeEventListener("x-calendar-change", handler));
    }
    {
      const handler = (e: Event) => local.onNavigate?.(e as CustomEvent<{ month: string }>);
      el.addEventListener("x-calendar-navigate", handler);
      onCleanup(() => el.removeEventListener("x-calendar-navigate", handler));
    }
  });

  return (
    <x-calendar ref={setRef} {...others}>
      {local.children}
    </x-calendar>
  );
}
