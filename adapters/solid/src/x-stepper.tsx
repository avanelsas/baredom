// x-stepper.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XStepper as XStepperElement } from "@vanelsas/baredom/x-stepper";
import { init } from "@vanelsas/baredom/x-stepper";

init();

export interface XStepperProps {
  steps?: string;
  current?: number;
  orientation?: string;
  size?: string;
  disabled?: boolean;
  onChange?: (e: CustomEvent<{ from: number; to: number }>) => void;
  ref?: XStepperElement | ((el: XStepperElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XStepper(props: XStepperProps): JSX.Element {
  const [local, others] = splitProps(props, ["onChange", "ref", "children"]);
  let el!: XStepperElement;

  const setRef = (r: XStepperElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ from: number; to: number }>);
      el.addEventListener("x-stepper-change", handler);
      onCleanup(() => el.removeEventListener("x-stepper-change", handler));
    }
  });

  return (
    <x-stepper ref={setRef} {...others}>
      {local.children}
    </x-stepper>
  );
}
