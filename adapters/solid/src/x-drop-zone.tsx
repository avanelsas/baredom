// x-drop-zone.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XDropZone as XDropZoneElement } from "@vanelsas/baredom/x-drop-zone";
import { init } from "@vanelsas/baredom/x-drop-zone";

init();

export interface XDropZoneProps {
  value?: string;
  accepts?: string;
  max?: number;
  label?: string;
  disabled?: boolean;
  pending?: boolean;
  pendingIndex?: number;
  animateMoves?: boolean;
  onEnter?: (e: CustomEvent<{ kind: string; value: string }>) => void;
  onLeave?: (e: CustomEvent<{ kind: string; value: string }>) => void;
  onDrop?: (e: CustomEvent<{ kind: string; value: string; from: string | null; to: string; index: number; panel: HTMLElement }>) => void;
  ref?: XDropZoneElement | ((el: XDropZoneElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XDropZone(props: XDropZoneProps): JSX.Element {
  const [local, others] = splitProps(props, ["onEnter", "onLeave", "onDrop", "ref", "children"]);
  let el!: XDropZoneElement;

  const setRef = (r: XDropZoneElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onEnter?.(e as CustomEvent<{ kind: string; value: string }>);
      el.addEventListener("x-drop-zone-enter", handler);
      onCleanup(() => el.removeEventListener("x-drop-zone-enter", handler));
    }
    {
      const handler = (e: Event) => local.onLeave?.(e as CustomEvent<{ kind: string; value: string }>);
      el.addEventListener("x-drop-zone-leave", handler);
      onCleanup(() => el.removeEventListener("x-drop-zone-leave", handler));
    }
    {
      const handler = (e: Event) => local.onDrop?.(e as CustomEvent<{ kind: string; value: string; from: string | null; to: string; index: number; panel: HTMLElement }>);
      el.addEventListener("x-drop-zone-drop", handler);
      onCleanup(() => el.removeEventListener("x-drop-zone-drop", handler));
    }
  });

  return (
    <x-drop-zone ref={setRef} {...others}>
      {local.children}
    </x-drop-zone>
  );
}
