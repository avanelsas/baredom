// x-dropdown.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XDropdown as XDropdownElement } from "@vanelsas/baredom/x-dropdown";
import { init } from "@vanelsas/baredom/x-dropdown";

init();

export interface XDropdownProps {
  open?: boolean;
  disabled?: boolean;
  label?: string;
  placement?: string;
  onToggle?: (e: CustomEvent<{ open: boolean; source: string }>) => void;
  onChange?: (e: CustomEvent<{ open: boolean }>) => void;
  ref?: XDropdownElement | ((el: XDropdownElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XDropdown(props: XDropdownProps): JSX.Element {
  const [local, others] = splitProps(props, ["onToggle", "onChange", "ref", "children"]);
  let el!: XDropdownElement;

  const setRef = (r: XDropdownElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onToggle?.(e as CustomEvent<{ open: boolean; source: string }>);
      el.addEventListener("x-dropdown-toggle", handler);
      onCleanup(() => el.removeEventListener("x-dropdown-toggle", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ open: boolean }>);
      el.addEventListener("x-dropdown-change", handler);
      onCleanup(() => el.removeEventListener("x-dropdown-change", handler));
    }
  });

  return (
    <x-dropdown ref={setRef} {...others}>
      {local.children}
    </x-dropdown>
  );
}
