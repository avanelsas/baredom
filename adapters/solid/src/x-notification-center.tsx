// x-notification-center.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XNotificationCenter as XNotificationCenterElement } from "@vanelsas/baredom/x-notification-center";
import { init } from "@vanelsas/baredom/x-notification-center";

init();

export interface XNotificationCenterProps {
  position?: string;
  max?: number;
  onPush?: (e: CustomEvent<{ id: string; count: number }>) => void;
  onDismiss?: (e: CustomEvent<{ id: string; type: string; reason: string; text: string; count: number }>) => void;
  onEmpty?: (e: CustomEvent<{}>) => void;
  ref?: XNotificationCenterElement | ((el: XNotificationCenterElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XNotificationCenter(props: XNotificationCenterProps): JSX.Element {
  const [local, others] = splitProps(props, ["onPush", "onDismiss", "onEmpty", "ref", "children"]);
  let el!: XNotificationCenterElement;

  const setRef = (r: XNotificationCenterElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onPush?.(e as CustomEvent<{ id: string; count: number }>);
      el.addEventListener("x-notification-center-push", handler);
      onCleanup(() => el.removeEventListener("x-notification-center-push", handler));
    }
    {
      const handler = (e: Event) => local.onDismiss?.(e as CustomEvent<{ id: string; type: string; reason: string; text: string; count: number }>);
      el.addEventListener("x-notification-center-dismiss", handler);
      onCleanup(() => el.removeEventListener("x-notification-center-dismiss", handler));
    }
    {
      const handler = (e: Event) => local.onEmpty?.(e as CustomEvent<{}>);
      el.addEventListener("x-notification-center-empty", handler);
      onCleanup(() => el.removeEventListener("x-notification-center-empty", handler));
    }
  });

  return (
    <x-notification-center ref={setRef} {...others}>
      {local.children}
    </x-notification-center>
  );
}
