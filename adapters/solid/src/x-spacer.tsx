// x-spacer.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XSpacer as XSpacerElement } from "@vanelsas/baredom/x-spacer";
import { init } from "@vanelsas/baredom/x-spacer";

init();

export interface XSpacerProps {
  size?: string;
  axis?: string;
  grow?: boolean;
  ref?: XSpacerElement | ((el: XSpacerElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XSpacer(props: XSpacerProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XSpacerElement;

  const setRef = (r: XSpacerElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-spacer ref={setRef} {...others}>
      {local.children}
    </x-spacer>
  );
}
