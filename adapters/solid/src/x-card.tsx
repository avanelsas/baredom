// x-card.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XCard as XCardElement } from "@vanelsas/baredom/x-card";
import { init } from "@vanelsas/baredom/x-card";

init();

export interface XCardProps {
  interactive?: boolean;
  disabled?: boolean;
  onPress?: (e: CustomEvent<{}>) => void;
  ref?: XCardElement | ((el: XCardElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XCard(props: XCardProps): JSX.Element {
  const [local, others] = splitProps(props, ["onPress", "ref", "children"]);
  let el!: XCardElement;

  const setRef = (r: XCardElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onPress?.(e as CustomEvent<{}>);
      el.addEventListener("press", handler);
      onCleanup(() => el.removeEventListener("press", handler));
    }
  });

  return (
    <x-card ref={setRef} {...others}>
      {local.children}
    </x-card>
  );
}
