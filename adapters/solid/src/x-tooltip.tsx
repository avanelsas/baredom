// x-tooltip.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XTooltip as XTooltipElement } from "@vanelsas/baredom/x-tooltip";
import { init } from "@vanelsas/baredom/x-tooltip";

init();

export interface XTooltipProps {
  text?: string;
  placement?: string;
  delay?: number;
  disabled?: boolean;
  open?: boolean;
  onShow?: (e: CustomEvent<{}>) => void;
  onHide?: (e: CustomEvent<{}>) => void;
  ref?: XTooltipElement | ((el: XTooltipElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XTooltip(props: XTooltipProps): JSX.Element {
  const [local, others] = splitProps(props, ["onShow", "onHide", "ref", "children"]);
  let el!: XTooltipElement;

  const setRef = (r: XTooltipElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onShow?.(e as CustomEvent<{}>);
      el.addEventListener("x-tooltip-show", handler);
      onCleanup(() => el.removeEventListener("x-tooltip-show", handler));
    }
    {
      const handler = (e: Event) => local.onHide?.(e as CustomEvent<{}>);
      el.addEventListener("x-tooltip-hide", handler);
      onCleanup(() => el.removeEventListener("x-tooltip-hide", handler));
    }
  });

  return (
    <x-tooltip ref={setRef} {...others}>
      {local.children}
    </x-tooltip>
  );
}
