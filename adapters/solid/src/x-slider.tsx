// x-slider.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup, createEffect } from "solid-js";
import type { JSX } from "solid-js";
import type { XSlider as XSliderElement } from "@vanelsas/baredom/x-slider";
import { init } from "@vanelsas/baredom/x-slider";

init();

export interface XSliderProps {
  min?: string;
  disabled?: boolean;
  showValue?: boolean;
  name?: string;
  value?: string;
  size?: string;
  readOnly?: boolean;
  max?: string;
  label?: string;
  step?: string;
  defaultValue?: string;
  onChangeRequest?: (e: CustomEvent<{ value: number; previousValue: number; min: number; max: number }>) => void;
  onInput?: (e: CustomEvent<{ value: number; min: number; max: number }>) => void;
  onChange?: (e: CustomEvent<{ value: number; min: number; max: number }>) => void;
  ref?: XSliderElement | ((el: XSliderElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XSlider(props: XSliderProps): JSX.Element {
  const [local, others] = splitProps(props, ["value", "defaultValue", "onChangeRequest", "onInput", "onChange", "ref", "children"]);
  let el!: XSliderElement;

  const setRef = (r: XSliderElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el || local.value !== undefined || local.defaultValue === undefined) return;
    el.setAttribute("value", String(local.defaultValue));
  });

  createEffect(() => {
    if (!el) return;
    const v = local.value;
    if (v === undefined) return;
    el.setAttribute("value", String(v));
  });

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => {
        if (local.value !== undefined) e.preventDefault();
        local.onChangeRequest?.(e as CustomEvent<{ value: number; previousValue: number; min: number; max: number }>);
      };
      el.addEventListener("x-slider-change-request", handler);
      onCleanup(() => el.removeEventListener("x-slider-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onInput?.(e as CustomEvent<{ value: number; min: number; max: number }>);
      el.addEventListener("x-slider-input", handler);
      onCleanup(() => el.removeEventListener("x-slider-input", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ value: number; min: number; max: number }>);
      el.addEventListener("x-slider-change", handler);
      onCleanup(() => el.removeEventListener("x-slider-change", handler));
    }
  });

  return (
    <x-slider ref={setRef} {...others}>
      {local.children}
    </x-slider>
  );
}
