// x-tabs.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup, createEffect } from "solid-js";
import type { JSX } from "solid-js";
import type { XTabs as XTabsElement } from "@vanelsas/baredom/x-tabs";
import { init } from "@vanelsas/baredom/x-tabs";

init();

export interface XTabsProps {
  value?: string;
  defaultValue?: string;
  onValueChangeRequest?: (e: CustomEvent<{ value: string; previousValue: string }>) => void;
  onValueChange?: (e: CustomEvent<{ value: string }>) => void;
  ref?: XTabsElement | ((el: XTabsElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XTabs(props: XTabsProps): JSX.Element {
  const [local, others] = splitProps(props, ["value", "defaultValue", "onValueChangeRequest", "onValueChange", "ref", "children"]);
  let el!: XTabsElement;

  const setRef = (r: XTabsElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el || local.value !== undefined || local.defaultValue === undefined) return;
    el.setAttribute("value", String(local.defaultValue));
  });

  createEffect(() => {
    if (!el) return;
    const v = local.value;
    if (v === undefined) return;
    el.setAttribute("value", String(v));
  });

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => {
        if (local.value !== undefined) e.preventDefault();
        local.onValueChangeRequest?.(e as CustomEvent<{ value: string; previousValue: string }>);
      };
      el.addEventListener("value-change-request", handler);
      onCleanup(() => el.removeEventListener("value-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onValueChange?.(e as CustomEvent<{ value: string }>);
      el.addEventListener("value-change", handler);
      onCleanup(() => el.removeEventListener("value-change", handler));
    }
  });

  return (
    <x-tabs ref={setRef} {...others}>
      {local.children}
    </x-tabs>
  );
}
