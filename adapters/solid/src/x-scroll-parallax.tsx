// x-scroll-parallax.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XScrollParallax as XScrollParallaxElement } from "@vanelsas/baredom/x-scroll-parallax";
import { init } from "@vanelsas/baredom/x-scroll-parallax";

init();

export interface XScrollParallaxProps {
  direction?: string;
  source?: string;
  easing?: string;
  disabled?: boolean;
  label?: string;
  onEnter?: (e: CustomEvent<{ progress: number }>) => void;
  onLeave?: (e: CustomEvent<{ progress: number }>) => void;
  onProgress?: (e: CustomEvent<{ progress: number }>) => void;
  ref?: XScrollParallaxElement | ((el: XScrollParallaxElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XScrollParallax(props: XScrollParallaxProps): JSX.Element {
  const [local, others] = splitProps(props, ["onEnter", "onLeave", "onProgress", "ref", "children"]);
  let el!: XScrollParallaxElement;

  const setRef = (r: XScrollParallaxElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onEnter?.(e as CustomEvent<{ progress: number }>);
      el.addEventListener("x-scroll-parallax-enter", handler);
      onCleanup(() => el.removeEventListener("x-scroll-parallax-enter", handler));
    }
    {
      const handler = (e: Event) => local.onLeave?.(e as CustomEvent<{ progress: number }>);
      el.addEventListener("x-scroll-parallax-leave", handler);
      onCleanup(() => el.removeEventListener("x-scroll-parallax-leave", handler));
    }
    {
      const handler = (e: Event) => local.onProgress?.(e as CustomEvent<{ progress: number }>);
      el.addEventListener("x-scroll-parallax-progress", handler);
      onCleanup(() => el.removeEventListener("x-scroll-parallax-progress", handler));
    }
  });

  return (
    <x-scroll-parallax ref={setRef} {...others}>
      {local.children}
    </x-scroll-parallax>
  );
}
