// x-menu.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XMenu as XMenuElement } from "@vanelsas/baredom/x-menu";
import { init } from "@vanelsas/baredom/x-menu";

init();

export interface XMenuProps {
  open?: boolean;
  placement?: string;
  label?: string;
  onOpen?: (e: CustomEvent<{}>) => void;
  onClose?: (e: CustomEvent<{}>) => void;
  onSelect?: (e: CustomEvent<{ value: string }>) => void;
  ref?: XMenuElement | ((el: XMenuElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XMenu(props: XMenuProps): JSX.Element {
  const [local, others] = splitProps(props, ["onOpen", "onClose", "onSelect", "ref", "children"]);
  let el!: XMenuElement;

  const setRef = (r: XMenuElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onOpen?.(e as CustomEvent<{}>);
      el.addEventListener("x-menu-open", handler);
      onCleanup(() => el.removeEventListener("x-menu-open", handler));
    }
    {
      const handler = (e: Event) => local.onClose?.(e as CustomEvent<{}>);
      el.addEventListener("x-menu-close", handler);
      onCleanup(() => el.removeEventListener("x-menu-close", handler));
    }
    {
      const handler = (e: Event) => local.onSelect?.(e as CustomEvent<{ value: string }>);
      el.addEventListener("x-menu-select", handler);
      onCleanup(() => el.removeEventListener("x-menu-select", handler));
    }
  });

  return (
    <x-menu ref={setRef} {...others}>
      {local.children}
    </x-menu>
  );
}
