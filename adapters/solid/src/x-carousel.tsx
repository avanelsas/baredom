// x-carousel.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XCarousel as XCarouselElement } from "@vanelsas/baredom/x-carousel";
import { init } from "@vanelsas/baredom/x-carousel";

init();

export interface XCarouselProps {
  arrows?: boolean;
  disabled?: boolean;
  transition?: string;
  loop?: boolean;
  interval?: number;
  autoplay?: boolean;
  peek?: string;
  dots?: boolean;
  currentSlide?: number;
  direction?: string;
  onChange?: (e: CustomEvent<{ index: number; previousIndex: number; reason: string }>) => void;
  ref?: XCarouselElement | ((el: XCarouselElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XCarousel(props: XCarouselProps): JSX.Element {
  const [local, others] = splitProps(props, ["onChange", "ref", "children"]);
  let el!: XCarouselElement;

  const setRef = (r: XCarouselElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ index: number; previousIndex: number; reason: string }>);
      el.addEventListener("x-carousel-change", handler);
      onCleanup(() => el.removeEventListener("x-carousel-change", handler));
    }
  });

  return (
    <x-carousel ref={setRef} {...others}>
      {local.children}
    </x-carousel>
  );
}
