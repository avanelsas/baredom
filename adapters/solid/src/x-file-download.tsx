// x-file-download.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XFileDownload as XFileDownloadElement } from "@vanelsas/baredom/x-file-download";
import { init } from "@vanelsas/baredom/x-file-download";

init();

export interface XFileDownloadProps {
  href?: string;
  filename?: string;
  disabled?: boolean;
  onClick?: (e: CustomEvent<{ href: string; filename: string }>) => void;
  ref?: XFileDownloadElement | ((el: XFileDownloadElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XFileDownload(props: XFileDownloadProps): JSX.Element {
  const [local, others] = splitProps(props, ["onClick", "ref", "children"]);
  let el!: XFileDownloadElement;

  const setRef = (r: XFileDownloadElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onClick?.(e as CustomEvent<{ href: string; filename: string }>);
      el.addEventListener("x-file-download-click", handler);
      onCleanup(() => el.removeEventListener("x-file-download-click", handler));
    }
  });

  return (
    <x-file-download ref={setRef} {...others}>
      {local.children}
    </x-file-download>
  );
}
