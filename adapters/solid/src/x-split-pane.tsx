// x-split-pane.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XSplitPane as XSplitPaneElement } from "@vanelsas/baredom/x-split-pane";
import { init } from "@vanelsas/baredom/x-split-pane";

init();

export interface XSplitPaneProps {
  orientation?: string;
  position?: number;
  minStart?: number;
  minEnd?: number;
  disabled?: boolean;
  dividerLabel?: string;
  onResize?: (e: CustomEvent<{ position: number; orientation: string }>) => void;
  onResizeEnd?: (e: CustomEvent<{ position: number; orientation: string }>) => void;
  ref?: XSplitPaneElement | ((el: XSplitPaneElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XSplitPane(props: XSplitPaneProps): JSX.Element {
  const [local, others] = splitProps(props, ["onResize", "onResizeEnd", "ref", "children"]);
  let el!: XSplitPaneElement;

  const setRef = (r: XSplitPaneElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onResize?.(e as CustomEvent<{ position: number; orientation: string }>);
      el.addEventListener("x-split-pane-resize", handler);
      onCleanup(() => el.removeEventListener("x-split-pane-resize", handler));
    }
    {
      const handler = (e: Event) => local.onResizeEnd?.(e as CustomEvent<{ position: number; orientation: string }>);
      el.addEventListener("x-split-pane-resize-end", handler);
      onCleanup(() => el.removeEventListener("x-split-pane-resize-end", handler));
    }
  });

  return (
    <x-split-pane ref={setRef} {...others}>
      {local.children}
    </x-split-pane>
  );
}
