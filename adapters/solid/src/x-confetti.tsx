// x-confetti.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XConfetti as XConfettiElement } from "@vanelsas/baredom/x-confetti";
import { init } from "@vanelsas/baredom/x-confetti";

init();

export interface XConfettiProps {
  disabled?: boolean;
  spread?: number;
  mode?: string;
  colors?: string;
  duration?: number;
  autoFire?: boolean;
  gravity?: number;
  count?: number;
  origin?: string;
  velocity?: number;
  shapes?: string;
  onFire?: (e: CustomEvent<{ count: number; origin: string }>) => void;
  onEnd?: (e: CustomEvent<{ duration: number }>) => void;
  ref?: XConfettiElement | ((el: XConfettiElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XConfetti(props: XConfettiProps): JSX.Element {
  const [local, others] = splitProps(props, ["onFire", "onEnd", "ref", "children"]);
  let el!: XConfettiElement;

  const setRef = (r: XConfettiElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onFire?.(e as CustomEvent<{ count: number; origin: string }>);
      el.addEventListener("x-confetti-fire", handler);
      onCleanup(() => el.removeEventListener("x-confetti-fire", handler));
    }
    {
      const handler = (e: Event) => local.onEnd?.(e as CustomEvent<{ duration: number }>);
      el.addEventListener("x-confetti-end", handler);
      onCleanup(() => el.removeEventListener("x-confetti-end", handler));
    }
  });

  return (
    <x-confetti ref={setRef} {...others}>
      {local.children}
    </x-confetti>
  );
}
