// x-timeline.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XTimeline as XTimelineElement } from "@vanelsas/baredom/x-timeline";
import { init } from "@vanelsas/baredom/x-timeline";

init();

export interface XTimelineProps {
  label?: string;
  position?: string;
  striped?: boolean;
  onSelect?: (e: CustomEvent<{ index: number; status: string; label: string }>) => void;
  ref?: XTimelineElement | ((el: XTimelineElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XTimeline(props: XTimelineProps): JSX.Element {
  const [local, others] = splitProps(props, ["onSelect", "ref", "children"]);
  let el!: XTimelineElement;

  const setRef = (r: XTimelineElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onSelect?.(e as CustomEvent<{ index: number; status: string; label: string }>);
      el.addEventListener("x-timeline-select", handler);
      onCleanup(() => el.removeEventListener("x-timeline-select", handler));
    }
  });

  return (
    <x-timeline ref={setRef} {...others}>
      {local.children}
    </x-timeline>
  );
}
