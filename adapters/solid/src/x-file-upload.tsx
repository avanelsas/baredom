// x-file-upload.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XFileUpload as XFileUploadElement } from "@vanelsas/baredom/x-file-upload";
import { init } from "@vanelsas/baredom/x-file-upload";

init();

export interface XFileUploadProps {
  accept?: string;
  multiple?: boolean;
  maxSize?: number;
  maxFiles?: number;
  disabled?: boolean;
  required?: boolean;
  name?: string;
  onSelect?: (e: CustomEvent<{ files: any; rejected: any }>) => void;
  onRemove?: (e: CustomEvent<{ file: Record<string, any>; remaining: any }>) => void;
  ref?: XFileUploadElement | ((el: XFileUploadElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XFileUpload(props: XFileUploadProps): JSX.Element {
  const [local, others] = splitProps(props, ["onSelect", "onRemove", "ref", "children"]);
  let el!: XFileUploadElement;

  const setRef = (r: XFileUploadElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onSelect?.(e as CustomEvent<{ files: any; rejected: any }>);
      el.addEventListener("x-file-upload-select", handler);
      onCleanup(() => el.removeEventListener("x-file-upload-select", handler));
    }
    {
      const handler = (e: Event) => local.onRemove?.(e as CustomEvent<{ file: Record<string, any>; remaining: any }>);
      el.addEventListener("x-file-upload-remove", handler);
      onCleanup(() => el.removeEventListener("x-file-upload-remove", handler));
    }
  });

  return (
    <x-file-upload ref={setRef} {...others}>
      {local.children}
    </x-file-upload>
  );
}
