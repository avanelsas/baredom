// x-divider.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XDivider as XDividerElement } from "@vanelsas/baredom/x-divider";
import { init } from "@vanelsas/baredom/x-divider";

init();

export interface XDividerProps {
  orientation?: string;
  variant?: string;
  align?: string;
  label?: string;
  thickness?: string;
  color?: string;
  inset?: string;
  length?: string;
  ref?: XDividerElement | ((el: XDividerElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XDivider(props: XDividerProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XDividerElement;

  const setRef = (r: XDividerElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-divider ref={setRef} {...others}>
      {local.children}
    </x-divider>
  );
}
