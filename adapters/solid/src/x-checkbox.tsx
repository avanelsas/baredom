// x-checkbox.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup, createEffect } from "solid-js";
import type { JSX } from "solid-js";
import type { XCheckbox as XCheckboxElement } from "@vanelsas/baredom/x-checkbox";
import { init } from "@vanelsas/baredom/x-checkbox";

init();

export interface XCheckboxProps {
  checked?: boolean;
  indeterminate?: boolean;
  disabled?: boolean;
  readOnly?: boolean;
  required?: boolean;
  name?: string;
  value?: string;
  defaultChecked?: boolean;
  onChangeRequest?: (e: CustomEvent<{ value: string; previousChecked: boolean; nextChecked: boolean }>) => void;
  onChange?: (e: CustomEvent<{ value: string; checked: boolean }>) => void;
  ref?: XCheckboxElement | ((el: XCheckboxElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XCheckbox(props: XCheckboxProps): JSX.Element {
  const [local, others] = splitProps(props, ["checked", "defaultChecked", "onChangeRequest", "onChange", "ref", "children"]);
  let el!: XCheckboxElement;

  const setRef = (r: XCheckboxElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el || local.checked !== undefined || local.defaultChecked === undefined) return;
    if (local.defaultChecked) el.setAttribute("checked", "");
    else el.removeAttribute("checked");
  });

  createEffect(() => {
    if (!el) return;
    const v = local.checked;
    if (v === undefined) return;
    if (v) el.setAttribute("checked", "");
    else el.removeAttribute("checked");
  });

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => {
        if (local.checked !== undefined) e.preventDefault();
        local.onChangeRequest?.(e as CustomEvent<{ value: string; previousChecked: boolean; nextChecked: boolean }>);
      };
      el.addEventListener("x-checkbox-change-request", handler);
      onCleanup(() => el.removeEventListener("x-checkbox-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ value: string; checked: boolean }>);
      el.addEventListener("x-checkbox-change", handler);
      onCleanup(() => el.removeEventListener("x-checkbox-change", handler));
    }
  });

  return (
    <x-checkbox ref={setRef} {...others}>
      {local.children}
    </x-checkbox>
  );
}
