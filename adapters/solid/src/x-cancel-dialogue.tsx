// x-cancel-dialogue.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XCancelDialogue as XCancelDialogueElement } from "@vanelsas/baredom/x-cancel-dialogue";
import { init } from "@vanelsas/baredom/x-cancel-dialogue";

init();

export interface XCancelDialogueProps {
  open?: boolean;
  disabled?: boolean;
  headline?: string;
  message?: string;
  confirmText?: string;
  cancelText?: string;
  danger?: boolean;
  onCancelRequest?: (e: CustomEvent<{ reason: string }>) => void;
  onCancel?: (e: CustomEvent<{}>) => void;
  onConfirmRequest?: (e: CustomEvent<{}>) => void;
  onConfirm?: (e: CustomEvent<{}>) => void;
  ref?: XCancelDialogueElement | ((el: XCancelDialogueElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XCancelDialogue(props: XCancelDialogueProps): JSX.Element {
  const [local, others] = splitProps(props, ["onCancelRequest", "onCancel", "onConfirmRequest", "onConfirm", "ref", "children"]);
  let el!: XCancelDialogueElement;

  const setRef = (r: XCancelDialogueElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onCancelRequest?.(e as CustomEvent<{ reason: string }>);
      el.addEventListener("x-cancel-dialogue-cancel-request", handler);
      onCleanup(() => el.removeEventListener("x-cancel-dialogue-cancel-request", handler));
    }
    {
      const handler = (e: Event) => local.onCancel?.(e as CustomEvent<{}>);
      el.addEventListener("x-cancel-dialogue-cancel", handler);
      onCleanup(() => el.removeEventListener("x-cancel-dialogue-cancel", handler));
    }
    {
      const handler = (e: Event) => local.onConfirmRequest?.(e as CustomEvent<{}>);
      el.addEventListener("x-cancel-dialogue-confirm-request", handler);
      onCleanup(() => el.removeEventListener("x-cancel-dialogue-confirm-request", handler));
    }
    {
      const handler = (e: Event) => local.onConfirm?.(e as CustomEvent<{}>);
      el.addEventListener("x-cancel-dialogue-confirm", handler);
      onCleanup(() => el.removeEventListener("x-cancel-dialogue-confirm", handler));
    }
  });

  return (
    <x-cancel-dialogue ref={setRef} {...others}>
      {local.children}
    </x-cancel-dialogue>
  );
}
