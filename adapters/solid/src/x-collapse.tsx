// x-collapse.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XCollapse as XCollapseElement } from "@vanelsas/baredom/x-collapse";
import { init } from "@vanelsas/baredom/x-collapse";

init();

export interface XCollapseProps {
  open?: boolean;
  disabled?: boolean;
  header?: string;
  durationMs?: number;
  onToggle?: (e: CustomEvent<{ open: boolean; source: string }>) => void;
  onChange?: (e: CustomEvent<{ open: boolean }>) => void;
  ref?: XCollapseElement | ((el: XCollapseElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XCollapse(props: XCollapseProps): JSX.Element {
  const [local, others] = splitProps(props, ["onToggle", "onChange", "ref", "children"]);
  let el!: XCollapseElement;

  const setRef = (r: XCollapseElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onToggle?.(e as CustomEvent<{ open: boolean; source: string }>);
      el.addEventListener("x-collapse-toggle", handler);
      onCleanup(() => el.removeEventListener("x-collapse-toggle", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ open: boolean }>);
      el.addEventListener("x-collapse-change", handler);
      onCleanup(() => el.removeEventListener("x-collapse-change", handler));
    }
  });

  return (
    <x-collapse ref={setRef} {...others}>
      {local.children}
    </x-collapse>
  );
}
