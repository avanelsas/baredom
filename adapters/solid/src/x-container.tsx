// x-container.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XContainer as XContainerElement } from "@vanelsas/baredom/x-container";
import { init } from "@vanelsas/baredom/x-container";

init();

export interface XContainerProps {
  center?: boolean;
  fluid?: boolean;
  ref?: XContainerElement | ((el: XContainerElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XContainer(props: XContainerProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XContainerElement;

  const setRef = (r: XContainerElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-container ref={setRef} {...others}>
      {local.children}
    </x-container>
  );
}
