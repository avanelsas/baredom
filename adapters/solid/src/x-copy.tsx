// x-copy.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XCopy as XCopyElement } from "@vanelsas/baredom/x-copy";
import { init } from "@vanelsas/baredom/x-copy";

init();

export interface XCopyProps {
  errorMessage?: string;
  disabled?: boolean;
  textValue?: string;
  mode?: string;
  fromAttr?: string;
  tooltipMs?: number;
  hotkey?: string;
  from?: string;
  showTooltip?: boolean;
  successMessage?: string;
  text?: string;
  onRequest?: (e: CustomEvent<{ text: string; mode: string; from: string; fromAttr: string }>) => void;
  onSuccess?: (e: CustomEvent<{ text: string }>) => void;
  onError?: (e: CustomEvent<{ error: string }>) => void;
  ref?: XCopyElement | ((el: XCopyElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XCopy(props: XCopyProps): JSX.Element {
  const [local, others] = splitProps(props, ["onRequest", "onSuccess", "onError", "ref", "children"]);
  let el!: XCopyElement;

  const setRef = (r: XCopyElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onRequest?.(e as CustomEvent<{ text: string; mode: string; from: string; fromAttr: string }>);
      el.addEventListener("x-copy-request", handler);
      onCleanup(() => el.removeEventListener("x-copy-request", handler));
    }
    {
      const handler = (e: Event) => local.onSuccess?.(e as CustomEvent<{ text: string }>);
      el.addEventListener("x-copy-success", handler);
      onCleanup(() => el.removeEventListener("x-copy-success", handler));
    }
    {
      const handler = (e: Event) => local.onError?.(e as CustomEvent<{ error: string }>);
      el.addEventListener("x-copy-error", handler);
      onCleanup(() => el.removeEventListener("x-copy-error", handler));
    }
  });

  return (
    <x-copy ref={setRef} {...others}>
      {local.children}
    </x-copy>
  );
}
