// x-popover.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XPopover as XPopoverElement } from "@vanelsas/baredom/x-popover";
import { init } from "@vanelsas/baredom/x-popover";

init();

export interface XPopoverProps {
  open?: boolean;
  placement?: string;
  heading?: string;
  closeLabel?: string;
  noClose?: boolean;
  disabled?: boolean;
  portal?: boolean;
  onToggle?: (e: CustomEvent<{ open: boolean; source: string }>) => void;
  onChange?: (e: CustomEvent<{ open: boolean }>) => void;
  ref?: XPopoverElement | ((el: XPopoverElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XPopover(props: XPopoverProps): JSX.Element {
  const [local, others] = splitProps(props, ["onToggle", "onChange", "ref", "children"]);
  let el!: XPopoverElement;

  const setRef = (r: XPopoverElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onToggle?.(e as CustomEvent<{ open: boolean; source: string }>);
      el.addEventListener("x-popover-toggle", handler);
      onCleanup(() => el.removeEventListener("x-popover-toggle", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ open: boolean }>);
      el.addEventListener("x-popover-change", handler);
      onCleanup(() => el.removeEventListener("x-popover-change", handler));
    }
  });

  return (
    <x-popover ref={setRef} {...others}>
      {local.children}
    </x-popover>
  );
}
