// x-multi-combobox.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup, createEffect } from "solid-js";
import type { JSX } from "solid-js";
import type { XMultiCombobox as XMultiComboboxElement } from "@vanelsas/baredom/x-multi-combobox";
import { init } from "@vanelsas/baredom/x-multi-combobox";

init();

export interface XMultiComboboxProps {
  open?: boolean;
  placeholder?: string;
  disabled?: boolean;
  name?: string;
  value?: string[];
  max?: number;
  error?: string;
  placement?: string;
  required?: boolean;
  defaultValue?: string[];
  onChangeRequest?: (e: CustomEvent<{ value: any; action: string; item: string }>) => void;
  onChange?: (e: CustomEvent<{ value: any }>) => void;
  onInput?: (e: CustomEvent<{ query: string }>) => void;
  onToggle?: (e: CustomEvent<{ open: boolean; source: string }>) => void;
  ref?: XMultiComboboxElement | ((el: XMultiComboboxElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XMultiCombobox(props: XMultiComboboxProps): JSX.Element {
  const [local, others] = splitProps(props, ["value", "defaultValue", "onChangeRequest", "onChange", "onInput", "onToggle", "ref", "children"]);
  let el!: XMultiComboboxElement;

  const setRef = (r: XMultiComboboxElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el || local.value !== undefined || local.defaultValue === undefined) return;
    el.setAttribute("value", String(local.defaultValue));
  });

  createEffect(() => {
    if (!el) return;
    const v = local.value;
    if (v === undefined) return;
    el.setAttribute("value", String(v));
  });

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => {
        if (local.value !== undefined) e.preventDefault();
        local.onChangeRequest?.(e as CustomEvent<{ value: any; action: string; item: string }>);
      };
      el.addEventListener("x-multi-combobox-change-request", handler);
      onCleanup(() => el.removeEventListener("x-multi-combobox-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ value: any }>);
      el.addEventListener("x-multi-combobox-change", handler);
      onCleanup(() => el.removeEventListener("x-multi-combobox-change", handler));
    }
    {
      const handler = (e: Event) => local.onInput?.(e as CustomEvent<{ query: string }>);
      el.addEventListener("x-multi-combobox-input", handler);
      onCleanup(() => el.removeEventListener("x-multi-combobox-input", handler));
    }
    {
      const handler = (e: Event) => local.onToggle?.(e as CustomEvent<{ open: boolean; source: string }>);
      el.addEventListener("x-multi-combobox-toggle", handler);
      onCleanup(() => el.removeEventListener("x-multi-combobox-toggle", handler));
    }
  });

  return (
    <x-multi-combobox ref={setRef} {...others}>
      {local.children}
    </x-multi-combobox>
  );
}
