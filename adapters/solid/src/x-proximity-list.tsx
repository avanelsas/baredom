// x-proximity-list.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XProximityList as XProximityListElement } from "@vanelsas/baredom/x-proximity-list";
import { init } from "@vanelsas/baredom/x-proximity-list";

init();

export interface XProximityListProps {
  direction?: string;
  radius?: number;
  maxScale?: number;
  lift?: number;
  gap?: number;
  disabled?: boolean;
  onSelect?: (e: CustomEvent<{ index: number; item: any; source: string }>) => void;
  ref?: XProximityListElement | ((el: XProximityListElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XProximityList(props: XProximityListProps): JSX.Element {
  const [local, others] = splitProps(props, ["onSelect", "ref", "children"]);
  let el!: XProximityListElement;

  const setRef = (r: XProximityListElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onSelect?.(e as CustomEvent<{ index: number; item: any; source: string }>);
      el.addEventListener("x-proximity-list-select", handler);
      onCleanup(() => el.removeEventListener("x-proximity-list-select", handler));
    }
  });

  return (
    <x-proximity-list ref={setRef} {...others}>
      {local.children}
    </x-proximity-list>
  );
}
