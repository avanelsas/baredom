// x-modal.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XModal as XModalElement } from "@vanelsas/baredom/x-modal";
import { init } from "@vanelsas/baredom/x-modal";

init();

export interface XModalProps {
  open?: boolean;
  size?: string;
  label?: string;
  onToggle?: (e: CustomEvent<{ open: boolean }>) => void;
  onDismiss?: (e: CustomEvent<{ reason: string }>) => void;
  ref?: XModalElement | ((el: XModalElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XModal(props: XModalProps): JSX.Element {
  const [local, others] = splitProps(props, ["onToggle", "onDismiss", "ref", "children"]);
  let el!: XModalElement;

  const setRef = (r: XModalElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onToggle?.(e as CustomEvent<{ open: boolean }>);
      el.addEventListener("x-modal-toggle", handler);
      onCleanup(() => el.removeEventListener("x-modal-toggle", handler));
    }
    {
      const handler = (e: Event) => local.onDismiss?.(e as CustomEvent<{ reason: string }>);
      el.addEventListener("x-modal-dismiss", handler);
      onCleanup(() => el.removeEventListener("x-modal-dismiss", handler));
    }
  });

  return (
    <x-modal ref={setRef} {...others}>
      {local.children}
    </x-modal>
  );
}
