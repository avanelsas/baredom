// x-splash.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XSplash as XSplashElement } from "@vanelsas/baredom/x-splash";
import { init } from "@vanelsas/baredom/x-splash";

init();

export interface XSplashProps {
  active?: boolean;
  variant?: string;
  progress?: number;
  spinner?: boolean;
  overlay?: string;
  onHidden?: (e: CustomEvent<{}>) => void;
  ref?: XSplashElement | ((el: XSplashElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XSplash(props: XSplashProps): JSX.Element {
  const [local, others] = splitProps(props, ["onHidden", "ref", "children"]);
  let el!: XSplashElement;

  const setRef = (r: XSplashElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onHidden?.(e as CustomEvent<{}>);
      el.addEventListener("x-splash-hidden", handler);
      onCleanup(() => el.removeEventListener("x-splash-hidden", handler));
    }
  });

  return (
    <x-splash ref={setRef} {...others}>
      {local.children}
    </x-splash>
  );
}
