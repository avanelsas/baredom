// x-rating.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XRating as XRatingElement } from "@vanelsas/baredom/x-rating";
import { init } from "@vanelsas/baredom/x-rating";

init();

export interface XRatingProps {
  allowClear?: boolean;
  disabled?: boolean;
  name?: string;
  value?: string;
  precision?: string;
  size?: string;
  readOnly?: boolean;
  max?: string;
  label?: string;
  shape?: string;
  onChangeRequest?: (e: CustomEvent<{ value: number; previousValue: number; max: number }>) => void;
  onChange?: (e: CustomEvent<{ value: number; max: number }>) => void;
  onHover?: (e: CustomEvent<{ value: number; max: number }>) => void;
  ref?: XRatingElement | ((el: XRatingElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XRating(props: XRatingProps): JSX.Element {
  const [local, others] = splitProps(props, ["onChangeRequest", "onChange", "onHover", "ref", "children"]);
  let el!: XRatingElement;

  const setRef = (r: XRatingElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onChangeRequest?.(e as CustomEvent<{ value: number; previousValue: number; max: number }>);
      el.addEventListener("x-rating-change-request", handler);
      onCleanup(() => el.removeEventListener("x-rating-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ value: number; max: number }>);
      el.addEventListener("x-rating-change", handler);
      onCleanup(() => el.removeEventListener("x-rating-change", handler));
    }
    {
      const handler = (e: Event) => local.onHover?.(e as CustomEvent<{ value: number; max: number }>);
      el.addEventListener("x-rating-hover", handler);
      onCleanup(() => el.removeEventListener("x-rating-hover", handler));
    }
  });

  return (
    <x-rating ref={setRef} {...others}>
      {local.children}
    </x-rating>
  );
}
