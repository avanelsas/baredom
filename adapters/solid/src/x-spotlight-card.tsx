// x-spotlight-card.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XSpotlightCard as XSpotlightCardElement } from "@vanelsas/baredom/x-spotlight-card";
import { init } from "@vanelsas/baredom/x-spotlight-card";

init();

export interface XSpotlightCardProps {
  variant?: string;
  radius?: string;
  padding?: string;
  color?: string;
  intensity?: string;
  size?: string;
  static?: boolean;
  ref?: XSpotlightCardElement | ((el: XSpotlightCardElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XSpotlightCard(props: XSpotlightCardProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XSpotlightCardElement;

  const setRef = (r: XSpotlightCardElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-spotlight-card ref={setRef} {...others}>
      {local.children}
    </x-spotlight-card>
  );
}
