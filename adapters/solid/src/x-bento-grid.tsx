// x-bento-grid.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XBentoGrid as XBentoGridElement } from "@vanelsas/baredom/x-bento-grid";
import { init } from "@vanelsas/baredom/x-bento-grid";

init();

export interface XBentoGridProps {
  ref?: XBentoGridElement | ((el: XBentoGridElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XBentoGrid(props: XBentoGridProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XBentoGridElement;

  const setRef = (r: XBentoGridElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-bento-grid ref={setRef} {...others}>
      {local.children}
    </x-bento-grid>
  );
}
