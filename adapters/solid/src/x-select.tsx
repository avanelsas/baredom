// x-select.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup, createEffect } from "solid-js";
import type { JSX } from "solid-js";
import type { XSelect as XSelectElement } from "@vanelsas/baredom/x-select";
import { init } from "@vanelsas/baredom/x-select";

init();

export interface XSelectProps {
  disabled?: boolean;
  required?: boolean;
  value?: string;
  defaultValue?: string;
  onChangeRequest?: (e: CustomEvent<{ value: string; label: string; previousValue: string }>) => void;
  onSelectChange?: (e: CustomEvent<{ value: string; label: string }>) => void;
  ref?: XSelectElement | ((el: XSelectElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XSelect(props: XSelectProps): JSX.Element {
  const [local, others] = splitProps(props, ["value", "defaultValue", "onChangeRequest", "onSelectChange", "ref", "children"]);
  let el!: XSelectElement;

  const setRef = (r: XSelectElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el || local.value !== undefined || local.defaultValue === undefined) return;
    el.setAttribute("value", String(local.defaultValue));
  });

  createEffect(() => {
    if (!el) return;
    const v = local.value;
    if (v === undefined) return;
    el.setAttribute("value", String(v));
  });

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => {
        if (local.value !== undefined) e.preventDefault();
        local.onChangeRequest?.(e as CustomEvent<{ value: string; label: string; previousValue: string }>);
      };
      el.addEventListener("x-select-change-request", handler);
      onCleanup(() => el.removeEventListener("x-select-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onSelectChange?.(e as CustomEvent<{ value: string; label: string }>);
      el.addEventListener("select-change", handler);
      onCleanup(() => el.removeEventListener("select-change", handler));
    }
  });

  return (
    <x-select ref={setRef} {...others}>
      {local.children}
    </x-select>
  );
}
