// x-image.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XImage as XImageElement } from "@vanelsas/baredom/x-image";
import { init } from "@vanelsas/baredom/x-image";

init();

export interface XImageProps {
  alt?: string;
  fit?: string;
  src?: string;
  decorative?: boolean;
  loading?: string;
  ratio?: string;
  position?: string;
  onLoad?: (e: CustomEvent<{ src: string; naturalWidth: number; naturalHeight: number }>) => void;
  onError?: (e: CustomEvent<{ src: string }>) => void;
  ref?: XImageElement | ((el: XImageElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XImage(props: XImageProps): JSX.Element {
  const [local, others] = splitProps(props, ["onLoad", "onError", "ref", "children"]);
  let el!: XImageElement;

  const setRef = (r: XImageElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onLoad?.(e as CustomEvent<{ src: string; naturalWidth: number; naturalHeight: number }>);
      el.addEventListener("x-image-load", handler);
      onCleanup(() => el.removeEventListener("x-image-load", handler));
    }
    {
      const handler = (e: Event) => local.onError?.(e as CustomEvent<{ src: string }>);
      el.addEventListener("x-image-error", handler);
      onCleanup(() => el.removeEventListener("x-image-error", handler));
    }
  });

  return (
    <x-image ref={setRef} {...others}>
      {local.children}
    </x-image>
  );
}
