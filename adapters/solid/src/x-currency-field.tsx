// x-currency-field.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup, createEffect } from "solid-js";
import type { JSX } from "solid-js";
import type { XCurrencyField as XCurrencyFieldElement } from "@vanelsas/baredom/x-currency-field";
import { init } from "@vanelsas/baredom/x-currency-field";

init();

export interface XCurrencyFieldProps {
  min?: string;
  placeholder?: string;
  disabled?: boolean;
  locale?: string;
  name?: string;
  value?: string;
  hint?: string;
  currency?: string;
  readOnly?: boolean;
  max?: string;
  label?: string;
  error?: string;
  required?: boolean;
  defaultValue?: string;
  onChangeRequest?: (e: CustomEvent<{ name: string; value: string; previousValue: string }>) => void;
  onInput?: (e: CustomEvent<{ name: string; value: string }>) => void;
  onChange?: (e: CustomEvent<{ name: string; value: string }>) => void;
  ref?: XCurrencyFieldElement | ((el: XCurrencyFieldElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XCurrencyField(props: XCurrencyFieldProps): JSX.Element {
  const [local, others] = splitProps(props, ["value", "defaultValue", "onChangeRequest", "onInput", "onChange", "ref", "children"]);
  let el!: XCurrencyFieldElement;

  const setRef = (r: XCurrencyFieldElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el || local.value !== undefined || local.defaultValue === undefined) return;
    el.setAttribute("value", String(local.defaultValue));
  });

  createEffect(() => {
    if (!el) return;
    const v = local.value;
    if (v === undefined) return;
    el.setAttribute("value", String(v));
  });

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => {
        if (local.value !== undefined) e.preventDefault();
        local.onChangeRequest?.(e as CustomEvent<{ name: string; value: string; previousValue: string }>);
      };
      el.addEventListener("x-currency-field-change-request", handler);
      onCleanup(() => el.removeEventListener("x-currency-field-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onInput?.(e as CustomEvent<{ name: string; value: string }>);
      el.addEventListener("x-currency-field-input", handler);
      onCleanup(() => el.removeEventListener("x-currency-field-input", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ name: string; value: string }>);
      el.addEventListener("x-currency-field-change", handler);
      onCleanup(() => el.removeEventListener("x-currency-field-change", handler));
    }
  });

  return (
    <x-currency-field ref={setRef} {...others}>
      {local.children}
    </x-currency-field>
  );
}
