// x-chip.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XChip as XChipElement } from "@vanelsas/baredom/x-chip";
import { init } from "@vanelsas/baredom/x-chip";

init();

export interface XChipProps {
  label?: string;
  value?: string;
  removable?: boolean;
  disabled?: boolean;
  onRemove?: (e: CustomEvent<{ value: string; label: string }>) => void;
  ref?: XChipElement | ((el: XChipElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XChip(props: XChipProps): JSX.Element {
  const [local, others] = splitProps(props, ["onRemove", "ref", "children"]);
  let el!: XChipElement;

  const setRef = (r: XChipElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onRemove?.(e as CustomEvent<{ value: string; label: string }>);
      el.addEventListener("x-chip-remove", handler);
      onCleanup(() => el.removeEventListener("x-chip-remove", handler));
    }
  });

  return (
    <x-chip ref={setRef} {...others}>
      {local.children}
    </x-chip>
  );
}
