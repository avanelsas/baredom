// x-i18n-provider.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XI18nProvider as XI18nProviderElement } from "@vanelsas/baredom/x-i18n-provider";
import { init } from "@vanelsas/baredom/x-i18n-provider";

init();

export interface XI18nProviderProps {
  src?: string;
  locale?: string;
  fallbackLocale?: string;
  onXI18nLoading?: (e: CustomEvent<{ locale: string }>) => void;
  onXI18nChange?: (e: CustomEvent<{ locale: string }>) => void;
  onXI18nError?: (e: CustomEvent<{ locale: string; message: string }>) => void;
  ref?: XI18nProviderElement | ((el: XI18nProviderElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XI18nProvider(props: XI18nProviderProps): JSX.Element {
  const [local, others] = splitProps(props, ["onXI18nLoading", "onXI18nChange", "onXI18nError", "ref", "children"]);
  let el!: XI18nProviderElement;

  const setRef = (r: XI18nProviderElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onXI18nLoading?.(e as CustomEvent<{ locale: string }>);
      el.addEventListener("x-i18n-loading", handler);
      onCleanup(() => el.removeEventListener("x-i18n-loading", handler));
    }
    {
      const handler = (e: Event) => local.onXI18nChange?.(e as CustomEvent<{ locale: string }>);
      el.addEventListener("x-i18n-change", handler);
      onCleanup(() => el.removeEventListener("x-i18n-change", handler));
    }
    {
      const handler = (e: Event) => local.onXI18nError?.(e as CustomEvent<{ locale: string; message: string }>);
      el.addEventListener("x-i18n-error", handler);
      onCleanup(() => el.removeEventListener("x-i18n-error", handler));
    }
  });

  return (
    <x-i18n-provider ref={setRef} {...others}>
      {local.children}
    </x-i18n-provider>
  );
}
