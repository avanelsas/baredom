// x-range-slider.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XRangeSlider as XRangeSliderElement } from "@vanelsas/baredom/x-range-slider";
import { init } from "@vanelsas/baredom/x-range-slider";

init();

export interface XRangeSliderProps {
  min?: string;
  disabled?: boolean;
  showValue?: boolean;
  name?: string;
  start?: string;
  minGap?: string;
  size?: string;
  readOnly?: boolean;
  max?: string;
  label?: string;
  end?: string;
  step?: string;
  onChangeRequest?: (e: CustomEvent<{ start: number; end: number; previousStart: number; previousEnd: number; min: number; max: number }>) => void;
  onInput?: (e: CustomEvent<{ start: number; end: number; min: number; max: number }>) => void;
  onChange?: (e: CustomEvent<{ start: number; end: number; min: number; max: number }>) => void;
  ref?: XRangeSliderElement | ((el: XRangeSliderElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XRangeSlider(props: XRangeSliderProps): JSX.Element {
  const [local, others] = splitProps(props, ["onChangeRequest", "onInput", "onChange", "ref", "children"]);
  let el!: XRangeSliderElement;

  const setRef = (r: XRangeSliderElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onChangeRequest?.(e as CustomEvent<{ start: number; end: number; previousStart: number; previousEnd: number; min: number; max: number }>);
      el.addEventListener("x-range-slider-change-request", handler);
      onCleanup(() => el.removeEventListener("x-range-slider-change-request", handler));
    }
    {
      const handler = (e: Event) => local.onInput?.(e as CustomEvent<{ start: number; end: number; min: number; max: number }>);
      el.addEventListener("x-range-slider-input", handler);
      onCleanup(() => el.removeEventListener("x-range-slider-input", handler));
    }
    {
      const handler = (e: Event) => local.onChange?.(e as CustomEvent<{ start: number; end: number; min: number; max: number }>);
      el.addEventListener("x-range-slider-change", handler);
      onCleanup(() => el.removeEventListener("x-range-slider-change", handler));
    }
  });

  return (
    <x-range-slider ref={setRef} {...others}>
      {local.children}
    </x-range-slider>
  );
}
