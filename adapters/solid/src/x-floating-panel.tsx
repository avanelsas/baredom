// x-floating-panel.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XFloatingPanel as XFloatingPanelElement } from "@vanelsas/baredom/x-floating-panel";
import { init } from "@vanelsas/baredom/x-floating-panel";

init();

export interface XFloatingPanelProps {
  open?: boolean;
  x?: number;
  y?: number;
  step?: number;
  closable?: boolean;
  resizable?: boolean;
  label?: string;
  focusOnOpen?: boolean;
  onToggle?: (e: CustomEvent<{ open: boolean }>) => void;
  onDismissRequest?: (e: CustomEvent<{ reason: string }>) => void;
  onMove?: (e: CustomEvent<{ x: number; y: number; source: string }>) => void;
  ref?: XFloatingPanelElement | ((el: XFloatingPanelElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XFloatingPanel(props: XFloatingPanelProps): JSX.Element {
  const [local, others] = splitProps(props, ["onToggle", "onDismissRequest", "onMove", "ref", "children"]);
  let el!: XFloatingPanelElement;

  const setRef = (r: XFloatingPanelElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onToggle?.(e as CustomEvent<{ open: boolean }>);
      el.addEventListener("x-floating-panel-toggle", handler);
      onCleanup(() => el.removeEventListener("x-floating-panel-toggle", handler));
    }
    {
      const handler = (e: Event) => local.onDismissRequest?.(e as CustomEvent<{ reason: string }>);
      el.addEventListener("x-floating-panel-dismiss-request", handler);
      onCleanup(() => el.removeEventListener("x-floating-panel-dismiss-request", handler));
    }
    {
      const handler = (e: Event) => local.onMove?.(e as CustomEvent<{ x: number; y: number; source: string }>);
      el.addEventListener("x-floating-panel-move", handler);
      onCleanup(() => el.removeEventListener("x-floating-panel-move", handler));
    }
  });

  return (
    <x-floating-panel ref={setRef} {...others}>
      {local.children}
    </x-floating-panel>
  );
}
