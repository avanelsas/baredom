// x-command-palette.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XCommandPalette as XCommandPaletteElement } from "@vanelsas/baredom/x-command-palette";
import { init } from "@vanelsas/baredom/x-command-palette";

init();

export interface XCommandPaletteProps {
  items?: any;
  open?: boolean;
  disabled?: boolean;
  onOpenRequest?: (e: CustomEvent<{}>) => void;
  onOpen?: (e: CustomEvent<{}>) => void;
  onCloseRequest?: (e: CustomEvent<{}>) => void;
  onClose?: (e: CustomEvent<{}>) => void;
  onSelectRequest?: (e: CustomEvent<{ item: Record<string, any> }>) => void;
  onSelect?: (e: CustomEvent<{ item: Record<string, any> }>) => void;
  onQueryChange?: (e: CustomEvent<{ query: string }>) => void;
  ref?: XCommandPaletteElement | ((el: XCommandPaletteElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XCommandPalette(props: XCommandPaletteProps): JSX.Element {
  const [local, others] = splitProps(props, ["onOpenRequest", "onOpen", "onCloseRequest", "onClose", "onSelectRequest", "onSelect", "onQueryChange", "ref", "children"]);
  let el!: XCommandPaletteElement;

  const setRef = (r: XCommandPaletteElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onOpenRequest?.(e as CustomEvent<{}>);
      el.addEventListener("x-command-palette-open-request", handler);
      onCleanup(() => el.removeEventListener("x-command-palette-open-request", handler));
    }
    {
      const handler = (e: Event) => local.onOpen?.(e as CustomEvent<{}>);
      el.addEventListener("x-command-palette-open", handler);
      onCleanup(() => el.removeEventListener("x-command-palette-open", handler));
    }
    {
      const handler = (e: Event) => local.onCloseRequest?.(e as CustomEvent<{}>);
      el.addEventListener("x-command-palette-close-request", handler);
      onCleanup(() => el.removeEventListener("x-command-palette-close-request", handler));
    }
    {
      const handler = (e: Event) => local.onClose?.(e as CustomEvent<{}>);
      el.addEventListener("x-command-palette-close", handler);
      onCleanup(() => el.removeEventListener("x-command-palette-close", handler));
    }
    {
      const handler = (e: Event) => local.onSelectRequest?.(e as CustomEvent<{ item: Record<string, any> }>);
      el.addEventListener("x-command-palette-select-request", handler);
      onCleanup(() => el.removeEventListener("x-command-palette-select-request", handler));
    }
    {
      const handler = (e: Event) => local.onSelect?.(e as CustomEvent<{ item: Record<string, any> }>);
      el.addEventListener("x-command-palette-select", handler);
      onCleanup(() => el.removeEventListener("x-command-palette-select", handler));
    }
    {
      const handler = (e: Event) => local.onQueryChange?.(e as CustomEvent<{ query: string }>);
      el.addEventListener("x-command-palette-query-change", handler);
      onCleanup(() => el.removeEventListener("x-command-palette-query-change", handler));
    }
  });

  return (
    <x-command-palette ref={setRef} {...others}>
      {local.children}
    </x-command-palette>
  );
}
