// x-metaball-cursor.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XMetaballCursor as XMetaballCursorElement } from "@vanelsas/baredom/x-metaball-cursor";
import { init } from "@vanelsas/baredom/x-metaball-cursor";

init();

export interface XMetaballCursorProps {
  color?: string;
  blobSize?: number;
  noiseIntensity?: number;
  palette?: string;
  noiseScale?: number;
  noise?: boolean;
  threshold?: string;
  blur?: number;
  noiseSpeed?: number;
  blobCount?: number;
  ref?: XMetaballCursorElement | ((el: XMetaballCursorElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XMetaballCursor(props: XMetaballCursorProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XMetaballCursorElement;

  const setRef = (r: XMetaballCursorElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-metaball-cursor ref={setRef} {...others}>
      {local.children}
    </x-metaball-cursor>
  );
}
