// x-timeline-item.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XTimelineItem as XTimelineItemElement } from "@vanelsas/baredom/x-timeline-item";
import { init } from "@vanelsas/baredom/x-timeline-item";

init();

export interface XTimelineItemProps {
  label?: string;
  title?: string;
  status?: string;
  icon?: string;
  connector?: string;
  position?: string;
  disabled?: boolean;
  onConnected?: (e: CustomEvent<{ status: string; label: string; position: string; disabled: boolean }>) => void;
  onDisconnected?: (e: CustomEvent<{ status: string; label: string }>) => void;
  onClick?: (e: CustomEvent<{ status: string; label: string }>) => void;
  ref?: XTimelineItemElement | ((el: XTimelineItemElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XTimelineItem(props: XTimelineItemProps): JSX.Element {
  const [local, others] = splitProps(props, ["onConnected", "onDisconnected", "onClick", "ref", "children"]);
  let el!: XTimelineItemElement;

  const setRef = (r: XTimelineItemElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onConnected?.(e as CustomEvent<{ status: string; label: string; position: string; disabled: boolean }>);
      el.addEventListener("x-timeline-item-connected", handler);
      onCleanup(() => el.removeEventListener("x-timeline-item-connected", handler));
    }
    {
      const handler = (e: Event) => local.onDisconnected?.(e as CustomEvent<{ status: string; label: string }>);
      el.addEventListener("x-timeline-item-disconnected", handler);
      onCleanup(() => el.removeEventListener("x-timeline-item-disconnected", handler));
    }
    {
      const handler = (e: Event) => local.onClick?.(e as CustomEvent<{ status: string; label: string }>);
      el.addEventListener("x-timeline-item-click", handler);
      onCleanup(() => el.removeEventListener("x-timeline-item-click", handler));
    }
  });

  return (
    <x-timeline-item ref={setRef} {...others}>
      {local.children}
    </x-timeline-item>
  );
}
