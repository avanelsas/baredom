// x-scroll-story.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XScrollStory as XScrollStoryElement } from "@vanelsas/baredom/x-scroll-story";
import { init } from "@vanelsas/baredom/x-scroll-story";

init();

export interface XScrollStoryProps {
  autoplayLoop?: boolean;
  autoplaySpeed?: number;
  autoplayIndicator?: boolean;
  disabled?: boolean;
  layout?: string;
  threshold?: number;
  split?: number;
  label?: string;
  autoplay?: boolean;
  onStepChange?: (e: CustomEvent<{ index: number; id: string; previousIndex: number; previousId: string }>) => void;
  onStepEnter?: (e: CustomEvent<{ index: number; id: string; progress: number }>) => void;
  onStepLeave?: (e: CustomEvent<{ index: number; id: string; progress: number }>) => void;
  onProgress?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
  onEnter?: (e: CustomEvent<{ progress: number }>) => void;
  onLeave?: (e: CustomEvent<{ progress: number }>) => void;
  onAutoplayPause?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
  onAutoplayResume?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
  ref?: XScrollStoryElement | ((el: XScrollStoryElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XScrollStory(props: XScrollStoryProps): JSX.Element {
  const [local, others] = splitProps(props, ["onStepChange", "onStepEnter", "onStepLeave", "onProgress", "onEnter", "onLeave", "onAutoplayPause", "onAutoplayResume", "ref", "children"]);
  let el!: XScrollStoryElement;

  const setRef = (r: XScrollStoryElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onStepChange?.(e as CustomEvent<{ index: number; id: string; previousIndex: number; previousId: string }>);
      el.addEventListener("x-scroll-story-step-change", handler);
      onCleanup(() => el.removeEventListener("x-scroll-story-step-change", handler));
    }
    {
      const handler = (e: Event) => local.onStepEnter?.(e as CustomEvent<{ index: number; id: string; progress: number }>);
      el.addEventListener("x-scroll-story-step-enter", handler);
      onCleanup(() => el.removeEventListener("x-scroll-story-step-enter", handler));
    }
    {
      const handler = (e: Event) => local.onStepLeave?.(e as CustomEvent<{ index: number; id: string; progress: number }>);
      el.addEventListener("x-scroll-story-step-leave", handler);
      onCleanup(() => el.removeEventListener("x-scroll-story-step-leave", handler));
    }
    {
      const handler = (e: Event) => local.onProgress?.(e as CustomEvent<{ progress: number; activeIndex: number; activeId: string }>);
      el.addEventListener("x-scroll-story-progress", handler);
      onCleanup(() => el.removeEventListener("x-scroll-story-progress", handler));
    }
    {
      const handler = (e: Event) => local.onEnter?.(e as CustomEvent<{ progress: number }>);
      el.addEventListener("x-scroll-story-enter", handler);
      onCleanup(() => el.removeEventListener("x-scroll-story-enter", handler));
    }
    {
      const handler = (e: Event) => local.onLeave?.(e as CustomEvent<{ progress: number }>);
      el.addEventListener("x-scroll-story-leave", handler);
      onCleanup(() => el.removeEventListener("x-scroll-story-leave", handler));
    }
    {
      const handler = (e: Event) => local.onAutoplayPause?.(e as CustomEvent<{ progress: number; activeIndex: number; activeId: string }>);
      el.addEventListener("x-scroll-story-autoplay-pause", handler);
      onCleanup(() => el.removeEventListener("x-scroll-story-autoplay-pause", handler));
    }
    {
      const handler = (e: Event) => local.onAutoplayResume?.(e as CustomEvent<{ progress: number; activeIndex: number; activeId: string }>);
      el.addEventListener("x-scroll-story-autoplay-resume", handler);
      onCleanup(() => el.removeEventListener("x-scroll-story-autoplay-resume", handler));
    }
  });

  return (
    <x-scroll-story ref={setRef} {...others}>
      {local.children}
    </x-scroll-story>
  );
}
