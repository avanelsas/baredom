// x-progress.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XProgress as XProgressElement } from "@vanelsas/baredom/x-progress";
import { init } from "@vanelsas/baredom/x-progress";

init();

export interface XProgressProps {
  value?: string;
  max?: string;
  variant?: string;
  size?: string;
  label?: string;
  indeterminate?: boolean;
  showValue?: boolean;
  onComplete?: (e: CustomEvent<{ value: number; max: number }>) => void;
  ref?: XProgressElement | ((el: XProgressElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XProgress(props: XProgressProps): JSX.Element {
  const [local, others] = splitProps(props, ["onComplete", "ref", "children"]);
  let el!: XProgressElement;

  const setRef = (r: XProgressElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onComplete?.(e as CustomEvent<{ value: number; max: number }>);
      el.addEventListener("x-progress-complete", handler);
      onCleanup(() => el.removeEventListener("x-progress-complete", handler));
    }
  });

  return (
    <x-progress ref={setRef} {...others}>
      {local.children}
    </x-progress>
  );
}
