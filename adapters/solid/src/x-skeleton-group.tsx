// x-skeleton-group.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XSkeletonGroup as XSkeletonGroupElement } from "@vanelsas/baredom/x-skeleton-group";
import { init } from "@vanelsas/baredom/x-skeleton-group";

init();

export interface XSkeletonGroupProps {
  preset?: string;
  animation?: string;
  count?: number;
  ref?: XSkeletonGroupElement | ((el: XSkeletonGroupElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XSkeletonGroup(props: XSkeletonGroupProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XSkeletonGroupElement;

  const setRef = (r: XSkeletonGroupElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-skeleton-group ref={setRef} {...others}>
      {local.children}
    </x-skeleton-group>
  );
}
