// x-soft-body.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XSoftBody as XSoftBodyElement } from "@vanelsas/baredom/x-soft-body";
import { init } from "@vanelsas/baredom/x-soft-body";

init();

export interface XSoftBodyProps {
  stiffness?: number;
  damping?: number;
  radius?: number;
  intensity?: number;
  grabRadius?: number;
  disabled?: boolean;
  onGrab?: (e: CustomEvent<{}>) => void;
  onRelease?: (e: CustomEvent<{}>) => void;
  ref?: XSoftBodyElement | ((el: XSoftBodyElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XSoftBody(props: XSoftBodyProps): JSX.Element {
  const [local, others] = splitProps(props, ["onGrab", "onRelease", "ref", "children"]);
  let el!: XSoftBodyElement;

  const setRef = (r: XSoftBodyElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onGrab?.(e as CustomEvent<{}>);
      el.addEventListener("x-soft-body-grab", handler);
      onCleanup(() => el.removeEventListener("x-soft-body-grab", handler));
    }
    {
      const handler = (e: Event) => local.onRelease?.(e as CustomEvent<{}>);
      el.addEventListener("x-soft-body-release", handler);
      onCleanup(() => el.removeEventListener("x-soft-body-release", handler));
    }
  });

  return (
    <x-soft-body ref={setRef} {...others}>
      {local.children}
    </x-soft-body>
  );
}
