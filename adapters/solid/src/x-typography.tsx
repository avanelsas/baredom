// x-typography.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XTypography as XTypographyElement } from "@vanelsas/baredom/x-typography";
import { init } from "@vanelsas/baredom/x-typography";

init();

export interface XTypographyProps {
  variant?: string;
  align?: string;
  truncate?: boolean;
  lineClamp?: number;
  ref?: XTypographyElement | ((el: XTypographyElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XTypography(props: XTypographyProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XTypographyElement;

  const setRef = (r: XTypographyElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-typography ref={setRef} {...others}>
      {local.children}
    </x-typography>
  );
}
