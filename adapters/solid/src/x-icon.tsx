// x-icon.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";
import type { XIcon as XIconElement } from "@vanelsas/baredom/x-icon";
import { init } from "@vanelsas/baredom/x-icon";

init();

export interface XIconProps {
  size?: string;
  color?: string;
  label?: string;
  ref?: XIconElement | ((el: XIconElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XIcon(props: XIconProps): JSX.Element {
  const [local, others] = splitProps(props, ["ref", "children"]);
  let el!: XIconElement;

  const setRef = (r: XIconElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  return (
    <x-icon ref={setRef} {...others}>
      {local.children}
    </x-icon>
  );
}
