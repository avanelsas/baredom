// x-welcome-tour.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XWelcomeTour as XWelcomeTourElement } from "@vanelsas/baredom/x-welcome-tour";
import { init } from "@vanelsas/baredom/x-welcome-tour";

init();

export interface XWelcomeTourProps {
  connector?: string;
  open?: boolean;
  prevLabel?: string;
  nextLabel?: string;
  counter?: boolean;
  doneLabel?: string;
  skipLabel?: string;
  dots?: boolean;
  step?: number;
  onStart?: (e: CustomEvent<{}>) => void;
  onStepChange?: (e: CustomEvent<{ step: number; previousStep: number }>) => void;
  onComplete?: (e: CustomEvent<{ stepsCompleted: number }>) => void;
  onSkip?: (e: CustomEvent<{ step: number }>) => void;
  ref?: XWelcomeTourElement | ((el: XWelcomeTourElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XWelcomeTour(props: XWelcomeTourProps): JSX.Element {
  const [local, others] = splitProps(props, ["onStart", "onStepChange", "onComplete", "onSkip", "ref", "children"]);
  let el!: XWelcomeTourElement;

  const setRef = (r: XWelcomeTourElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onStart?.(e as CustomEvent<{}>);
      el.addEventListener("x-welcome-tour-start", handler);
      onCleanup(() => el.removeEventListener("x-welcome-tour-start", handler));
    }
    {
      const handler = (e: Event) => local.onStepChange?.(e as CustomEvent<{ step: number; previousStep: number }>);
      el.addEventListener("x-welcome-tour-step-change", handler);
      onCleanup(() => el.removeEventListener("x-welcome-tour-step-change", handler));
    }
    {
      const handler = (e: Event) => local.onComplete?.(e as CustomEvent<{ stepsCompleted: number }>);
      el.addEventListener("x-welcome-tour-complete", handler);
      onCleanup(() => el.removeEventListener("x-welcome-tour-complete", handler));
    }
    {
      const handler = (e: Event) => local.onSkip?.(e as CustomEvent<{ step: number }>);
      el.addEventListener("x-welcome-tour-skip", handler);
      onCleanup(() => el.removeEventListener("x-welcome-tour-skip", handler));
    }
  });

  return (
    <x-welcome-tour ref={setRef} {...others}>
      {local.children}
    </x-welcome-tour>
  );
}
