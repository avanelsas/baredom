// x-table-cell.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XTableCell as XTableCellElement } from "@vanelsas/baredom/x-table-cell";
import { init } from "@vanelsas/baredom/x-table-cell";

init();

export interface XTableCellProps {
  align?: string;
  rowSpan?: number;
  truncate?: boolean;
  disabled?: boolean;
  valign?: string;
  type?: string;
  scope?: string;
  colSpan?: number;
  sticky?: string;
  sortable?: boolean;
  sortDirection?: string;
  onSort?: (e: CustomEvent<{ direction: string; previousDirection: string }>) => void;
  onConnected?: (e: CustomEvent<{ type: string; scope: string; colSpan: number; rowSpan: number; align: string }>) => void;
  onDisconnected?: (e: CustomEvent<{}>) => void;
  ref?: XTableCellElement | ((el: XTableCellElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XTableCell(props: XTableCellProps): JSX.Element {
  const [local, others] = splitProps(props, ["onSort", "onConnected", "onDisconnected", "ref", "children"]);
  let el!: XTableCellElement;

  const setRef = (r: XTableCellElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onSort?.(e as CustomEvent<{ direction: string; previousDirection: string }>);
      el.addEventListener("x-table-cell-sort", handler);
      onCleanup(() => el.removeEventListener("x-table-cell-sort", handler));
    }
    {
      const handler = (e: Event) => local.onConnected?.(e as CustomEvent<{ type: string; scope: string; colSpan: number; rowSpan: number; align: string }>);
      el.addEventListener("x-table-cell-connected", handler);
      onCleanup(() => el.removeEventListener("x-table-cell-connected", handler));
    }
    {
      const handler = (e: Event) => local.onDisconnected?.(e as CustomEvent<{}>);
      el.addEventListener("x-table-cell-disconnected", handler);
      onCleanup(() => el.removeEventListener("x-table-cell-disconnected", handler));
    }
  });

  return (
    <x-table-cell ref={setRef} {...others}>
      {local.children}
    </x-table-cell>
  );
}
