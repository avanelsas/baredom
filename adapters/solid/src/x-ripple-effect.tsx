// x-ripple-effect.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XRippleEffect as XRippleEffectElement } from "@vanelsas/baredom/x-ripple-effect";
import { init } from "@vanelsas/baredom/x-ripple-effect";

init();

export interface XRippleEffectProps {
  intensity?: number;
  duration?: number;
  frequency?: number;
  disabled?: boolean;
  onStart?: (e: CustomEvent<{ x: number; y: number }>) => void;
  onEnd?: (e: CustomEvent<{ x: number; y: number }>) => void;
  ref?: XRippleEffectElement | ((el: XRippleEffectElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XRippleEffect(props: XRippleEffectProps): JSX.Element {
  const [local, others] = splitProps(props, ["onStart", "onEnd", "ref", "children"]);
  let el!: XRippleEffectElement;

  const setRef = (r: XRippleEffectElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onStart?.(e as CustomEvent<{ x: number; y: number }>);
      el.addEventListener("x-ripple-effect-start", handler);
      onCleanup(() => el.removeEventListener("x-ripple-effect-start", handler));
    }
    {
      const handler = (e: Event) => local.onEnd?.(e as CustomEvent<{ x: number; y: number }>);
      el.addEventListener("x-ripple-effect-end", handler);
      onCleanup(() => el.removeEventListener("x-ripple-effect-end", handler));
    }
  });

  return (
    <x-ripple-effect ref={setRef} {...others}>
      {local.children}
    </x-ripple-effect>
  );
}
