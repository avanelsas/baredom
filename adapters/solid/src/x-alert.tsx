// x-alert.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XAlert as XAlertElement } from "@vanelsas/baredom/x-alert";
import { init } from "@vanelsas/baredom/x-alert";

init();

export interface XAlertProps {
  type?: string;
  text?: string;
  icon?: string;
  dismissible?: boolean;
  disabled?: boolean;
  timeoutMs?: number;
  onDismiss?: (e: CustomEvent<{ type: string; reason: string; text: string }>) => void;
  ref?: XAlertElement | ((el: XAlertElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XAlert(props: XAlertProps): JSX.Element {
  const [local, others] = splitProps(props, ["onDismiss", "ref", "children"]);
  let el!: XAlertElement;

  const setRef = (r: XAlertElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onDismiss?.(e as CustomEvent<{ type: string; reason: string; text: string }>);
      el.addEventListener("x-alert-dismiss", handler);
      onCleanup(() => el.removeEventListener("x-alert-dismiss", handler));
    }
  });

  return (
    <x-alert ref={setRef} {...others}>
      {local.children}
    </x-alert>
  );
}
