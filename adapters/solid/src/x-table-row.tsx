// x-table-row.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XTableRow as XTableRowElement } from "@vanelsas/baredom/x-table-row";
import { init } from "@vanelsas/baredom/x-table-row";

init();

export interface XTableRowProps {
  selected?: boolean;
  disabled?: boolean;
  interactive?: boolean;
  rowIndex?: number;
  onClick?: (e: CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean }>) => void;
  onConnected?: (e: CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean; interactive: boolean }>) => void;
  onDisconnected?: (e: CustomEvent<{}>) => void;
  ref?: XTableRowElement | ((el: XTableRowElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XTableRow(props: XTableRowProps): JSX.Element {
  const [local, others] = splitProps(props, ["onClick", "onConnected", "onDisconnected", "ref", "children"]);
  let el!: XTableRowElement;

  const setRef = (r: XTableRowElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onClick?.(e as CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean }>);
      el.addEventListener("x-table-row-click", handler);
      onCleanup(() => el.removeEventListener("x-table-row-click", handler));
    }
    {
      const handler = (e: Event) => local.onConnected?.(e as CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean; interactive: boolean }>);
      el.addEventListener("x-table-row-connected", handler);
      onCleanup(() => el.removeEventListener("x-table-row-connected", handler));
    }
    {
      const handler = (e: Event) => local.onDisconnected?.(e as CustomEvent<{}>);
      el.addEventListener("x-table-row-disconnected", handler);
      onCleanup(() => el.removeEventListener("x-table-row-disconnected", handler));
    }
  });

  return (
    <x-table-row ref={setRef} {...others}>
      {local.children}
    </x-table-row>
  );
}
