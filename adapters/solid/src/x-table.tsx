// x-table.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XTable as XTableElement } from "@vanelsas/baredom/x-table";
import { init } from "@vanelsas/baredom/x-table";

init();

export interface XTableProps {
  columns?: string;
  caption?: string;
  selectable?: string;
  striped?: boolean;
  bordered?: boolean;
  fullWidth?: boolean;
  compact?: boolean;
  rowCount?: number;
  onSort?: (e: CustomEvent<{ colIndex: number; direction: string; previousDirection: string }>) => void;
  onRowSelect?: (e: CustomEvent<{ rowIndex: number; selected: boolean; selectionMode: string }>) => void;
  ref?: XTableElement | ((el: XTableElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XTable(props: XTableProps): JSX.Element {
  const [local, others] = splitProps(props, ["onSort", "onRowSelect", "ref", "children"]);
  let el!: XTableElement;

  const setRef = (r: XTableElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onSort?.(e as CustomEvent<{ colIndex: number; direction: string; previousDirection: string }>);
      el.addEventListener("x-table-sort", handler);
      onCleanup(() => el.removeEventListener("x-table-sort", handler));
    }
    {
      const handler = (e: Event) => local.onRowSelect?.(e as CustomEvent<{ rowIndex: number; selected: boolean; selectionMode: string }>);
      el.addEventListener("x-table-row-select", handler);
      onCleanup(() => el.removeEventListener("x-table-row-select", handler));
    }
  });

  return (
    <x-table ref={setRef} {...others}>
      {local.children}
    </x-table>
  );
}
