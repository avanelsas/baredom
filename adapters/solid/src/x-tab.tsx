// x-tab.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XTab as XTabElement } from "@vanelsas/baredom/x-tab";
import { init } from "@vanelsas/baredom/x-tab";

init();

export interface XTabProps {
  selected?: boolean;
  disabled?: boolean;
  value?: string;
  onTabSelect?: (e: CustomEvent<{ value: string }>) => void;
  ref?: XTabElement | ((el: XTabElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XTab(props: XTabProps): JSX.Element {
  const [local, others] = splitProps(props, ["onTabSelect", "ref", "children"]);
  let el!: XTabElement;

  const setRef = (r: XTabElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onTabSelect?.(e as CustomEvent<{ value: string }>);
      el.addEventListener("tab-select", handler);
      onCleanup(() => el.removeEventListener("tab-select", handler));
    }
  });

  return (
    <x-tab ref={setRef} {...others}>
      {local.children}
    </x-tab>
  );
}
