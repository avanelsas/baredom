// x-sidebar.tsx — auto-generated by generate_solid.bb, do not edit
import { splitProps, onMount, onCleanup } from "solid-js";
import type { JSX } from "solid-js";
import type { XSidebar as XSidebarElement } from "@vanelsas/baredom/x-sidebar";
import { init } from "@vanelsas/baredom/x-sidebar";

init();

export interface XSidebarProps {
  open?: boolean;
  collapsed?: boolean;
  onToggle?: (e: CustomEvent<{ open: boolean }>) => void;
  onDismiss?: (e: CustomEvent<{ reason: string }>) => void;
  ref?: XSidebarElement | ((el: XSidebarElement) => void);
  children?: JSX.Element;
  class?: string;
  style?: JSX.CSSProperties | string;
  id?: string;
  slot?: string;
}

export function XSidebar(props: XSidebarProps): JSX.Element {
  const [local, others] = splitProps(props, ["onToggle", "onDismiss", "ref", "children"]);
  let el!: XSidebarElement;

  const setRef = (r: XSidebarElement) => {
    el = r;
    if (typeof local.ref === "function") local.ref(r);
  };

  onMount(() => {
    if (!el) return;
    {
      const handler = (e: Event) => local.onToggle?.(e as CustomEvent<{ open: boolean }>);
      el.addEventListener("toggle", handler);
      onCleanup(() => el.removeEventListener("toggle", handler));
    }
    {
      const handler = (e: Event) => local.onDismiss?.(e as CustomEvent<{ reason: string }>);
      el.addEventListener("dismiss", handler);
      onCleanup(() => el.removeEventListener("dismiss", handler));
    }
  });

  return (
    <x-sidebar ref={setRef} {...others}>
      {local.children}
    </x-sidebar>
  );
}
