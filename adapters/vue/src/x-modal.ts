// x-modal.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XModal as XModalElement } from "@vanelsas/baredom/x-modal";
import { init } from "@vanelsas/baredom/x-modal";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XModalExposed>(name)`. */
export interface XModalExposed {
  readonly el: XModalElement | null;
}

export const XModal = defineComponent({
  name: "XModal",
  inheritAttrs: false,
  props: {
    open: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    size: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "toggle": (_e: CustomEvent<{ open: boolean }>) => true,
    "dismiss": (_e: CustomEvent<{ reason: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XModalElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("toggle", e as CustomEvent);
        el.addEventListener("x-modal-toggle", handler);
        cleanup.push(() => el.removeEventListener("x-modal-toggle", handler));
      }
      {
        const handler = (e: Event) => emit("dismiss", e as CustomEvent);
        el.addEventListener("x-modal-dismiss", handler);
        cleanup.push(() => el.removeEventListener("x-modal-dismiss", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-modal" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XModalInstance = InstanceType<typeof XModal>;
