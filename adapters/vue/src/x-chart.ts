// x-chart.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XChart as XChartElement } from "@vanelsas/baredom/x-chart";
import { init } from "@vanelsas/baredom/x-chart";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XChartExposed>(name)`. */
export interface XChartExposed {
  readonly el: XChartElement | null;
}

export const XChart = defineComponent({
  name: "XChart",
  inheritAttrs: false,
  props: {
    selected: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    axes: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    grid: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    cursor: { type: String as PropType<string | undefined>, default: undefined },
    type: { type: String as PropType<string | undefined>, default: undefined },
    loading: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    padding: { type: Number as PropType<number | undefined>, default: undefined },
    tooltip: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    height: { type: Number as PropType<number | undefined>, default: undefined },
    data: { type: Object as PropType<any | undefined>, default: undefined },
  },
  emits: {
    "select": (_e: CustomEvent<{ seriesId: string; index: number; x: number; y: number; value: number }>) => true,
    "hover": (_e: CustomEvent<{ seriesId: string; index: number; x: number; y: number; value: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XChartElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("select", e as CustomEvent);
        el.addEventListener("x-chart-select", handler);
        cleanup.push(() => el.removeEventListener("x-chart-select", handler));
      }
      {
        const handler = (e: Event) => emit("hover", e as CustomEvent);
        el.addEventListener("x-chart-hover", handler);
        cleanup.push(() => el.removeEventListener("x-chart-hover", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-chart" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XChartInstance = InstanceType<typeof XChart>;
