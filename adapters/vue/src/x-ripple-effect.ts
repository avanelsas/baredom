// x-ripple-effect.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XRippleEffect as XRippleEffectElement } from "@vanelsas/baredom/x-ripple-effect";
import { init } from "@vanelsas/baredom/x-ripple-effect";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XRippleEffectExposed>(name)`. */
export interface XRippleEffectExposed {
  readonly el: XRippleEffectElement | null;
}

export const XRippleEffect = defineComponent({
  name: "XRippleEffect",
  inheritAttrs: false,
  props: {
    intensity: { type: Number as PropType<number | undefined>, default: undefined },
    duration: { type: Number as PropType<number | undefined>, default: undefined },
    frequency: { type: Number as PropType<number | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "start": (_e: CustomEvent<{ x: number; y: number }>) => true,
    "end": (_e: CustomEvent<{ x: number; y: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XRippleEffectElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("start", e as CustomEvent);
        el.addEventListener("x-ripple-effect-start", handler);
        cleanup.push(() => el.removeEventListener("x-ripple-effect-start", handler));
      }
      {
        const handler = (e: Event) => emit("end", e as CustomEvent);
        el.addEventListener("x-ripple-effect-end", handler);
        cleanup.push(() => el.removeEventListener("x-ripple-effect-end", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-ripple-effect" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XRippleEffectInstance = InstanceType<typeof XRippleEffect>;
