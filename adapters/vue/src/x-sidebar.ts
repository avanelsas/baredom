// x-sidebar.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XSidebar as XSidebarElement } from "@vanelsas/baredom/x-sidebar";
import { init } from "@vanelsas/baredom/x-sidebar";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XSidebarExposed>(name)`. */
export interface XSidebarExposed {
  readonly el: XSidebarElement | null;
}

export const XSidebar = defineComponent({
  name: "XSidebar",
  inheritAttrs: false,
  props: {
    open: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    collapsed: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "toggle": (_e: CustomEvent<{ open: boolean }>) => true,
    "dismiss": (_e: CustomEvent<{ reason: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XSidebarElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("toggle", e as CustomEvent);
        el.addEventListener("toggle", handler);
        cleanup.push(() => el.removeEventListener("toggle", handler));
      }
      {
        const handler = (e: Event) => emit("dismiss", e as CustomEvent);
        el.addEventListener("dismiss", handler);
        cleanup.push(() => el.removeEventListener("dismiss", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-sidebar" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XSidebarInstance = InstanceType<typeof XSidebar>;
