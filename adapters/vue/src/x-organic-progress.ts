// x-organic-progress.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XOrganicProgress as XOrganicProgressElement } from "@vanelsas/baredom/x-organic-progress";
import { init } from "@vanelsas/baredom/x-organic-progress";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XOrganicProgressExposed>(name)`. */
export interface XOrganicProgressExposed {
  readonly el: XOrganicProgressElement | null;
}

export const XOrganicProgress = defineComponent({
  name: "XOrganicProgress",
  inheritAttrs: false,
  props: {
    progress: { type: String as PropType<string | undefined>, default: undefined },
    variant: { type: String as PropType<string | undefined>, default: undefined },
    color: { type: String as PropType<string | undefined>, default: undefined },
    bloom: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    density: { type: String as PropType<string | undefined>, default: undefined },
    seed: { type: Number as PropType<number | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "complete": (_e: CustomEvent<{ progress: number }>) => true,
    "bloom-end": (_e: CustomEvent<{ progress: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XOrganicProgressElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("complete", e as CustomEvent);
        el.addEventListener("x-organic-progress-complete", handler);
        cleanup.push(() => el.removeEventListener("x-organic-progress-complete", handler));
      }
      {
        const handler = (e: Event) => emit("bloom-end", e as CustomEvent);
        el.addEventListener("x-organic-progress-bloom-end", handler);
        cleanup.push(() => el.removeEventListener("x-organic-progress-bloom-end", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-organic-progress" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XOrganicProgressInstance = InstanceType<typeof XOrganicProgress>;
