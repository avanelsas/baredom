// x-stepper.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XStepper as XStepperElement } from "@vanelsas/baredom/x-stepper";
import { init } from "@vanelsas/baredom/x-stepper";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XStepperExposed>(name)`. */
export interface XStepperExposed {
  readonly el: XStepperElement | null;
}

export const XStepper = defineComponent({
  name: "XStepper",
  inheritAttrs: false,
  props: {
    steps: { type: String as PropType<string | undefined>, default: undefined },
    current: { type: Number as PropType<number | undefined>, default: undefined },
    orientation: { type: String as PropType<string | undefined>, default: undefined },
    size: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "change": (_e: CustomEvent<{ from: number; to: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XStepperElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-stepper-change", handler);
        cleanup.push(() => el.removeEventListener("x-stepper-change", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-stepper" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XStepperInstance = InstanceType<typeof XStepper>;
