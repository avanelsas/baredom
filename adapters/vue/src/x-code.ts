// x-code.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XCode as XCodeElement } from "@vanelsas/baredom/x-code";
import { init } from "@vanelsas/baredom/x-code";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XCodeExposed>(name)`. */
export interface XCodeExposed {
  readonly el: XCodeElement | null;
}

export const XCode = defineComponent({
  name: "XCode",
  inheritAttrs: false,
  props: {
    language: { type: String as PropType<string | undefined>, default: undefined },
    filename: { type: String as PropType<string | undefined>, default: undefined },
    showCopy: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    lineNumbers: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    wrap: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    maxLines: { type: Number as PropType<number | undefined>, default: undefined },
    expanded: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    code: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "copy": (_e: CustomEvent<{ code: string }>) => true,
    "toggle": (_e: CustomEvent<{ expanded: boolean }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XCodeElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("copy", e as CustomEvent);
        el.addEventListener("x-code-copy", handler);
        cleanup.push(() => el.removeEventListener("x-code-copy", handler));
      }
      {
        const handler = (e: Event) => emit("toggle", e as CustomEvent);
        el.addEventListener("x-code-toggle", handler);
        cleanup.push(() => el.removeEventListener("x-code-toggle", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-code" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XCodeInstance = InstanceType<typeof XCode>;
