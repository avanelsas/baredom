// x-copy.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XCopy as XCopyElement } from "@vanelsas/baredom/x-copy";
import { init } from "@vanelsas/baredom/x-copy";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XCopyExposed>(name)`. */
export interface XCopyExposed {
  readonly el: XCopyElement | null;
}

export const XCopy = defineComponent({
  name: "XCopy",
  inheritAttrs: false,
  props: {
    errorMessage: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    textValue: { type: String as PropType<string | undefined>, default: undefined },
    mode: { type: String as PropType<string | undefined>, default: undefined },
    fromAttr: { type: String as PropType<string | undefined>, default: undefined },
    tooltipMs: { type: Number as PropType<number | undefined>, default: undefined },
    hotkey: { type: String as PropType<string | undefined>, default: undefined },
    from: { type: String as PropType<string | undefined>, default: undefined },
    showTooltip: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    successMessage: { type: String as PropType<string | undefined>, default: undefined },
    text: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "request": (_e: CustomEvent<{ text: string; mode: string; from: string; fromAttr: string }>) => true,
    "success": (_e: CustomEvent<{ text: string }>) => true,
    "error": (_e: CustomEvent<{ error: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XCopyElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("request", e as CustomEvent);
        el.addEventListener("x-copy-request", handler);
        cleanup.push(() => el.removeEventListener("x-copy-request", handler));
      }
      {
        const handler = (e: Event) => emit("success", e as CustomEvent);
        el.addEventListener("x-copy-success", handler);
        cleanup.push(() => el.removeEventListener("x-copy-success", handler));
      }
      {
        const handler = (e: Event) => emit("error", e as CustomEvent);
        el.addEventListener("x-copy-error", handler);
        cleanup.push(() => el.removeEventListener("x-copy-error", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-copy" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XCopyInstance = InstanceType<typeof XCopy>;
