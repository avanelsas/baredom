// x-icon.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, type PropType } from "vue";
import type { XIcon as XIconElement } from "@vanelsas/baredom/x-icon";
import { init } from "@vanelsas/baredom/x-icon";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XIconExposed>(name)`. */
export interface XIconExposed {
  readonly el: XIconElement | null;
}

export const XIcon = defineComponent({
  name: "XIcon",
  inheritAttrs: false,
  props: {
    size: { type: String as PropType<string | undefined>, default: undefined },
    color: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XIconElement | null>(null);

    expose({ el: elRef });

    return () => {
      return h("x-icon" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XIconInstance = InstanceType<typeof XIcon>;
