// x-drawer.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XDrawer as XDrawerElement } from "@vanelsas/baredom/x-drawer";
import { init } from "@vanelsas/baredom/x-drawer";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XDrawerExposed>(name)`. */
export interface XDrawerExposed {
  readonly el: XDrawerElement | null;
}

export const XDrawer = defineComponent({
  name: "XDrawer",
  inheritAttrs: false,
  props: {
    open: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    placement: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "toggle": (_e: CustomEvent<{ open: boolean }>) => true,
    "dismiss": (_e: CustomEvent<{ reason: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XDrawerElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("toggle", e as CustomEvent);
        el.addEventListener("x-drawer-toggle", handler);
        cleanup.push(() => el.removeEventListener("x-drawer-toggle", handler));
      }
      {
        const handler = (e: Event) => emit("dismiss", e as CustomEvent);
        el.addEventListener("x-drawer-dismiss", handler);
        cleanup.push(() => el.removeEventListener("x-drawer-dismiss", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-drawer" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XDrawerInstance = InstanceType<typeof XDrawer>;
