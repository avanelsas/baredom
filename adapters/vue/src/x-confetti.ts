// x-confetti.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XConfetti as XConfettiElement } from "@vanelsas/baredom/x-confetti";
import { init } from "@vanelsas/baredom/x-confetti";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XConfettiExposed>(name)`. */
export interface XConfettiExposed {
  readonly el: XConfettiElement | null;
}

export const XConfetti = defineComponent({
  name: "XConfetti",
  inheritAttrs: false,
  props: {
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    spread: { type: Number as PropType<number | undefined>, default: undefined },
    mode: { type: String as PropType<string | undefined>, default: undefined },
    colors: { type: String as PropType<string | undefined>, default: undefined },
    duration: { type: Number as PropType<number | undefined>, default: undefined },
    autoFire: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    gravity: { type: Number as PropType<number | undefined>, default: undefined },
    count: { type: Number as PropType<number | undefined>, default: undefined },
    origin: { type: String as PropType<string | undefined>, default: undefined },
    velocity: { type: Number as PropType<number | undefined>, default: undefined },
    shapes: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "fire": (_e: CustomEvent<{ count: number; origin: string }>) => true,
    "end": (_e: CustomEvent<{ duration: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XConfettiElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("fire", e as CustomEvent);
        el.addEventListener("x-confetti-fire", handler);
        cleanup.push(() => el.removeEventListener("x-confetti-fire", handler));
      }
      {
        const handler = (e: Event) => emit("end", e as CustomEvent);
        el.addEventListener("x-confetti-end", handler);
        cleanup.push(() => el.removeEventListener("x-confetti-end", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-confetti" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XConfettiInstance = InstanceType<typeof XConfetti>;
