// x-popover.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XPopover as XPopoverElement } from "@vanelsas/baredom/x-popover";
import { init } from "@vanelsas/baredom/x-popover";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XPopoverExposed>(name)`. */
export interface XPopoverExposed {
  readonly el: XPopoverElement | null;
}

export const XPopover = defineComponent({
  name: "XPopover",
  inheritAttrs: false,
  props: {
    open: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    placement: { type: String as PropType<string | undefined>, default: undefined },
    heading: { type: String as PropType<string | undefined>, default: undefined },
    closeLabel: { type: String as PropType<string | undefined>, default: undefined },
    noClose: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    portal: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "toggle": (_e: CustomEvent<{ open: boolean; source: string }>) => true,
    "change": (_e: CustomEvent<{ open: boolean }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XPopoverElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("toggle", e as CustomEvent);
        el.addEventListener("x-popover-toggle", handler);
        cleanup.push(() => el.removeEventListener("x-popover-toggle", handler));
      }
      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-popover-change", handler);
        cleanup.push(() => el.removeEventListener("x-popover-change", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-popover" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XPopoverInstance = InstanceType<typeof XPopover>;
