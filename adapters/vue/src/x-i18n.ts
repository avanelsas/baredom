// x-i18n.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, type PropType } from "vue";
import type { XI18n as XI18nElement } from "@vanelsas/baredom/x-i18n";
import { init } from "@vanelsas/baredom/x-i18n";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XI18nExposed>(name)`. */
export interface XI18nExposed {
  readonly el: XI18nElement | null;
}

export const XI18n = defineComponent({
  name: "XI18n",
  inheritAttrs: false,
  props: {
    key: { type: String as PropType<string | undefined>, default: undefined },
    params: { type: String as PropType<string | undefined>, default: undefined },
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XI18nElement | null>(null);

    expose({ el: elRef });

    return () => {
      return h("x-i18n" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XI18nInstance = InstanceType<typeof XI18n>;
