// x-calendar.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XCalendar as XCalendarElement } from "@vanelsas/baredom/x-calendar";
import { init } from "@vanelsas/baredom/x-calendar";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XCalendarExposed>(name)`. */
export interface XCalendarExposed {
  readonly el: XCalendarElement | null;
}

export const XCalendar = defineComponent({
  name: "XCalendar",
  inheritAttrs: false,
  props: {
    min: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    locale: { type: String as PropType<string | undefined>, default: undefined },
    autoSwap: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    value: { type: String as PropType<string | undefined>, default: undefined },
    mode: { type: String as PropType<string | undefined>, default: undefined },
    start: { type: String as PropType<string | undefined>, default: undefined },
    month: { type: String as PropType<string | undefined>, default: undefined },
    showWeekNumbers: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    disabledDates: { type: String as PropType<string | undefined>, default: undefined },
    max: { type: String as PropType<string | undefined>, default: undefined },
    firstDayOfWeek: { type: String as PropType<string | undefined>, default: undefined },
    end: { type: String as PropType<string | undefined>, default: undefined },
    rangeAllowSameDay: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "change": (_e: CustomEvent<{ value: string; start: string; end: string; mode: string }>) => true,
    "navigate": (_e: CustomEvent<{ month: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XCalendarElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-calendar-change", handler);
        cleanup.push(() => el.removeEventListener("x-calendar-change", handler));
      }
      {
        const handler = (e: Event) => emit("navigate", e as CustomEvent);
        el.addEventListener("x-calendar-navigate", handler);
        cleanup.push(() => el.removeEventListener("x-calendar-navigate", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-calendar" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XCalendarInstance = InstanceType<typeof XCalendar>;
