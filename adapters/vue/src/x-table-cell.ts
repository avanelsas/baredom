// x-table-cell.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XTableCell as XTableCellElement } from "@vanelsas/baredom/x-table-cell";
import { init } from "@vanelsas/baredom/x-table-cell";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XTableCellExposed>(name)`. */
export interface XTableCellExposed {
  readonly el: XTableCellElement | null;
}

export const XTableCell = defineComponent({
  name: "XTableCell",
  inheritAttrs: false,
  props: {
    align: { type: String as PropType<string | undefined>, default: undefined },
    rowSpan: { type: Number as PropType<number | undefined>, default: undefined },
    truncate: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    valign: { type: String as PropType<string | undefined>, default: undefined },
    type: { type: String as PropType<string | undefined>, default: undefined },
    scope: { type: String as PropType<string | undefined>, default: undefined },
    colSpan: { type: Number as PropType<number | undefined>, default: undefined },
    sticky: { type: String as PropType<string | undefined>, default: undefined },
    sortable: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    sortDirection: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "sort": (_e: CustomEvent<{ direction: string; previousDirection: string }>) => true,
    "connected": (_e: CustomEvent<{ type: string; scope: string; colSpan: number; rowSpan: number; align: string }>) => true,
    "disconnected": (_e: CustomEvent<{}>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XTableCellElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("sort", e as CustomEvent);
        el.addEventListener("x-table-cell-sort", handler);
        cleanup.push(() => el.removeEventListener("x-table-cell-sort", handler));
      }
      {
        const handler = (e: Event) => emit("connected", e as CustomEvent);
        el.addEventListener("x-table-cell-connected", handler);
        cleanup.push(() => el.removeEventListener("x-table-cell-connected", handler));
      }
      {
        const handler = (e: Event) => emit("disconnected", e as CustomEvent);
        el.addEventListener("x-table-cell-disconnected", handler);
        cleanup.push(() => el.removeEventListener("x-table-cell-disconnected", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-table-cell" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XTableCellInstance = InstanceType<typeof XTableCell>;
