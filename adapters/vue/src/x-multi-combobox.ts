// x-multi-combobox.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, watch, type PropType } from "vue";
import type { XMultiCombobox as XMultiComboboxElement } from "@vanelsas/baredom/x-multi-combobox";
import { init } from "@vanelsas/baredom/x-multi-combobox";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XMultiComboboxExposed>(name)`. */
export interface XMultiComboboxExposed {
  readonly el: XMultiComboboxElement | null;
}

export const XMultiCombobox = defineComponent({
  name: "XMultiCombobox",
  inheritAttrs: false,
  props: {
    open: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    placeholder: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    name: { type: String as PropType<string | undefined>, default: undefined },
    value: { type: String as PropType<string | undefined>, default: undefined },
    max: { type: Number as PropType<number | undefined>, default: undefined },
    error: { type: String as PropType<string | undefined>, default: undefined },
    placement: { type: String as PropType<string | undefined>, default: undefined },
    required: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    modelValue: { type: Array as PropType<string[] | undefined>, default: undefined },
  },
  emits: {
    "update:modelValue": (_v: string[]) => true,
    "change-request": (_e: CustomEvent<{ value: any; action: string; item: string }>) => true,
    "change": (_e: CustomEvent<{ value: any }>) => true,
    "input": (_e: CustomEvent<{ query: string }>) => true,
    "toggle": (_e: CustomEvent<{ open: boolean; source: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XMultiComboboxElement | null>(null);
    const cleanup: Array<() => void> = [];

    const writeValue = (v: string[] | undefined) => {
      const el = elRef.value;
      if (!el || v === undefined) return;
      if (v != null) el.setAttribute("value", String(v));
      else el.removeAttribute("value");
    };

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;
      writeValue(props.modelValue);

      {
        const handler = (e: Event) => emit("change-request", e as CustomEvent);
        el.addEventListener("x-multi-combobox-change-request", handler);
        cleanup.push(() => el.removeEventListener("x-multi-combobox-change-request", handler));
      }
      {
        const handler = (e: Event) => {
          const detail = (e as CustomEvent).detail;
          emit("update:modelValue", (detail.value));
          emit("change", e as CustomEvent);
        };
        el.addEventListener("x-multi-combobox-change", handler);
        cleanup.push(() => el.removeEventListener("x-multi-combobox-change", handler));
      }
      {
        const handler = (e: Event) => emit("input", e as CustomEvent);
        el.addEventListener("x-multi-combobox-input", handler);
        cleanup.push(() => el.removeEventListener("x-multi-combobox-input", handler));
      }
      {
        const handler = (e: Event) => emit("toggle", e as CustomEvent);
        el.addEventListener("x-multi-combobox-toggle", handler);
        cleanup.push(() => el.removeEventListener("x-multi-combobox-toggle", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    watch(() => props.modelValue, writeValue);

    expose({ el: elRef });

    return () => {
      const { modelValue: _modelValue, ...elProps } = props;
      void _modelValue;
      return h("x-multi-combobox" as any, { ...attrs, ref: elRef, ...elProps }, slots.default?.());
    };
  },
});

export type XMultiComboboxInstance = InstanceType<typeof XMultiCombobox>;
