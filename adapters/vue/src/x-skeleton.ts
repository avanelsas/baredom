// x-skeleton.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, type PropType } from "vue";
import type { XSkeleton as XSkeletonElement } from "@vanelsas/baredom/x-skeleton";
import { init } from "@vanelsas/baredom/x-skeleton";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XSkeletonExposed>(name)`. */
export interface XSkeletonExposed {
  readonly el: XSkeletonElement | null;
}

export const XSkeleton = defineComponent({
  name: "XSkeleton",
  inheritAttrs: false,
  props: {
    variant: { type: String as PropType<string | undefined>, default: undefined },
    animation: { type: String as PropType<string | undefined>, default: undefined },
    width: { type: String as PropType<string | undefined>, default: undefined },
    height: { type: String as PropType<string | undefined>, default: undefined },
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XSkeletonElement | null>(null);

    expose({ el: elRef });

    return () => {
      return h("x-skeleton" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XSkeletonInstance = InstanceType<typeof XSkeleton>;
