// x-typography.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, type PropType } from "vue";
import type { XTypography as XTypographyElement } from "@vanelsas/baredom/x-typography";
import { init } from "@vanelsas/baredom/x-typography";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XTypographyExposed>(name)`. */
export interface XTypographyExposed {
  readonly el: XTypographyElement | null;
}

export const XTypography = defineComponent({
  name: "XTypography",
  inheritAttrs: false,
  props: {
    variant: { type: String as PropType<string | undefined>, default: undefined },
    align: { type: String as PropType<string | undefined>, default: undefined },
    truncate: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    lineClamp: { type: Number as PropType<number | undefined>, default: undefined },
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XTypographyElement | null>(null);

    expose({ el: elRef });

    return () => {
      return h("x-typography" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XTypographyInstance = InstanceType<typeof XTypography>;
