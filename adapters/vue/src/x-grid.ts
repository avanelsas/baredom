// x-grid.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref } from "vue";
import type { XGrid as XGridElement } from "@vanelsas/baredom/x-grid";
import { init } from "@vanelsas/baredom/x-grid";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XGridExposed>(name)`. */
export interface XGridExposed {
  readonly el: XGridElement | null;
}

export const XGrid = defineComponent({
  name: "XGrid",
  inheritAttrs: false,
  props: {},
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XGridElement | null>(null);

    expose({ el: elRef });

    return () => {
      return h("x-grid" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XGridInstance = InstanceType<typeof XGrid>;
