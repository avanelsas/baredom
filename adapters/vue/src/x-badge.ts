// x-badge.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, type PropType } from "vue";
import type { XBadge as XBadgeElement } from "@vanelsas/baredom/x-badge";
import { init } from "@vanelsas/baredom/x-badge";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XBadgeExposed>(name)`. */
export interface XBadgeExposed {
  readonly el: XBadgeElement | null;
}

export const XBadge = defineComponent({
  name: "XBadge",
  inheritAttrs: false,
  props: {
    variant: { type: String as PropType<string | undefined>, default: undefined },
    size: { type: String as PropType<string | undefined>, default: undefined },
    pill: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    dot: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    count: { type: Number as PropType<number | undefined>, default: undefined },
    max: { type: Number as PropType<number | undefined>, default: undefined },
    text: { type: String as PropType<string | undefined>, default: undefined },
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XBadgeElement | null>(null);

    expose({ el: elRef });

    return () => {
      return h("x-badge" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XBadgeInstance = InstanceType<typeof XBadge>;
