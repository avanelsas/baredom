// x-splash.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XSplash as XSplashElement } from "@vanelsas/baredom/x-splash";
import { init } from "@vanelsas/baredom/x-splash";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XSplashExposed>(name)`. */
export interface XSplashExposed {
  readonly el: XSplashElement | null;
}

export const XSplash = defineComponent({
  name: "XSplash",
  inheritAttrs: false,
  props: {
    active: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    variant: { type: String as PropType<string | undefined>, default: undefined },
    progress: { type: Number as PropType<number | undefined>, default: undefined },
    spinner: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    overlay: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "hidden": (_e: CustomEvent<{}>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XSplashElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("hidden", e as CustomEvent);
        el.addEventListener("x-splash-hidden", handler);
        cleanup.push(() => el.removeEventListener("x-splash-hidden", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-splash" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XSplashInstance = InstanceType<typeof XSplash>;
