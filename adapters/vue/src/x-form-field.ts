// x-form-field.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XFormField as XFormFieldElement } from "@vanelsas/baredom/x-form-field";
import { init } from "@vanelsas/baredom/x-form-field";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XFormFieldExposed>(name)`. */
export interface XFormFieldExposed {
  readonly el: XFormFieldElement | null;
}

export const XFormField = defineComponent({
  name: "XFormField",
  inheritAttrs: false,
  props: {
    placeholder: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    name: { type: String as PropType<string | undefined>, default: undefined },
    value: { type: String as PropType<string | undefined>, default: undefined },
    readonly: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    type: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    autocomplete: { type: String as PropType<string | undefined>, default: undefined },
    required: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "input": (_e: CustomEvent<{ name: string; value: string }>) => true,
    "change": (_e: CustomEvent<{ name: string; value: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XFormFieldElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("input", e as CustomEvent);
        el.addEventListener("x-form-field-input", handler);
        cleanup.push(() => el.removeEventListener("x-form-field-input", handler));
      }
      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-form-field-change", handler);
        cleanup.push(() => el.removeEventListener("x-form-field-change", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-form-field" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XFormFieldInstance = InstanceType<typeof XFormField>;
