// x-navbar.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XNavbar as XNavbarElement } from "@vanelsas/baredom/x-navbar";
import { init } from "@vanelsas/baredom/x-navbar";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XNavbarExposed>(name)`. */
export interface XNavbarExposed {
  readonly el: XNavbarElement | null;
}

export const XNavbar = defineComponent({
  name: "XNavbar",
  inheritAttrs: false,
  props: {
    sticky: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    elevated: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    variant: { type: String as PropType<string | undefined>, default: undefined },
    orientation: { type: String as PropType<string | undefined>, default: undefined },
    alignment: { type: String as PropType<string | undefined>, default: undefined },
    breakpoint: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "focus-visible": (_e: CustomEvent<{}>) => true,
    "navigate": (_e: CustomEvent<{ href: string; source: string }>) => true,
    "brand-activate": (_e: CustomEvent<{ source: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XNavbarElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("focus-visible", e as CustomEvent);
        el.addEventListener("focus-visible", handler);
        cleanup.push(() => el.removeEventListener("focus-visible", handler));
      }
      {
        const handler = (e: Event) => emit("navigate", e as CustomEvent);
        el.addEventListener("navigate", handler);
        cleanup.push(() => el.removeEventListener("navigate", handler));
      }
      {
        const handler = (e: Event) => emit("brand-activate", e as CustomEvent);
        el.addEventListener("brand-activate", handler);
        cleanup.push(() => el.removeEventListener("brand-activate", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-navbar" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XNavbarInstance = InstanceType<typeof XNavbar>;
