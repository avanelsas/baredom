// x-theme.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, type PropType } from "vue";
import type { XTheme as XThemeElement } from "@vanelsas/baredom/x-theme";
import { init } from "@vanelsas/baredom/x-theme";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XThemeExposed>(name)`. */
export interface XThemeExposed {
  readonly el: XThemeElement | null;
}

export const XTheme = defineComponent({
  name: "XTheme",
  inheritAttrs: false,
  props: {
    preset: { type: String as PropType<string | undefined>, default: undefined },
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XThemeElement | null>(null);

    expose({ el: elRef });

    return () => {
      return h("x-theme" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XThemeInstance = InstanceType<typeof XTheme>;
