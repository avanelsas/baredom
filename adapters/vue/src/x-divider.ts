// x-divider.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, type PropType } from "vue";
import type { XDivider as XDividerElement } from "@vanelsas/baredom/x-divider";
import { init } from "@vanelsas/baredom/x-divider";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XDividerExposed>(name)`. */
export interface XDividerExposed {
  readonly el: XDividerElement | null;
}

export const XDivider = defineComponent({
  name: "XDivider",
  inheritAttrs: false,
  props: {
    orientation: { type: String as PropType<string | undefined>, default: undefined },
    variant: { type: String as PropType<string | undefined>, default: undefined },
    align: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    thickness: { type: String as PropType<string | undefined>, default: undefined },
    color: { type: String as PropType<string | undefined>, default: undefined },
    inset: { type: String as PropType<string | undefined>, default: undefined },
    length: { type: String as PropType<string | undefined>, default: undefined },
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XDividerElement | null>(null);

    expose({ el: elRef });

    return () => {
      return h("x-divider" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XDividerInstance = InstanceType<typeof XDivider>;
