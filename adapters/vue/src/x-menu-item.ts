// x-menu-item.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XMenuItem as XMenuItemElement } from "@vanelsas/baredom/x-menu-item";
import { init } from "@vanelsas/baredom/x-menu-item";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XMenuItemExposed>(name)`. */
export interface XMenuItemExposed {
  readonly el: XMenuItemElement | null;
}

export const XMenuItem = defineComponent({
  name: "XMenuItem",
  inheritAttrs: false,
  props: {
    value: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    variant: { type: String as PropType<string | undefined>, default: undefined },
    type: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "select": (_e: CustomEvent<{ value: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XMenuItemElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("select", e as CustomEvent);
        el.addEventListener("x-menu-item-select", handler);
        cleanup.push(() => el.removeEventListener("x-menu-item-select", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-menu-item" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XMenuItemInstance = InstanceType<typeof XMenuItem>;
