// x-color-picker.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XColorPicker as XColorPickerElement } from "@vanelsas/baredom/x-color-picker";
import { init } from "@vanelsas/baredom/x-color-picker";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XColorPickerExposed>(name)`. */
export interface XColorPickerExposed {
  readonly el: XColorPickerElement | null;
}

export const XColorPicker = defineComponent({
  name: "XColorPicker",
  inheritAttrs: false,
  props: {
    open: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    name: { type: String as PropType<string | undefined>, default: undefined },
    value: { type: String as PropType<string | undefined>, default: undefined },
    mode: { type: String as PropType<string | undefined>, default: undefined },
    swatches: { type: String as PropType<string | undefined>, default: undefined },
    readOnly: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    alpha: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "input": (_e: CustomEvent<{ value: string; h: number; s: number; l: number; a: number }>) => true,
    "change": (_e: CustomEvent<{ value: string; h: number; s: number; l: number; a: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XColorPickerElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("input", e as CustomEvent);
        el.addEventListener("x-color-picker-input", handler);
        cleanup.push(() => el.removeEventListener("x-color-picker-input", handler));
      }
      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-color-picker-change", handler);
        cleanup.push(() => el.removeEventListener("x-color-picker-change", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-color-picker" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XColorPickerInstance = InstanceType<typeof XColorPicker>;
