// x-dropdown.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XDropdown as XDropdownElement } from "@vanelsas/baredom/x-dropdown";
import { init } from "@vanelsas/baredom/x-dropdown";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XDropdownExposed>(name)`. */
export interface XDropdownExposed {
  readonly el: XDropdownElement | null;
}

export const XDropdown = defineComponent({
  name: "XDropdown",
  inheritAttrs: false,
  props: {
    open: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    placement: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "toggle": (_e: CustomEvent<{ open: boolean; source: string }>) => true,
    "change": (_e: CustomEvent<{ open: boolean }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XDropdownElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("toggle", e as CustomEvent);
        el.addEventListener("x-dropdown-toggle", handler);
        cleanup.push(() => el.removeEventListener("x-dropdown-toggle", handler));
      }
      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-dropdown-change", handler);
        cleanup.push(() => el.removeEventListener("x-dropdown-change", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-dropdown" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XDropdownInstance = InstanceType<typeof XDropdown>;
