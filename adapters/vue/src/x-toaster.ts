// x-toaster.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XToaster as XToasterElement } from "@vanelsas/baredom/x-toaster";
import { init } from "@vanelsas/baredom/x-toaster";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XToasterExposed>(name)`. */
export interface XToasterExposed {
  readonly el: XToasterElement | null;
}

export const XToaster = defineComponent({
  name: "XToaster",
  inheritAttrs: false,
  props: {
    position: { type: String as PropType<string | undefined>, default: undefined },
    maxToasts: { type: Number as PropType<number | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "dismiss": (_e: CustomEvent<{ type: string; reason: string; heading: string; message: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XToasterElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("dismiss", e as CustomEvent);
        el.addEventListener("x-toaster-dismiss", handler);
        cleanup.push(() => el.removeEventListener("x-toaster-dismiss", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-toaster" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XToasterInstance = InstanceType<typeof XToaster>;
