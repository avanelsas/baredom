// x-toast.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XToast as XToastElement } from "@vanelsas/baredom/x-toast";
import { init } from "@vanelsas/baredom/x-toast";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XToastExposed>(name)`. */
export interface XToastExposed {
  readonly el: XToastElement | null;
}

export const XToast = defineComponent({
  name: "XToast",
  inheritAttrs: false,
  props: {
    type: { type: String as PropType<string | undefined>, default: undefined },
    heading: { type: String as PropType<string | undefined>, default: undefined },
    message: { type: String as PropType<string | undefined>, default: undefined },
    icon: { type: String as PropType<string | undefined>, default: undefined },
    dismissible: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    timeoutMs: { type: Number as PropType<number | undefined>, default: undefined },
    showProgress: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "dismiss": (_e: CustomEvent<{ type: string; reason: string; heading: string; message: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XToastElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("dismiss", e as CustomEvent);
        el.addEventListener("x-toast-dismiss", handler);
        cleanup.push(() => el.removeEventListener("x-toast-dismiss", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-toast" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XToastInstance = InstanceType<typeof XToast>;
