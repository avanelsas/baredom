// x-search-field.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XSearchField as XSearchFieldElement } from "@vanelsas/baredom/x-search-field";
import { init } from "@vanelsas/baredom/x-search-field";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XSearchFieldExposed>(name)`. */
export interface XSearchFieldExposed {
  readonly el: XSearchFieldElement | null;
}

export const XSearchField = defineComponent({
  name: "XSearchField",
  inheritAttrs: false,
  props: {
    value: { type: String as PropType<string | undefined>, default: undefined },
    name: { type: String as PropType<string | undefined>, default: undefined },
    placeholder: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    autocomplete: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    required: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    debounce: { type: Number as PropType<number | undefined>, default: undefined },
  },
  emits: {
    "input": (_e: CustomEvent<{ name: string; value: string }>) => true,
    "change": (_e: CustomEvent<{ name: string; value: string }>) => true,
    "search": (_e: CustomEvent<{ name: string; value: string }>) => true,
    "clear": (_e: CustomEvent<{ name: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XSearchFieldElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("input", e as CustomEvent);
        el.addEventListener("x-search-field-input", handler);
        cleanup.push(() => el.removeEventListener("x-search-field-input", handler));
      }
      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-search-field-change", handler);
        cleanup.push(() => el.removeEventListener("x-search-field-change", handler));
      }
      {
        const handler = (e: Event) => emit("search", e as CustomEvent);
        el.addEventListener("x-search-field-search", handler);
        cleanup.push(() => el.removeEventListener("x-search-field-search", handler));
      }
      {
        const handler = (e: Event) => emit("clear", e as CustomEvent);
        el.addEventListener("x-search-field-clear", handler);
        cleanup.push(() => el.removeEventListener("x-search-field-clear", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-search-field" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XSearchFieldInstance = InstanceType<typeof XSearchField>;
