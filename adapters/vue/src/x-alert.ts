// x-alert.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XAlert as XAlertElement } from "@vanelsas/baredom/x-alert";
import { init } from "@vanelsas/baredom/x-alert";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XAlertExposed>(name)`. */
export interface XAlertExposed {
  readonly el: XAlertElement | null;
}

export const XAlert = defineComponent({
  name: "XAlert",
  inheritAttrs: false,
  props: {
    type: { type: String as PropType<string | undefined>, default: undefined },
    text: { type: String as PropType<string | undefined>, default: undefined },
    icon: { type: String as PropType<string | undefined>, default: undefined },
    dismissible: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    timeoutMs: { type: Number as PropType<number | undefined>, default: undefined },
  },
  emits: {
    "dismiss": (_e: CustomEvent<{ type: string; reason: string; text: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XAlertElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("dismiss", e as CustomEvent);
        el.addEventListener("x-alert-dismiss", handler);
        cleanup.push(() => el.removeEventListener("x-alert-dismiss", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-alert" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XAlertInstance = InstanceType<typeof XAlert>;
