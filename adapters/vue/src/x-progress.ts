// x-progress.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XProgress as XProgressElement } from "@vanelsas/baredom/x-progress";
import { init } from "@vanelsas/baredom/x-progress";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XProgressExposed>(name)`. */
export interface XProgressExposed {
  readonly el: XProgressElement | null;
}

export const XProgress = defineComponent({
  name: "XProgress",
  inheritAttrs: false,
  props: {
    value: { type: String as PropType<string | undefined>, default: undefined },
    max: { type: String as PropType<string | undefined>, default: undefined },
    variant: { type: String as PropType<string | undefined>, default: undefined },
    size: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    indeterminate: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    showValue: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "complete": (_e: CustomEvent<{ value: number; max: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XProgressElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("complete", e as CustomEvent);
        el.addEventListener("x-progress-complete", handler);
        cleanup.push(() => el.removeEventListener("x-progress-complete", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-progress" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XProgressInstance = InstanceType<typeof XProgress>;
