// x-spacer.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, type PropType } from "vue";
import type { XSpacer as XSpacerElement } from "@vanelsas/baredom/x-spacer";
import { init } from "@vanelsas/baredom/x-spacer";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XSpacerExposed>(name)`. */
export interface XSpacerExposed {
  readonly el: XSpacerElement | null;
}

export const XSpacer = defineComponent({
  name: "XSpacer",
  inheritAttrs: false,
  props: {
    size: { type: String as PropType<string | undefined>, default: undefined },
    axis: { type: String as PropType<string | undefined>, default: undefined },
    grow: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XSpacerElement | null>(null);

    expose({ el: elRef });

    return () => {
      return h("x-spacer" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XSpacerInstance = InstanceType<typeof XSpacer>;
