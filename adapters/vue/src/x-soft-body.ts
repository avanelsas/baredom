// x-soft-body.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XSoftBody as XSoftBodyElement } from "@vanelsas/baredom/x-soft-body";
import { init } from "@vanelsas/baredom/x-soft-body";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XSoftBodyExposed>(name)`. */
export interface XSoftBodyExposed {
  readonly el: XSoftBodyElement | null;
}

export const XSoftBody = defineComponent({
  name: "XSoftBody",
  inheritAttrs: false,
  props: {
    stiffness: { type: Number as PropType<number | undefined>, default: undefined },
    damping: { type: Number as PropType<number | undefined>, default: undefined },
    radius: { type: Number as PropType<number | undefined>, default: undefined },
    intensity: { type: Number as PropType<number | undefined>, default: undefined },
    grabRadius: { type: Number as PropType<number | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "grab": (_e: CustomEvent<{}>) => true,
    "release": (_e: CustomEvent<{}>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XSoftBodyElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("grab", e as CustomEvent);
        el.addEventListener("x-soft-body-grab", handler);
        cleanup.push(() => el.removeEventListener("x-soft-body-grab", handler));
      }
      {
        const handler = (e: Event) => emit("release", e as CustomEvent);
        el.addEventListener("x-soft-body-release", handler);
        cleanup.push(() => el.removeEventListener("x-soft-body-release", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-soft-body" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XSoftBodyInstance = InstanceType<typeof XSoftBody>;
