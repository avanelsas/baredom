// x-timeline.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XTimeline as XTimelineElement } from "@vanelsas/baredom/x-timeline";
import { init } from "@vanelsas/baredom/x-timeline";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XTimelineExposed>(name)`. */
export interface XTimelineExposed {
  readonly el: XTimelineElement | null;
}

export const XTimeline = defineComponent({
  name: "XTimeline",
  inheritAttrs: false,
  props: {
    label: { type: String as PropType<string | undefined>, default: undefined },
    position: { type: String as PropType<string | undefined>, default: undefined },
    striped: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "select": (_e: CustomEvent<{ index: number; status: string; label: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XTimelineElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("select", e as CustomEvent);
        el.addEventListener("x-timeline-select", handler);
        cleanup.push(() => el.removeEventListener("x-timeline-select", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-timeline" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XTimelineInstance = InstanceType<typeof XTimeline>;
