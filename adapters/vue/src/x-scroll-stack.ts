// x-scroll-stack.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XScrollStack as XScrollStackElement } from "@vanelsas/baredom/x-scroll-stack";
import { init } from "@vanelsas/baredom/x-scroll-stack";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XScrollStackExposed>(name)`. */
export interface XScrollStackExposed {
  readonly el: XScrollStackElement | null;
}

export const XScrollStack = defineComponent({
  name: "XScrollStack",
  inheritAttrs: false,
  props: {
    peek: { type: Number as PropType<number | undefined>, default: undefined },
    rotation: { type: Number as PropType<number | undefined>, default: undefined },
    scrollDistance: { type: Number as PropType<number | undefined>, default: undefined },
    align: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "change": (_e: CustomEvent<{ stackedCount: number; totalCount: number; progress: number }>) => true,
    "progress": (_e: CustomEvent<{ progress: number; stackedCount: number; totalCount: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XScrollStackElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-scroll-stack-change", handler);
        cleanup.push(() => el.removeEventListener("x-scroll-stack-change", handler));
      }
      {
        const handler = (e: Event) => emit("progress", e as CustomEvent);
        el.addEventListener("x-scroll-stack-progress", handler);
        cleanup.push(() => el.removeEventListener("x-scroll-stack-progress", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-scroll-stack" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XScrollStackInstance = InstanceType<typeof XScrollStack>;
