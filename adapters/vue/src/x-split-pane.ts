// x-split-pane.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XSplitPane as XSplitPaneElement } from "@vanelsas/baredom/x-split-pane";
import { init } from "@vanelsas/baredom/x-split-pane";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XSplitPaneExposed>(name)`. */
export interface XSplitPaneExposed {
  readonly el: XSplitPaneElement | null;
}

export const XSplitPane = defineComponent({
  name: "XSplitPane",
  inheritAttrs: false,
  props: {
    orientation: { type: String as PropType<string | undefined>, default: undefined },
    position: { type: Number as PropType<number | undefined>, default: undefined },
    minStart: { type: Number as PropType<number | undefined>, default: undefined },
    minEnd: { type: Number as PropType<number | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    dividerLabel: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "resize": (_e: CustomEvent<{ position: number; orientation: string }>) => true,
    "resize-end": (_e: CustomEvent<{ position: number; orientation: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XSplitPaneElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("resize", e as CustomEvent);
        el.addEventListener("x-split-pane-resize", handler);
        cleanup.push(() => el.removeEventListener("x-split-pane-resize", handler));
      }
      {
        const handler = (e: Event) => emit("resize-end", e as CustomEvent);
        el.addEventListener("x-split-pane-resize-end", handler);
        cleanup.push(() => el.removeEventListener("x-split-pane-resize-end", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-split-pane" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XSplitPaneInstance = InstanceType<typeof XSplitPane>;
