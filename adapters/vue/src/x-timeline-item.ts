// x-timeline-item.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XTimelineItem as XTimelineItemElement } from "@vanelsas/baredom/x-timeline-item";
import { init } from "@vanelsas/baredom/x-timeline-item";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XTimelineItemExposed>(name)`. */
export interface XTimelineItemExposed {
  readonly el: XTimelineItemElement | null;
}

export const XTimelineItem = defineComponent({
  name: "XTimelineItem",
  inheritAttrs: false,
  props: {
    label: { type: String as PropType<string | undefined>, default: undefined },
    title: { type: String as PropType<string | undefined>, default: undefined },
    status: { type: String as PropType<string | undefined>, default: undefined },
    icon: { type: String as PropType<string | undefined>, default: undefined },
    connector: { type: String as PropType<string | undefined>, default: undefined },
    position: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "connected": (_e: CustomEvent<{ status: string; label: string; position: string; disabled: boolean }>) => true,
    "disconnected": (_e: CustomEvent<{ status: string; label: string }>) => true,
    "click": (_e: CustomEvent<{ status: string; label: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XTimelineItemElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("connected", e as CustomEvent);
        el.addEventListener("x-timeline-item-connected", handler);
        cleanup.push(() => el.removeEventListener("x-timeline-item-connected", handler));
      }
      {
        const handler = (e: Event) => emit("disconnected", e as CustomEvent);
        el.addEventListener("x-timeline-item-disconnected", handler);
        cleanup.push(() => el.removeEventListener("x-timeline-item-disconnected", handler));
      }
      {
        const handler = (e: Event) => emit("click", e as CustomEvent);
        el.addEventListener("x-timeline-item-click", handler);
        cleanup.push(() => el.removeEventListener("x-timeline-item-click", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-timeline-item" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XTimelineItemInstance = InstanceType<typeof XTimelineItem>;
