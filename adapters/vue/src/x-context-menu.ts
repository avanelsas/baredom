// x-context-menu.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XContextMenu as XContextMenuElement } from "@vanelsas/baredom/x-context-menu";
import { init } from "@vanelsas/baredom/x-context-menu";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XContextMenuExposed>(name)`. */
export interface XContextMenuExposed {
  readonly el: XContextMenuElement | null;
}

export const XContextMenu = defineComponent({
  name: "XContextMenu",
  inheritAttrs: false,
  props: {
    open: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    placement: { type: String as PropType<string | undefined>, default: undefined },
    offset: { type: String as PropType<string | undefined>, default: undefined },
    zIndex: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "open-request": (_e: CustomEvent<{ reason: string }>) => true,
    "open": (_e: CustomEvent<{ reason: string }>) => true,
    "close-request": (_e: CustomEvent<{ reason: string }>) => true,
    "close": (_e: CustomEvent<{ reason: string }>) => true,
    "select": (_e: CustomEvent<{ item: Record<string, any> }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XContextMenuElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("open-request", e as CustomEvent);
        el.addEventListener("x-context-menu-open-request", handler);
        cleanup.push(() => el.removeEventListener("x-context-menu-open-request", handler));
      }
      {
        const handler = (e: Event) => emit("open", e as CustomEvent);
        el.addEventListener("x-context-menu-open", handler);
        cleanup.push(() => el.removeEventListener("x-context-menu-open", handler));
      }
      {
        const handler = (e: Event) => emit("close-request", e as CustomEvent);
        el.addEventListener("x-context-menu-close-request", handler);
        cleanup.push(() => el.removeEventListener("x-context-menu-close-request", handler));
      }
      {
        const handler = (e: Event) => emit("close", e as CustomEvent);
        el.addEventListener("x-context-menu-close", handler);
        cleanup.push(() => el.removeEventListener("x-context-menu-close", handler));
      }
      {
        const handler = (e: Event) => emit("select", e as CustomEvent);
        el.addEventListener("x-context-menu-select", handler);
        cleanup.push(() => el.removeEventListener("x-context-menu-select", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-context-menu" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XContextMenuInstance = InstanceType<typeof XContextMenu>;
