// x-proximity-list.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XProximityList as XProximityListElement } from "@vanelsas/baredom/x-proximity-list";
import { init } from "@vanelsas/baredom/x-proximity-list";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XProximityListExposed>(name)`. */
export interface XProximityListExposed {
  readonly el: XProximityListElement | null;
}

export const XProximityList = defineComponent({
  name: "XProximityList",
  inheritAttrs: false,
  props: {
    direction: { type: String as PropType<string | undefined>, default: undefined },
    radius: { type: Number as PropType<number | undefined>, default: undefined },
    maxScale: { type: Number as PropType<number | undefined>, default: undefined },
    lift: { type: Number as PropType<number | undefined>, default: undefined },
    gap: { type: Number as PropType<number | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "select": (_e: CustomEvent<{ index: number; item: any; source: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XProximityListElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("select", e as CustomEvent);
        el.addEventListener("x-proximity-list-select", handler);
        cleanup.push(() => el.removeEventListener("x-proximity-list-select", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-proximity-list" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XProximityListInstance = InstanceType<typeof XProximityList>;
