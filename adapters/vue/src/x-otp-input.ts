// x-otp-input.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XOtpInput as XOtpInputElement } from "@vanelsas/baredom/x-otp-input";
import { init } from "@vanelsas/baredom/x-otp-input";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XOtpInputExposed>(name)`. */
export interface XOtpInputExposed {
  readonly el: XOtpInputElement | null;
}

export const XOtpInput = defineComponent({
  name: "XOtpInput",
  inheritAttrs: false,
  props: {
    mask: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    placeholder: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    name: { type: String as PropType<string | undefined>, default: undefined },
    value: { type: String as PropType<string | undefined>, default: undefined },
    readonly: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    type: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    autofocus: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    length: { type: Number as PropType<number | undefined>, default: undefined },
    error: { type: String as PropType<string | undefined>, default: undefined },
    required: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "input": (_e: CustomEvent<{ name: string; value: string; complete: boolean }>) => true,
    "change": (_e: CustomEvent<{ name: string; value: string; complete: boolean }>) => true,
    "complete": (_e: CustomEvent<{ name: string; value: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XOtpInputElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("input", e as CustomEvent);
        el.addEventListener("x-otp-input-input", handler);
        cleanup.push(() => el.removeEventListener("x-otp-input-input", handler));
      }
      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-otp-input-change", handler);
        cleanup.push(() => el.removeEventListener("x-otp-input-change", handler));
      }
      {
        const handler = (e: Event) => emit("complete", e as CustomEvent);
        el.addEventListener("x-otp-input-complete", handler);
        cleanup.push(() => el.removeEventListener("x-otp-input-complete", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-otp-input" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XOtpInputInstance = InstanceType<typeof XOtpInput>;
