// x-currency-field.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, watch, type PropType } from "vue";
import type { XCurrencyField as XCurrencyFieldElement } from "@vanelsas/baredom/x-currency-field";
import { init } from "@vanelsas/baredom/x-currency-field";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XCurrencyFieldExposed>(name)`. */
export interface XCurrencyFieldExposed {
  readonly el: XCurrencyFieldElement | null;
}

export const XCurrencyField = defineComponent({
  name: "XCurrencyField",
  inheritAttrs: false,
  props: {
    min: { type: String as PropType<string | undefined>, default: undefined },
    placeholder: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    locale: { type: String as PropType<string | undefined>, default: undefined },
    name: { type: String as PropType<string | undefined>, default: undefined },
    value: { type: String as PropType<string | undefined>, default: undefined },
    hint: { type: String as PropType<string | undefined>, default: undefined },
    currency: { type: String as PropType<string | undefined>, default: undefined },
    readOnly: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    max: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    error: { type: String as PropType<string | undefined>, default: undefined },
    required: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    modelValue: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "update:modelValue": (_v: string) => true,
    "change-request": (_e: CustomEvent<{ name: string; value: string; previousValue: string }>) => true,
    "input": (_e: CustomEvent<{ name: string; value: string }>) => true,
    "change": (_e: CustomEvent<{ name: string; value: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XCurrencyFieldElement | null>(null);
    const cleanup: Array<() => void> = [];

    const writeValue = (v: string | undefined) => {
      const el = elRef.value;
      if (!el || v === undefined) return;
      if (v != null) el.setAttribute("value", String(v));
      else el.removeAttribute("value");
    };

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;
      writeValue(props.modelValue);

      {
        const handler = (e: Event) => emit("change-request", e as CustomEvent);
        el.addEventListener("x-currency-field-change-request", handler);
        cleanup.push(() => el.removeEventListener("x-currency-field-change-request", handler));
      }
      {
        const handler = (e: Event) => emit("input", e as CustomEvent);
        el.addEventListener("x-currency-field-input", handler);
        cleanup.push(() => el.removeEventListener("x-currency-field-input", handler));
      }
      {
        const handler = (e: Event) => {
          const detail = (e as CustomEvent).detail;
          emit("update:modelValue", String(detail.value));
          emit("change", e as CustomEvent);
        };
        el.addEventListener("x-currency-field-change", handler);
        cleanup.push(() => el.removeEventListener("x-currency-field-change", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    watch(() => props.modelValue, writeValue);

    expose({ el: elRef });

    return () => {
      const { modelValue: _modelValue, ...elProps } = props;
      void _modelValue;
      return h("x-currency-field" as any, { ...attrs, ref: elRef, ...elProps }, slots.default?.());
    };
  },
});

export type XCurrencyFieldInstance = InstanceType<typeof XCurrencyField>;
