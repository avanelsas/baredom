// x-card.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XCard as XCardElement } from "@vanelsas/baredom/x-card";
import { init } from "@vanelsas/baredom/x-card";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XCardExposed>(name)`. */
export interface XCardExposed {
  readonly el: XCardElement | null;
}

export const XCard = defineComponent({
  name: "XCard",
  inheritAttrs: false,
  props: {
    interactive: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "press": (_e: CustomEvent<{}>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XCardElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("press", e as CustomEvent);
        el.addEventListener("press", handler);
        cleanup.push(() => el.removeEventListener("press", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-card" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XCardInstance = InstanceType<typeof XCard>;
