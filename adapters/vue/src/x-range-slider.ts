// x-range-slider.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XRangeSlider as XRangeSliderElement } from "@vanelsas/baredom/x-range-slider";
import { init } from "@vanelsas/baredom/x-range-slider";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XRangeSliderExposed>(name)`. */
export interface XRangeSliderExposed {
  readonly el: XRangeSliderElement | null;
}

export const XRangeSlider = defineComponent({
  name: "XRangeSlider",
  inheritAttrs: false,
  props: {
    min: { type: String as PropType<string | undefined>, default: undefined },
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    showValue: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    name: { type: String as PropType<string | undefined>, default: undefined },
    start: { type: String as PropType<string | undefined>, default: undefined },
    minGap: { type: String as PropType<string | undefined>, default: undefined },
    size: { type: String as PropType<string | undefined>, default: undefined },
    readOnly: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    max: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    end: { type: String as PropType<string | undefined>, default: undefined },
    step: { type: String as PropType<string | undefined>, default: undefined },
  },
  emits: {
    "change-request": (_e: CustomEvent<{ start: number; end: number; previousStart: number; previousEnd: number; min: number; max: number }>) => true,
    "input": (_e: CustomEvent<{ start: number; end: number; min: number; max: number }>) => true,
    "change": (_e: CustomEvent<{ start: number; end: number; min: number; max: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XRangeSliderElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("change-request", e as CustomEvent);
        el.addEventListener("x-range-slider-change-request", handler);
        cleanup.push(() => el.removeEventListener("x-range-slider-change-request", handler));
      }
      {
        const handler = (e: Event) => emit("input", e as CustomEvent);
        el.addEventListener("x-range-slider-input", handler);
        cleanup.push(() => el.removeEventListener("x-range-slider-input", handler));
      }
      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-range-slider-change", handler);
        cleanup.push(() => el.removeEventListener("x-range-slider-change", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-range-slider" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XRangeSliderInstance = InstanceType<typeof XRangeSlider>;
