// x-scroll.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XScroll as XScrollElement } from "@vanelsas/baredom/x-scroll";
import { init } from "@vanelsas/baredom/x-scroll";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XScrollExposed>(name)`. */
export interface XScrollExposed {
  readonly el: XScrollElement | null;
}

export const XScroll = defineComponent({
  name: "XScroll",
  inheritAttrs: false,
  props: {
    disabled: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    showIndicators: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    snap: { type: String as PropType<string | undefined>, default: undefined },
    activeIndex: { type: Number as PropType<number | undefined>, default: undefined },
    mode: { type: String as PropType<string | undefined>, default: undefined },
    showControls: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    loop: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    gap: { type: Number as PropType<number | undefined>, default: undefined },
    autoPlay: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    interval: { type: Number as PropType<number | undefined>, default: undefined },
  },
  emits: {
    "change": (_e: CustomEvent<{ activeIndex: number; previousIndex: number }>) => true,
    "start": (_e: CustomEvent<{ direction: string; activeIndex: number }>) => true,
    "end": (_e: CustomEvent<{ activeIndex: number }>) => true,
    "loop": (_e: CustomEvent<{ direction: string }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XScrollElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("change", e as CustomEvent);
        el.addEventListener("x-scroll-change", handler);
        cleanup.push(() => el.removeEventListener("x-scroll-change", handler));
      }
      {
        const handler = (e: Event) => emit("start", e as CustomEvent);
        el.addEventListener("x-scroll-start", handler);
        cleanup.push(() => el.removeEventListener("x-scroll-start", handler));
      }
      {
        const handler = (e: Event) => emit("end", e as CustomEvent);
        el.addEventListener("x-scroll-end", handler);
        cleanup.push(() => el.removeEventListener("x-scroll-end", handler));
      }
      {
        const handler = (e: Event) => emit("loop", e as CustomEvent);
        el.addEventListener("x-scroll-loop", handler);
        cleanup.push(() => el.removeEventListener("x-scroll-loop", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-scroll" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XScrollInstance = InstanceType<typeof XScroll>;
