// x-progress-circle.ts — auto-generated by generate_vue.bb, do not edit

import { defineComponent, h, ref, onMounted, onBeforeUnmount, type PropType } from "vue";
import type { XProgressCircle as XProgressCircleElement } from "@vanelsas/baredom/x-progress-circle";
import { init } from "@vanelsas/baredom/x-progress-circle";

init();

/** Shape exposed via `expose()` — use with `useTemplateRef<XProgressCircleExposed>(name)`. */
export interface XProgressCircleExposed {
  readonly el: XProgressCircleElement | null;
}

export const XProgressCircle = defineComponent({
  name: "XProgressCircle",
  inheritAttrs: false,
  props: {
    value: { type: String as PropType<string | undefined>, default: undefined },
    max: { type: String as PropType<string | undefined>, default: undefined },
    variant: { type: String as PropType<string | undefined>, default: undefined },
    size: { type: String as PropType<string | undefined>, default: undefined },
    label: { type: String as PropType<string | undefined>, default: undefined },
    indeterminate: { type: Boolean as PropType<boolean | undefined>, default: undefined },
    showValue: { type: Boolean as PropType<boolean | undefined>, default: undefined },
  },
  emits: {
    "complete": (_e: CustomEvent<{ value: number; max: number }>) => true,
  },
  setup(props, { emit, attrs, slots, expose }) {
    const elRef = ref<XProgressCircleElement | null>(null);
    const cleanup: Array<() => void> = [];

    onMounted(() => {
      const el = elRef.value;
      if (!el) return;

      {
        const handler = (e: Event) => emit("complete", e as CustomEvent);
        el.addEventListener("x-progress-circle-complete", handler);
        cleanup.push(() => el.removeEventListener("x-progress-circle-complete", handler));
      }
    });

    onBeforeUnmount(() => {
      cleanup.forEach((fn) => fn());
    });

    expose({ el: elRef });

    return () => {
      return h("x-progress-circle" as any, { ...attrs, ref: elRef, ...props }, slots.default?.());
    };
  },
});

export type XProgressCircleInstance = InstanceType<typeof XProgressCircle>;
