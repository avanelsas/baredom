// x-file-upload.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XFileUpload as XFileUploadElement } from '@vanelsas/baredom/x-file-upload';
import { init } from '@vanelsas/baredom/x-file-upload';

init();

@Directive({ selector: 'x-file-upload', standalone: true })
export class BaredomFileUpload implements OnInit, OnDestroy {
  private el: XFileUploadElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XFileUploadElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set accept(v: string) { this.el.accept = v as any; }
  @Input() set multiple(v: boolean) { this.el.multiple = v as any; }
  @Input() set maxSize(v: number) { this.el.maxSize = v as any; }
  @Input() set maxFiles(v: number) { this.el.maxFiles = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set required(v: boolean) { this.el.required = v as any; }
  @Input() set name(v: string) { this.el.name = v as any; }

  @Output() select = new EventEmitter<CustomEvent<{ files: any; rejected: any }>>();
  @Output() remove = new EventEmitter<CustomEvent<{ file: Record<string, any>; remaining: any }>>();

  ngOnInit(): void {
    this.listen('x-file-upload-select', this.select);
    this.listen('x-file-upload-remove', this.remove);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
