// x-navbar.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XNavbar as XNavbarElement } from '@vanelsas/baredom/x-navbar';
import { init } from '@vanelsas/baredom/x-navbar';

init();

@Directive({ selector: 'x-navbar', standalone: true })
export class BaredomNavbar implements OnInit, OnDestroy {
  private el: XNavbarElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XNavbarElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set sticky(v: boolean) { this.el.sticky = v as any; }
  @Input() set elevated(v: boolean) { this.el.elevated = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set variant(v: string) { this.el.variant = v as any; }
  @Input() set orientation(v: string) { this.el.orientation = v as any; }
  @Input() set alignment(v: string) { this.el.alignment = v as any; }
  @Input() set breakpoint(v: string) { this.el.breakpoint = v as any; }

  @Output() focusVisible = new EventEmitter<CustomEvent<{}>>();
  @Output() navigate = new EventEmitter<CustomEvent<{ href: string; source: string }>>();
  @Output() brandActivate = new EventEmitter<CustomEvent<{ source: string }>>();

  ngOnInit(): void {
    this.listen('focus-visible', this.focusVisible);
    this.listen('navigate', this.navigate);
    this.listen('brand-activate', this.brandActivate);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
