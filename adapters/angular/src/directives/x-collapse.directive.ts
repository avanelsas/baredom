// x-collapse.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XCollapse as XCollapseElement } from '@vanelsas/baredom/x-collapse';
import { init } from '@vanelsas/baredom/x-collapse';

init();

@Directive({ selector: 'x-collapse', standalone: true })
export class BaredomCollapse implements OnInit, OnDestroy {
  private el: XCollapseElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XCollapseElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set open(v: boolean) { this.el.open = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set header(v: string) { this.el.header = v as any; }
  @Input() set durationMs(v: number) { this.el.durationMs = v as any; }

  @Output() toggle = new EventEmitter<CustomEvent<{ open: boolean; source: string }>>();
  @Output() change = new EventEmitter<CustomEvent<{ open: boolean }>>();

  ngOnInit(): void {
    this.listen('x-collapse-toggle', this.toggle);
    this.listen('x-collapse-change', this.change);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
