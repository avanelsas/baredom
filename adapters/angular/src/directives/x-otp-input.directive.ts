// x-otp-input.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XOtpInput as XOtpInputElement } from '@vanelsas/baredom/x-otp-input';
import { init } from '@vanelsas/baredom/x-otp-input';

init();

@Directive({ selector: 'x-otp-input', standalone: true })
export class BaredomOtpInput implements OnInit, OnDestroy {
  private el: XOtpInputElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XOtpInputElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set mask(v: boolean) { this.el.mask = v as any; }
  @Input() set placeholder(v: string) { this.el.placeholder = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set name(v: string) { this.el.name = v as any; }
  @Input() set value(v: string) { this.el.value = v as any; }
  @Input() set readonly(v: boolean) { this.el.readonly = v as any; }
  @Input() set type(v: string) { this.el.type = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set autofocus(v: boolean) { this.el.autofocus = v as any; }
  @Input() set length(v: number) { this.el.length = v as any; }
  @Input() set error(v: string) { this.el.error = v as any; }
  @Input() set required(v: boolean) { this.el.required = v as any; }

  @Output() input = new EventEmitter<CustomEvent<{ name: string; value: string; complete: boolean }>>();
  @Output() change = new EventEmitter<CustomEvent<{ name: string; value: string; complete: boolean }>>();
  @Output() complete = new EventEmitter<CustomEvent<{ name: string; value: string }>>();

  ngOnInit(): void {
    this.listen('x-otp-input-input', this.input);
    this.listen('x-otp-input-change', this.change);
    this.listen('x-otp-input-complete', this.complete);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

  focus(): void { return this.el.focus(); }
  clear(): void { return this.el.clear(); }
  checkValidity(): boolean { return this.el.checkValidity(); }
  reportValidity(): boolean { return this.el.reportValidity(); }
}
