// x-tooltip.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XTooltip as XTooltipElement } from '@vanelsas/baredom/x-tooltip';
import { init } from '@vanelsas/baredom/x-tooltip';

init();

@Directive({ selector: 'x-tooltip', standalone: true })
export class BaredomTooltip implements OnInit, OnDestroy {
  private el: XTooltipElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XTooltipElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set text(v: string) { this.el.text = v as any; }
  @Input() set placement(v: string) { this.el.placement = v as any; }
  @Input() set delay(v: number) { this.el.delay = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set open(v: boolean) { this.el.open = v as any; }

  @Output() show = new EventEmitter<CustomEvent<{}>>();
  @Output() hide = new EventEmitter<CustomEvent<{}>>();

  ngOnInit(): void {
    this.listen('x-tooltip-show', this.show);
    this.listen('x-tooltip-hide', this.hide);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
