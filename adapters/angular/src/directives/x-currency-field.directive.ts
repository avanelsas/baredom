// x-currency-field.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XCurrencyField as XCurrencyFieldElement } from '@vanelsas/baredom/x-currency-field';
import { init } from '@vanelsas/baredom/x-currency-field';

init();

@Directive({ selector: 'x-currency-field', standalone: true })
export class BaredomCurrencyField implements OnInit, OnDestroy {
  private el: XCurrencyFieldElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XCurrencyFieldElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set min(v: string) { this.el.min = v as any; }
  @Input() set placeholder(v: string) { this.el.placeholder = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set locale(v: string) { this.el.locale = v as any; }
  @Input() set name(v: string) { this.el.name = v as any; }
  @Input() set value(v: string) { this.el.value = v as any; }
  @Input() set hint(v: string) { this.el.hint = v as any; }
  @Input() set currency(v: string) { this.el.currency = v as any; }
  @Input() set readOnly(v: boolean) { this.el.readOnly = v as any; }
  @Input() set max(v: string) { this.el.max = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set error(v: string) { this.el.error = v as any; }
  @Input() set required(v: boolean) { this.el.required = v as any; }

  @Output() changeRequest = new EventEmitter<CustomEvent<{ name: string; value: string; previousValue: string }>>();
  @Output() input = new EventEmitter<CustomEvent<{ name: string; value: string }>>();
  @Output() change = new EventEmitter<CustomEvent<{ name: string; value: string }>>();

  ngOnInit(): void {
    this.listen('x-currency-field-change-request', this.changeRequest);
    this.listen('x-currency-field-input', this.input);
    this.listen('x-currency-field-change', this.change);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
