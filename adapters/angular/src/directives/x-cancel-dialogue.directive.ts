// x-cancel-dialogue.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XCancelDialogue as XCancelDialogueElement } from '@vanelsas/baredom/x-cancel-dialogue';
import { init } from '@vanelsas/baredom/x-cancel-dialogue';

init();

@Directive({ selector: 'x-cancel-dialogue', standalone: true })
export class BaredomCancelDialogue implements OnInit, OnDestroy {
  private el: XCancelDialogueElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XCancelDialogueElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set open(v: boolean) { this.el.open = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set headline(v: string) { this.el.headline = v as any; }
  @Input() set message(v: string) { this.el.message = v as any; }
  @Input() set confirmText(v: string) { this.el.confirmText = v as any; }
  @Input() set cancelText(v: string) { this.el.cancelText = v as any; }
  @Input() set danger(v: boolean) { this.el.danger = v as any; }

  @Output() cancelRequest = new EventEmitter<CustomEvent<{ reason: string }>>();
  @Output() cancel = new EventEmitter<CustomEvent<{}>>();
  @Output() confirmRequest = new EventEmitter<CustomEvent<{}>>();
  @Output() confirm = new EventEmitter<CustomEvent<{}>>();

  ngOnInit(): void {
    this.listen('x-cancel-dialogue-cancel-request', this.cancelRequest);
    this.listen('x-cancel-dialogue-cancel', this.cancel);
    this.listen('x-cancel-dialogue-confirm-request', this.confirmRequest);
    this.listen('x-cancel-dialogue-confirm', this.confirm);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

  close(): void { return this.el.close(); }
  confirmMethod(): void { return this.el.confirm(); }
  cancelMethod(reason: string): void { return this.el.cancel(reason); }
}
