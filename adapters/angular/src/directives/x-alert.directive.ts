// x-alert.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XAlert as XAlertElement } from '@vanelsas/baredom/x-alert';
import { init } from '@vanelsas/baredom/x-alert';

init();

@Directive({ selector: 'x-alert', standalone: true })
export class BaredomAlert implements OnInit, OnDestroy {
  private el: XAlertElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XAlertElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set type(v: string) { this.el.type = v as any; }
  @Input() set text(v: string) { this.el.text = v as any; }
  @Input() set icon(v: string) { this.el.icon = v as any; }
  @Input() set dismissible(v: boolean) { this.el.dismissible = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set timeoutMs(v: number) { this.el.timeoutMs = v as any; }

  @Output() dismiss = new EventEmitter<CustomEvent<{ type: string; reason: string; text: string }>>();

  ngOnInit(): void {
    this.listen('x-alert-dismiss', this.dismiss);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
