// x-code.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XCode as XCodeElement } from '@vanelsas/baredom/x-code';
import { init } from '@vanelsas/baredom/x-code';

init();

@Directive({ selector: 'x-code', standalone: true })
export class BaredomCode implements OnInit, OnDestroy {
  private el: XCodeElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XCodeElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set language(v: string) { this.el.language = v as any; }
  @Input() set filename(v: string) { this.el.filename = v as any; }
  @Input() set showCopy(v: boolean) { this.el.showCopy = v as any; }
  @Input() set lineNumbers(v: boolean) { this.el.lineNumbers = v as any; }
  @Input() set wrap(v: boolean) { this.el.wrap = v as any; }
  @Input() set maxLines(v: number) { this.el.maxLines = v as any; }
  @Input() set expanded(v: boolean) { this.el.expanded = v as any; }
  @Input() set code(v: string) { this.el.code = v as any; }

  @Output() copy = new EventEmitter<CustomEvent<{ code: string }>>();
  @Output() toggle = new EventEmitter<CustomEvent<{ expanded: boolean }>>();

  ngOnInit(): void {
    this.listen('x-code-copy', this.copy);
    this.listen('x-code-toggle', this.toggle);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

  expand(): void { return this.el.expand(); }
  collapse(): void { return this.el.collapse(); }
}
