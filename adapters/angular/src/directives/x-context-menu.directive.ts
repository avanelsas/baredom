// x-context-menu.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XContextMenu as XContextMenuElement } from '@vanelsas/baredom/x-context-menu';
import { init } from '@vanelsas/baredom/x-context-menu';

init();

@Directive({ selector: 'x-context-menu', standalone: true })
export class BaredomContextMenu implements OnInit, OnDestroy {
  private el: XContextMenuElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XContextMenuElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set open(v: boolean) { this.el.open = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set placement(v: string) { this.el.placement = v as any; }
  @Input() set offset(v: string) { this.el.offset = v as any; }
  @Input() set zIndex(v: string) { this.el.zIndex = v as any; }

  @Output() openRequest = new EventEmitter<CustomEvent<{ reason: string }>>();
  @Output() openEvent = new EventEmitter<CustomEvent<{ reason: string }>>();
  @Output() closeRequest = new EventEmitter<CustomEvent<{ reason: string }>>();
  @Output() close = new EventEmitter<CustomEvent<{ reason: string }>>();
  @Output() select = new EventEmitter<CustomEvent<{ item: Record<string, any> }>>();

  ngOnInit(): void {
    this.listen('x-context-menu-open-request', this.openRequest);
    this.listen('x-context-menu-open', this.openEvent);
    this.listen('x-context-menu-close-request', this.closeRequest);
    this.listen('x-context-menu-close', this.close);
    this.listen('x-context-menu-select', this.select);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
