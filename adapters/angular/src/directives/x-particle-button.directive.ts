// x-particle-button.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XParticleButton as XParticleButtonElement } from '@vanelsas/baredom/x-particle-button';
import { init } from '@vanelsas/baredom/x-particle-button';

init();

@Directive({ selector: 'x-particle-button', standalone: true })
export class BaredomParticleButton implements OnInit, OnDestroy {
  private el: XParticleButtonElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XParticleButtonElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set loading(v: boolean) { this.el.loading = v as any; }
  @Input() set pressed(v: boolean) { this.el.pressed = v as any; }

  @Output() press = new EventEmitter<CustomEvent<{ source: string }>>();
  @Output() pressStart = new EventEmitter<CustomEvent<{ source: string }>>();
  @Output() pressEnd = new EventEmitter<CustomEvent<{ source: string }>>();
  @Output() hoverStart = new EventEmitter<CustomEvent<{}>>();
  @Output() hoverEnd = new EventEmitter<CustomEvent<{}>>();
  @Output() focusVisible = new EventEmitter<CustomEvent<{}>>();
  @Output() burst = new EventEmitter<CustomEvent<{ mode: string; pressX: number; pressY: number }>>();
  @Output() reform = new EventEmitter<CustomEvent<{ mode: string; duration: number }>>();

  ngOnInit(): void {
    this.listen('press', this.press);
    this.listen('press-start', this.pressStart);
    this.listen('press-end', this.pressEnd);
    this.listen('hover-start', this.hoverStart);
    this.listen('hover-end', this.hoverEnd);
    this.listen('focus-visible', this.focusVisible);
    this.listen('burst', this.burst);
    this.listen('reform', this.reform);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
