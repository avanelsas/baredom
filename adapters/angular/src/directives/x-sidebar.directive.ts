// x-sidebar.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XSidebar as XSidebarElement } from '@vanelsas/baredom/x-sidebar';
import { init } from '@vanelsas/baredom/x-sidebar';

init();

@Directive({ selector: 'x-sidebar', standalone: true })
export class BaredomSidebar implements OnInit, OnDestroy {
  private el: XSidebarElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XSidebarElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set open(v: boolean) { this.el.open = v as any; }
  @Input() set collapsed(v: boolean) { this.el.collapsed = v as any; }

  @Output() toggle = new EventEmitter<CustomEvent<{ open: boolean }>>();
  @Output() dismiss = new EventEmitter<CustomEvent<{ reason: string }>>();

  ngOnInit(): void {
    this.listen('toggle', this.toggle);
    this.listen('dismiss', this.dismiss);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
