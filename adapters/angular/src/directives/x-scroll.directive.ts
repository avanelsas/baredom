// x-scroll.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XScroll as XScrollElement } from '@vanelsas/baredom/x-scroll';
import { init } from '@vanelsas/baredom/x-scroll';

init();

@Directive({ selector: 'x-scroll', standalone: true })
export class BaredomScroll implements OnInit, OnDestroy {
  private el: XScrollElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XScrollElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set showIndicators(v: boolean) { this.el.showIndicators = v as any; }
  @Input() set snap(v: string) { this.el.snap = v as any; }
  @Input() set activeIndex(v: number) { this.el.activeIndex = v as any; }
  @Input() set mode(v: string) { this.el.mode = v as any; }
  @Input() set showControls(v: boolean) { this.el.showControls = v as any; }
  @Input() set loop(v: boolean) { this.el.loop = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set gap(v: number) { this.el.gap = v as any; }
  @Input() set autoPlay(v: boolean) { this.el.autoPlay = v as any; }
  @Input() set interval(v: number) { this.el.interval = v as any; }

  @Output() change = new EventEmitter<CustomEvent<{ activeIndex: number; previousIndex: number }>>();
  @Output() start = new EventEmitter<CustomEvent<{ direction: string; activeIndex: number }>>();
  @Output() end = new EventEmitter<CustomEvent<{ activeIndex: number }>>();
  @Output() loopEvent = new EventEmitter<CustomEvent<{ direction: string }>>();

  ngOnInit(): void {
    this.listen('x-scroll-change', this.change);
    this.listen('x-scroll-start', this.start);
    this.listen('x-scroll-end', this.end);
    this.listen('x-scroll-loop', this.loopEvent);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
