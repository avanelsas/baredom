// x-welcome-tour.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XWelcomeTour as XWelcomeTourElement } from '@vanelsas/baredom/x-welcome-tour';
import { init } from '@vanelsas/baredom/x-welcome-tour';

init();

@Directive({ selector: 'x-welcome-tour', standalone: true })
export class BaredomWelcomeTour implements OnInit, OnDestroy {
  private el: XWelcomeTourElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XWelcomeTourElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set connector(v: string) { this.el.connector = v as any; }
  @Input() set open(v: boolean) { this.el.open = v as any; }
  @Input() set prevLabel(v: string) { this.el.prevLabel = v as any; }
  @Input() set nextLabel(v: string) { this.el.nextLabel = v as any; }
  @Input() set counter(v: boolean) { this.el.counter = v as any; }
  @Input() set doneLabel(v: string) { this.el.doneLabel = v as any; }
  @Input() set skipLabel(v: string) { this.el.skipLabel = v as any; }
  @Input() set dots(v: boolean) { this.el.dots = v as any; }
  @Input() set step(v: number) { this.el.step = v as any; }

  @Output() start = new EventEmitter<CustomEvent<{}>>();
  @Output() stepChange = new EventEmitter<CustomEvent<{ step: number; previousStep: number }>>();
  @Output() complete = new EventEmitter<CustomEvent<{ stepsCompleted: number }>>();
  @Output() skip = new EventEmitter<CustomEvent<{ step: number }>>();

  ngOnInit(): void {
    this.listen('x-welcome-tour-start', this.start);
    this.listen('x-welcome-tour-step-change', this.stepChange);
    this.listen('x-welcome-tour-complete', this.complete);
    this.listen('x-welcome-tour-skip', this.skip);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

  startMethod(): void { return this.el.start(); }
  next(): void { return this.el.next(); }
  prev(): void { return this.el.prev(); }
  goTo(step: number): void { return this.el.goTo(step); }
  completeMethod(): void { return this.el.complete(); }
  skipMethod(): void { return this.el.skip(); }
}
