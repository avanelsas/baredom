// x-command-palette.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XCommandPalette as XCommandPaletteElement } from '@vanelsas/baredom/x-command-palette';
import { init } from '@vanelsas/baredom/x-command-palette';

init();

@Directive({ selector: 'x-command-palette', standalone: true })
export class BaredomCommandPalette implements OnInit, OnDestroy {
  private el: XCommandPaletteElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XCommandPaletteElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set items(v: any) { this.el.items = v as any; }
  @Input() set open(v: boolean) { this.el.open = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }

  @Output() openRequest = new EventEmitter<CustomEvent<{}>>();
  @Output() openEvent = new EventEmitter<CustomEvent<{}>>();
  @Output() closeRequest = new EventEmitter<CustomEvent<{}>>();
  @Output() close = new EventEmitter<CustomEvent<{}>>();
  @Output() selectRequest = new EventEmitter<CustomEvent<{ item: Record<string, any> }>>();
  @Output() select = new EventEmitter<CustomEvent<{ item: Record<string, any> }>>();
  @Output() queryChange = new EventEmitter<CustomEvent<{ query: string }>>();

  ngOnInit(): void {
    this.listen('x-command-palette-open-request', this.openRequest);
    this.listen('x-command-palette-open', this.openEvent);
    this.listen('x-command-palette-close-request', this.closeRequest);
    this.listen('x-command-palette-close', this.close);
    this.listen('x-command-palette-select-request', this.selectRequest);
    this.listen('x-command-palette-select', this.select);
    this.listen('x-command-palette-query-change', this.queryChange);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
