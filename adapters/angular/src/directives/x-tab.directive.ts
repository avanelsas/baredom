// x-tab.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XTab as XTabElement } from '@vanelsas/baredom/x-tab';
import { init } from '@vanelsas/baredom/x-tab';

init();

@Directive({ selector: 'x-tab', standalone: true })
export class BaredomTab implements OnInit, OnDestroy {
  private el: XTabElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XTabElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set selected(v: boolean) { this.el.selected = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set value(v: string) { this.el.value = v as any; }

  @Output() tabSelect = new EventEmitter<CustomEvent<{ value: string }>>();

  ngOnInit(): void {
    this.listen('tab-select', this.tabSelect);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
