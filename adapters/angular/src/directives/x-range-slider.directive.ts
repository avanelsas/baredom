// x-range-slider.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XRangeSlider as XRangeSliderElement } from '@vanelsas/baredom/x-range-slider';
import { init } from '@vanelsas/baredom/x-range-slider';

init();

@Directive({ selector: 'x-range-slider', standalone: true })
export class BaredomRangeSlider implements OnInit, OnDestroy {
  private el: XRangeSliderElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XRangeSliderElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set min(v: string) { this.el.min = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set showValue(v: boolean) { this.el.showValue = v as any; }
  @Input() set name(v: string) { this.el.name = v as any; }
  @Input() set start(v: string) { this.el.start = v as any; }
  @Input() set minGap(v: string) { this.el.minGap = v as any; }
  @Input() set size(v: string) { this.el.size = v as any; }
  @Input() set readOnly(v: boolean) { this.el.readOnly = v as any; }
  @Input() set max(v: string) { this.el.max = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set end(v: string) { this.el.end = v as any; }
  @Input() set step(v: string) { this.el.step = v as any; }

  @Output() changeRequest = new EventEmitter<CustomEvent<{ start: number; end: number; previousStart: number; previousEnd: number; min: number; max: number }>>();
  @Output() input = new EventEmitter<CustomEvent<{ start: number; end: number; min: number; max: number }>>();
  @Output() change = new EventEmitter<CustomEvent<{ start: number; end: number; min: number; max: number }>>();

  ngOnInit(): void {
    this.listen('x-range-slider-change-request', this.changeRequest);
    this.listen('x-range-slider-input', this.input);
    this.listen('x-range-slider-change', this.change);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
