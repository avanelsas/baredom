// x-scroll-timeline.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XScrollTimeline as XScrollTimelineElement } from '@vanelsas/baredom/x-scroll-timeline';
import { init } from '@vanelsas/baredom/x-scroll-timeline';

init();

@Directive({ selector: 'x-scroll-timeline', standalone: true })
export class BaredomScrollTimeline implements OnInit, OnDestroy {
  private el: XScrollTimelineElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XScrollTimelineElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set autoplayLoop(v: boolean) { this.el.autoplayLoop = v as any; }
  @Input() set autoplaySpeed(v: number) { this.el.autoplaySpeed = v as any; }
  @Input() set autoplayIndicator(v: boolean) { this.el.autoplayIndicator = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set layout(v: string) { this.el.layout = v as any; }
  @Input() set marker(v: string) { this.el.marker = v as any; }
  @Input() set threshold(v: number) { this.el.threshold = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set autoplay(v: boolean) { this.el.autoplay = v as any; }
  @Input() set noProgress(v: boolean) { this.el.noProgress = v as any; }
  @Input() set track(v: string) { this.el.track = v as any; }

  @Output() entryChange = new EventEmitter<CustomEvent<{ index: number; id: string; previousIndex: number; previousId: string }>>();
  @Output() entryEnter = new EventEmitter<CustomEvent<{ index: number; id: string; progress: number }>>();
  @Output() entryLeave = new EventEmitter<CustomEvent<{ index: number; id: string; progress: number }>>();
  @Output() progress = new EventEmitter<CustomEvent<{ progress: number; activeIndex: number; activeId: string }>>();
  @Output() enter = new EventEmitter<CustomEvent<{ progress: number }>>();
  @Output() leave = new EventEmitter<CustomEvent<{ progress: number }>>();
  @Output() autoplayPause = new EventEmitter<CustomEvent<{ progress: number; activeIndex: number; activeId: string }>>();
  @Output() autoplayResume = new EventEmitter<CustomEvent<{ progress: number; activeIndex: number; activeId: string }>>();

  ngOnInit(): void {
    this.listen('x-scroll-timeline-entry-change', this.entryChange);
    this.listen('x-scroll-timeline-entry-enter', this.entryEnter);
    this.listen('x-scroll-timeline-entry-leave', this.entryLeave);
    this.listen('x-scroll-timeline-progress', this.progress);
    this.listen('x-scroll-timeline-enter', this.enter);
    this.listen('x-scroll-timeline-leave', this.leave);
    this.listen('x-scroll-timeline-autoplay-pause', this.autoplayPause);
    this.listen('x-scroll-timeline-autoplay-resume', this.autoplayResume);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
