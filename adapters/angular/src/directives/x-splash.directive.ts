// x-splash.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XSplash as XSplashElement } from '@vanelsas/baredom/x-splash';
import { init } from '@vanelsas/baredom/x-splash';

init();

@Directive({ selector: 'x-splash', standalone: true })
export class BaredomSplash implements OnInit, OnDestroy {
  private el: XSplashElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XSplashElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set active(v: boolean) { this.el.active = v as any; }
  @Input() set variant(v: string) { this.el.variant = v as any; }
  @Input() set progress(v: number) { this.el.progress = v as any; }
  @Input() set spinner(v: boolean) { this.el.spinner = v as any; }
  @Input() set overlay(v: string) { this.el.overlay = v as any; }

  @Output() hidden = new EventEmitter<CustomEvent<{}>>();

  ngOnInit(): void {
    this.listen('x-splash-hidden', this.hidden);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
