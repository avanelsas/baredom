// x-menu.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XMenu as XMenuElement } from '@vanelsas/baredom/x-menu';
import { init } from '@vanelsas/baredom/x-menu';

init();

@Directive({ selector: 'x-menu', standalone: true })
export class BaredomMenu implements OnInit, OnDestroy {
  private el: XMenuElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XMenuElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set open(v: boolean) { this.el.open = v as any; }
  @Input() set placement(v: string) { this.el.placement = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }

  @Output() openEvent = new EventEmitter<CustomEvent<{}>>();
  @Output() close = new EventEmitter<CustomEvent<{}>>();
  @Output() select = new EventEmitter<CustomEvent<{ value: string }>>();

  ngOnInit(): void {
    this.listen('x-menu-open', this.openEvent);
    this.listen('x-menu-close', this.close);
    this.listen('x-menu-select', this.select);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
