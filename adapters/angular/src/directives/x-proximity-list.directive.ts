// x-proximity-list.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XProximityList as XProximityListElement } from '@vanelsas/baredom/x-proximity-list';
import { init } from '@vanelsas/baredom/x-proximity-list';

init();

@Directive({ selector: 'x-proximity-list', standalone: true })
export class BaredomProximityList implements OnInit, OnDestroy {
  private el: XProximityListElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XProximityListElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set direction(v: string) { this.el.direction = v as any; }
  @Input() set radius(v: number) { this.el.radius = v as any; }
  @Input() set maxScale(v: number) { this.el.maxScale = v as any; }
  @Input() set lift(v: number) { this.el.lift = v as any; }
  @Input() set gap(v: number) { this.el.gap = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }

  @Output() select = new EventEmitter<CustomEvent<{ index: number; item: any; source: string }>>();

  ngOnInit(): void {
    this.listen('x-proximity-list-select', this.select);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
