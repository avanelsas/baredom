// x-table.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XTable as XTableElement } from '@vanelsas/baredom/x-table';
import { init } from '@vanelsas/baredom/x-table';

init();

@Directive({ selector: 'x-table', standalone: true })
export class BaredomTable implements OnInit, OnDestroy {
  private el: XTableElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XTableElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set columns(v: string) { this.el.columns = v as any; }
  @Input() set caption(v: string) { this.el.caption = v as any; }
  @Input() set selectable(v: string) { this.el.selectable = v as any; }
  @Input() set striped(v: boolean) { this.el.striped = v as any; }
  @Input() set bordered(v: boolean) { this.el.bordered = v as any; }
  @Input() set fullWidth(v: boolean) { this.el.fullWidth = v as any; }
  @Input() set compact(v: boolean) { this.el.compact = v as any; }
  @Input() set rowCount(v: number) { this.el.rowCount = v as any; }

  @Output() sort = new EventEmitter<CustomEvent<{ colIndex: number; direction: string; previousDirection: string }>>();
  @Output() rowSelect = new EventEmitter<CustomEvent<{ rowIndex: number; selected: boolean; selectionMode: string }>>();

  ngOnInit(): void {
    this.listen('x-table-sort', this.sort);
    this.listen('x-table-row-select', this.rowSelect);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
