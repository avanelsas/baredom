// x-toaster.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XToaster as XToasterElement } from '@vanelsas/baredom/x-toaster';
import { init } from '@vanelsas/baredom/x-toaster';

init();

@Directive({ selector: 'x-toaster', standalone: true })
export class BaredomToaster implements OnInit, OnDestroy {
  private el: XToasterElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XToasterElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set position(v: string) { this.el.position = v as any; }
  @Input() set maxToasts(v: number) { this.el.maxToasts = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }

  @Output() dismiss = new EventEmitter<CustomEvent<{ type: string; reason: string; heading: string; message: string }>>();

  ngOnInit(): void {
    this.listen('x-toaster-dismiss', this.dismiss);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

  toast(options: Record<string, any>): void { return this.el.toast(options); }
}
