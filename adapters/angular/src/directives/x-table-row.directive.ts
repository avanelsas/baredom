// x-table-row.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XTableRow as XTableRowElement } from '@vanelsas/baredom/x-table-row';
import { init } from '@vanelsas/baredom/x-table-row';

init();

@Directive({ selector: 'x-table-row', standalone: true })
export class BaredomTableRow implements OnInit, OnDestroy {
  private el: XTableRowElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XTableRowElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set selected(v: boolean) { this.el.selected = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set interactive(v: boolean) { this.el.interactive = v as any; }
  @Input() set rowIndex(v: number) { this.el.rowIndex = v as any; }

  @Output() click = new EventEmitter<CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean }>>();
  @Output() connected = new EventEmitter<CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean; interactive: boolean }>>();
  @Output() disconnected = new EventEmitter<CustomEvent<{}>>();

  ngOnInit(): void {
    this.listen('x-table-row-click', this.click);
    this.listen('x-table-row-connected', this.connected);
    this.listen('x-table-row-disconnected', this.disconnected);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
