// x-slider.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XSlider as XSliderElement } from '@vanelsas/baredom/x-slider';
import { init } from '@vanelsas/baredom/x-slider';

init();

@Directive({ selector: 'x-slider', standalone: true })
export class BaredomSlider implements OnInit, OnDestroy {
  private el: XSliderElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XSliderElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set min(v: string) { this.el.min = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set showValue(v: boolean) { this.el.showValue = v as any; }
  @Input() set name(v: string) { this.el.name = v as any; }
  @Input() set value(v: string) { this.el.value = v as any; }
  @Input() set size(v: string) { this.el.size = v as any; }
  @Input() set readOnly(v: boolean) { this.el.readOnly = v as any; }
  @Input() set max(v: string) { this.el.max = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set step(v: string) { this.el.step = v as any; }

  @Output() changeRequest = new EventEmitter<CustomEvent<{ value: number; previousValue: number; min: number; max: number }>>();
  @Output() input = new EventEmitter<CustomEvent<{ value: number; min: number; max: number }>>();
  @Output() change = new EventEmitter<CustomEvent<{ value: number; min: number; max: number }>>();

  ngOnInit(): void {
    this.listen('x-slider-change-request', this.changeRequest);
    this.listen('x-slider-input', this.input);
    this.listen('x-slider-change', this.change);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
