// x-liquid-fill.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XLiquidFill as XLiquidFillElement } from '@vanelsas/baredom/x-liquid-fill';
import { init } from '@vanelsas/baredom/x-liquid-fill';

init();

@Directive({ selector: 'x-liquid-fill', standalone: true })
export class BaredomLiquidFill implements OnInit, OnDestroy {
  private el: XLiquidFillElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XLiquidFillElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set waveIntensity(v: number) { this.el.waveIntensity = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set splashIntensity(v: number) { this.el.splashIntensity = v as any; }
  @Input() set mode(v: string) { this.el.mode = v as any; }
  @Input() set orientation(v: string) { this.el.orientation = v as any; }
  @Input() set theme(v: string) { this.el.theme = v as any; }
  @Input() set layers(v: number) { this.el.layers = v as any; }
  @Input() set target(v: string) { this.el.target = v as any; }

  @Output() progress = new EventEmitter<CustomEvent<{ progress: number; velocity: number }>>();

  ngOnInit(): void {
    this.listen('x-liquid-fill-progress', this.progress);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
