// x-drag-panel.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XDragPanel as XDragPanelElement } from '@vanelsas/baredom/x-drag-panel';
import { init } from '@vanelsas/baredom/x-drag-panel';

init();

@Directive({ selector: 'x-drag-panel', standalone: true })
export class BaredomDragPanel implements OnInit, OnDestroy {
  private el: XDragPanelElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XDragPanelElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set kind(v: string) { this.el.kind = v as any; }
  @Input() set value(v: string) { this.el.value = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set grab(v: string) { this.el.grab = v as any; }
  @Input() set autoScroll(v: string) { this.el.autoScroll = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set pending(v: boolean) { this.el.pending = v as any; }

  @Output() dragStart = new EventEmitter<CustomEvent<{ kind: string; value: string }>>();
  @Output() dragCancel = new EventEmitter<CustomEvent<{ kind: string; value: string; reason: string }>>();

  ngOnInit(): void {
    this.listen('x-drag-panel-drag-start', this.dragStart);
    this.listen('x-drag-panel-drag-cancel', this.dragCancel);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
