// x-chip.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XChip as XChipElement } from '@vanelsas/baredom/x-chip';
import { init } from '@vanelsas/baredom/x-chip';

init();

@Directive({ selector: 'x-chip', standalone: true })
export class BaredomChip implements OnInit, OnDestroy {
  private el: XChipElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XChipElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set value(v: string) { this.el.value = v as any; }
  @Input() set removable(v: boolean) { this.el.removable = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }

  @Output() remove = new EventEmitter<CustomEvent<{ value: string; label: string }>>();

  ngOnInit(): void {
    this.listen('x-chip-remove', this.remove);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
