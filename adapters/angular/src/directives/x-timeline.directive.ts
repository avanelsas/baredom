// x-timeline.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XTimeline as XTimelineElement } from '@vanelsas/baredom/x-timeline';
import { init } from '@vanelsas/baredom/x-timeline';

init();

@Directive({ selector: 'x-timeline', standalone: true })
export class BaredomTimeline implements OnInit, OnDestroy {
  private el: XTimelineElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XTimelineElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set position(v: string) { this.el.position = v as any; }
  @Input() set striped(v: boolean) { this.el.striped = v as any; }

  @Output() select = new EventEmitter<CustomEvent<{ index: number; status: string; label: string }>>();

  ngOnInit(): void {
    this.listen('x-timeline-select', this.select);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
