// x-scroll-stack.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XScrollStack as XScrollStackElement } from '@vanelsas/baredom/x-scroll-stack';
import { init } from '@vanelsas/baredom/x-scroll-stack';

init();

@Directive({ selector: 'x-scroll-stack', standalone: true })
export class BaredomScrollStack implements OnInit, OnDestroy {
  private el: XScrollStackElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XScrollStackElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set peek(v: number) { this.el.peek = v as any; }
  @Input() set rotation(v: number) { this.el.rotation = v as any; }
  @Input() set scrollDistance(v: number) { this.el.scrollDistance = v as any; }
  @Input() set align(v: string) { this.el.align = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }

  @Output() change = new EventEmitter<CustomEvent<{ stackedCount: number; totalCount: number; progress: number }>>();
  @Output() progress = new EventEmitter<CustomEvent<{ progress: number; stackedCount: number; totalCount: number }>>();

  ngOnInit(): void {
    this.listen('x-scroll-stack-change', this.change);
    this.listen('x-scroll-stack-progress', this.progress);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

  refresh(): void { return this.el.refresh(); }
}
