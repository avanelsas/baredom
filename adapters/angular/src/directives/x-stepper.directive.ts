// x-stepper.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XStepper as XStepperElement } from '@vanelsas/baredom/x-stepper';
import { init } from '@vanelsas/baredom/x-stepper';

init();

@Directive({ selector: 'x-stepper', standalone: true })
export class BaredomStepper implements OnInit, OnDestroy {
  private el: XStepperElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XStepperElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set steps(v: string) { this.el.steps = v as any; }
  @Input() set current(v: number) { this.el.current = v as any; }
  @Input() set orientation(v: string) { this.el.orientation = v as any; }
  @Input() set size(v: string) { this.el.size = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }

  @Output() change = new EventEmitter<CustomEvent<{ from: number; to: number }>>();

  ngOnInit(): void {
    this.listen('x-stepper-change', this.change);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
