// x-rating.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XRating as XRatingElement } from '@vanelsas/baredom/x-rating';
import { init } from '@vanelsas/baredom/x-rating';

init();

@Directive({ selector: 'x-rating', standalone: true })
export class BaredomRating implements OnInit, OnDestroy {
  private el: XRatingElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XRatingElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set allowClear(v: boolean) { this.el.allowClear = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set name(v: string) { this.el.name = v as any; }
  @Input() set value(v: string) { this.el.value = v as any; }
  @Input() set precision(v: string) { this.el.precision = v as any; }
  @Input() set size(v: string) { this.el.size = v as any; }
  @Input() set readOnly(v: boolean) { this.el.readOnly = v as any; }
  @Input() set max(v: string) { this.el.max = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set shape(v: string) { this.el.shape = v as any; }

  @Output() changeRequest = new EventEmitter<CustomEvent<{ value: number; previousValue: number; max: number }>>();
  @Output() change = new EventEmitter<CustomEvent<{ value: number; max: number }>>();
  @Output() hover = new EventEmitter<CustomEvent<{ value: number; max: number }>>();

  ngOnInit(): void {
    this.listen('x-rating-change-request', this.changeRequest);
    this.listen('x-rating-change', this.change);
    this.listen('x-rating-hover', this.hover);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
