// x-copy.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XCopy as XCopyElement } from '@vanelsas/baredom/x-copy';
import { init } from '@vanelsas/baredom/x-copy';

init();

@Directive({ selector: 'x-copy', standalone: true })
export class BaredomCopy implements OnInit, OnDestroy {
  private el: XCopyElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XCopyElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set errorMessage(v: string) { this.el.errorMessage = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set textValue(v: string) { this.el.textValue = v as any; }
  @Input() set mode(v: string) { this.el.mode = v as any; }
  @Input() set fromAttr(v: string) { this.el.fromAttr = v as any; }
  @Input() set tooltipMs(v: number) { this.el.tooltipMs = v as any; }
  @Input() set hotkey(v: string) { this.el.hotkey = v as any; }
  @Input() set from(v: string) { this.el.from = v as any; }
  @Input() set showTooltip(v: boolean) { this.el.showTooltip = v as any; }
  @Input() set successMessage(v: string) { this.el.successMessage = v as any; }
  @Input() set text(v: string) { this.el.text = v as any; }

  @Output() request = new EventEmitter<CustomEvent<{ text: string; mode: string; from: string; fromAttr: string }>>();
  @Output() success = new EventEmitter<CustomEvent<{ text: string }>>();
  @Output() error = new EventEmitter<CustomEvent<{ error: string }>>();

  ngOnInit(): void {
    this.listen('x-copy-request', this.request);
    this.listen('x-copy-success', this.success);
    this.listen('x-copy-error', this.error);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

  copy(): void { return this.el.copy(); }
}
