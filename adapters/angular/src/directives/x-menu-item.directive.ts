// x-menu-item.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XMenuItem as XMenuItemElement } from '@vanelsas/baredom/x-menu-item';
import { init } from '@vanelsas/baredom/x-menu-item';

init();

@Directive({ selector: 'x-menu-item', standalone: true })
export class BaredomMenuItem implements OnInit, OnDestroy {
  private el: XMenuItemElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XMenuItemElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set value(v: string) { this.el.value = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set variant(v: string) { this.el.variant = v as any; }
  @Input() set type(v: string) { this.el.type = v as any; }

  @Output() select = new EventEmitter<CustomEvent<{ value: string }>>();

  ngOnInit(): void {
    this.listen('x-menu-item-select', this.select);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
