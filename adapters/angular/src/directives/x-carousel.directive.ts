// x-carousel.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XCarousel as XCarouselElement } from '@vanelsas/baredom/x-carousel';
import { init } from '@vanelsas/baredom/x-carousel';

init();

@Directive({ selector: 'x-carousel', standalone: true })
export class BaredomCarousel implements OnInit, OnDestroy {
  private el: XCarouselElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XCarouselElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set arrows(v: boolean) { this.el.arrows = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set transition(v: string) { this.el.transition = v as any; }
  @Input() set loop(v: boolean) { this.el.loop = v as any; }
  @Input() set interval(v: number) { this.el.interval = v as any; }
  @Input() set autoplay(v: boolean) { this.el.autoplay = v as any; }
  @Input() set peek(v: string) { this.el.peek = v as any; }
  @Input() set dots(v: boolean) { this.el.dots = v as any; }
  @Input() set currentSlide(v: number) { this.el.currentSlide = v as any; }
  @Input() set direction(v: string) { this.el.direction = v as any; }

  @Output() change = new EventEmitter<CustomEvent<{ index: number; previousIndex: number; reason: string }>>();

  ngOnInit(): void {
    this.listen('x-carousel-change', this.change);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
