// x-table-cell.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XTableCell as XTableCellElement } from '@vanelsas/baredom/x-table-cell';
import { init } from '@vanelsas/baredom/x-table-cell';

init();

@Directive({ selector: 'x-table-cell', standalone: true })
export class BaredomTableCell implements OnInit, OnDestroy {
  private el: XTableCellElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XTableCellElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set align(v: string) { this.el.align = v as any; }
  @Input() set rowSpan(v: number) { this.el.rowSpan = v as any; }
  @Input() set truncate(v: boolean) { this.el.truncate = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set valign(v: string) { this.el.valign = v as any; }
  @Input() set type(v: string) { this.el.type = v as any; }
  @Input() set scope(v: string) { this.el.scope = v as any; }
  @Input() set colSpan(v: number) { this.el.colSpan = v as any; }
  @Input() set sticky(v: string) { this.el.sticky = v as any; }
  @Input() set sortable(v: boolean) { this.el.sortable = v as any; }
  @Input() set sortDirection(v: string) { this.el.sortDirection = v as any; }

  @Output() sort = new EventEmitter<CustomEvent<{ direction: string; previousDirection: string }>>();
  @Output() connected = new EventEmitter<CustomEvent<{ type: string; scope: string; colSpan: number; rowSpan: number; align: string }>>();
  @Output() disconnected = new EventEmitter<CustomEvent<{}>>();

  ngOnInit(): void {
    this.listen('x-table-cell-sort', this.sort);
    this.listen('x-table-cell-connected', this.connected);
    this.listen('x-table-cell-disconnected', this.disconnected);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
