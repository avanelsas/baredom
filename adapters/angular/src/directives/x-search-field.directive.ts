// x-search-field.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XSearchField as XSearchFieldElement } from '@vanelsas/baredom/x-search-field';
import { init } from '@vanelsas/baredom/x-search-field';

init();

@Directive({ selector: 'x-search-field', standalone: true })
export class BaredomSearchField implements OnInit, OnDestroy {
  private el: XSearchFieldElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XSearchFieldElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set value(v: string) { this.el.value = v as any; }
  @Input() set name(v: string) { this.el.name = v as any; }
  @Input() set placeholder(v: string) { this.el.placeholder = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set autocomplete(v: string) { this.el.autocomplete = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }
  @Input() set required(v: boolean) { this.el.required = v as any; }

  @Output() input = new EventEmitter<CustomEvent<{ name: string; value: string }>>();
  @Output() change = new EventEmitter<CustomEvent<{ name: string; value: string }>>();
  @Output() search = new EventEmitter<CustomEvent<{ name: string; value: string }>>();
  @Output() clear = new EventEmitter<CustomEvent<{ name: string }>>();

  ngOnInit(): void {
    this.listen('x-search-field-input', this.input);
    this.listen('x-search-field-change', this.change);
    this.listen('x-search-field-search', this.search);
    this.listen('x-search-field-clear', this.clear);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
