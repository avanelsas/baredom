// x-floating-panel.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XFloatingPanel as XFloatingPanelElement } from '@vanelsas/baredom/x-floating-panel';
import { init } from '@vanelsas/baredom/x-floating-panel';

init();

@Directive({ selector: 'x-floating-panel', standalone: true })
export class BaredomFloatingPanel implements OnInit, OnDestroy {
  private el: XFloatingPanelElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XFloatingPanelElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set open(v: boolean) { this.el.open = v as any; }
  @Input() set x(v: number) { this.el.x = v as any; }
  @Input() set y(v: number) { this.el.y = v as any; }
  @Input() set step(v: number) { this.el.step = v as any; }
  @Input() set closable(v: boolean) { this.el.closable = v as any; }
  @Input() set resizable(v: boolean) { this.el.resizable = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }
  @Input() set focusOnOpen(v: boolean) { this.el.focusOnOpen = v as any; }

  @Output() toggle = new EventEmitter<CustomEvent<{ open: boolean }>>();
  @Output() dismissRequest = new EventEmitter<CustomEvent<{ reason: string }>>();
  @Output() move = new EventEmitter<CustomEvent<{ x: number; y: number; source: string }>>();

  ngOnInit(): void {
    this.listen('x-floating-panel-toggle', this.toggle);
    this.listen('x-floating-panel-dismiss-request', this.dismissRequest);
    this.listen('x-floating-panel-move', this.move);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

  show(): void { return this.el.show(); }
  hide(): void { return this.el.hide(); }
  toggleMethod(): void { return this.el.toggle(); }
}
