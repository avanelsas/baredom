// x-kinetic-font.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XKineticFont as XKineticFontElement } from '@vanelsas/baredom/x-kinetic-font';
import { init } from '@vanelsas/baredom/x-kinetic-font';

init();

@Directive({ selector: 'x-kinetic-font', standalone: true })
export class BaredomKineticFont implements OnInit, OnDestroy {
  private el: XKineticFontElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XKineticFontElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set mass(v: number) { this.el.mass = v as any; }
  @Input() set tension(v: number) { this.el.tension = v as any; }
  @Input() set mode(v: string) { this.el.mode = v as any; }
  @Input() set perChar(v: boolean) { this.el.perChar = v as any; }
  @Input() set radius(v: number) { this.el.radius = v as any; }
  @Input() set intensity(v: number) { this.el.intensity = v as any; }
  @Input() set fontFamily(v: string) { this.el.fontFamily = v as any; }
  @Input() set friction(v: number) { this.el.friction = v as any; }
  @Input() set trigger(v: string) { this.el.trigger = v as any; }
  @Input() set text(v: string) { this.el.text = v as any; }

  @Output() springActivate = new EventEmitter<CustomEvent<{}>>();
  @Output() springSettle = new EventEmitter<CustomEvent<{}>>();

  ngOnInit(): void {
    this.listen('x-kinetic-font-spring-activate', this.springActivate);
    this.listen('x-kinetic-font-spring-settle', this.springSettle);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
