// x-morph-stack.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XMorphStack as XMorphStackElement } from '@vanelsas/baredom/x-morph-stack';
import { init } from '@vanelsas/baredom/x-morph-stack';

init();

@Directive({ selector: 'x-morph-stack', standalone: true })
export class BaredomMorphStack implements OnInit, OnDestroy {
  private el: XMorphStackElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XMorphStackElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set activeState(v: string) { this.el.activeState = v as any; }
  @Input() set activeIndex(v: number) { this.el.activeIndex = v as any; }
  @Input() set stiffness(v: number) { this.el.stiffness = v as any; }
  @Input() set damping(v: number) { this.el.damping = v as any; }
  @Input() set mass(v: number) { this.el.mass = v as any; }
  @Input() set variant(v: string) { this.el.variant = v as any; }
  @Input() set duration(v: number) { this.el.duration = v as any; }
  @Input() set disabled(v: boolean) { this.el.disabled = v as any; }

  @Output() change = new EventEmitter<CustomEvent<{ from: string; to: string; reason: string }>>();
  @Output() changed = new EventEmitter<CustomEvent<{ from: string; to: string }>>();

  ngOnInit(): void {
    this.listen('x-morph-stack-change', this.change);
    this.listen('x-morph-stack-changed', this.changed);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
