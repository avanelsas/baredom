// x-progress-circle.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XProgressCircle as XProgressCircleElement } from '@vanelsas/baredom/x-progress-circle';
import { init } from '@vanelsas/baredom/x-progress-circle';

init();

@Directive({ selector: 'x-progress-circle', standalone: true })
export class BaredomProgressCircle implements OnInit, OnDestroy {
  private el: XProgressCircleElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XProgressCircleElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set value(v: string) { this.el.value = v as any; }
  @Input() set max(v: string) { this.el.max = v as any; }
  @Input() set indeterminate(v: boolean) { this.el.indeterminate = v as any; }
  @Input() set showValue(v: boolean) { this.el.showValue = v as any; }

  @Output() complete = new EventEmitter<CustomEvent<{ value: number; max: number }>>();

  ngOnInit(): void {
    this.listen('x-progress-circle-complete', this.complete);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

}
