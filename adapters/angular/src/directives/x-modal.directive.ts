// x-modal.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, Input, Output, EventEmitter, OnInit, OnDestroy, NgZone } from '@angular/core';
import type { XModal as XModalElement } from '@vanelsas/baredom/x-modal';
import { init } from '@vanelsas/baredom/x-modal';

init();

@Directive({ selector: 'x-modal', standalone: true })
export class BaredomModal implements OnInit, OnDestroy {
  private el: XModalElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XModalElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  @Input() set open(v: boolean) { this.el.open = v as any; }
  @Input() set size(v: string) { this.el.size = v as any; }
  @Input() set label(v: string) { this.el.label = v as any; }

  @Output() toggle = new EventEmitter<CustomEvent<{ open: boolean }>>();
  @Output() dismiss = new EventEmitter<CustomEvent<{ reason: string }>>();

  ngOnInit(): void {
    this.listen('x-modal-toggle', this.toggle);
    this.listen('x-modal-dismiss', this.dismiss);
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  private listen(event: string, emitter: EventEmitter<any>): void {
    const handler = (e: Event) => this.zone.run(() => emitter.emit(e));
    this.el.addEventListener(event, handler);
    this.listeners.push(() => this.el.removeEventListener(event, handler));
  }

  show(): void { return this.el.show(); }
  hide(): void { return this.el.hide(); }
  toggleMethod(): void { return this.el.toggle(); }
}
