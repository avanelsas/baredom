// x-currency-field-cva.directive.ts — auto-generated by generate_angular.bb, do not edit

import { Directive, ElementRef, forwardRef, OnInit, OnDestroy, NgZone } from '@angular/core';
import { ControlValueAccessor, NG_VALUE_ACCESSOR } from '@angular/forms';
import type { XCurrencyField as XCurrencyFieldElement } from '@vanelsas/baredom/x-currency-field';
import { init } from '@vanelsas/baredom/x-currency-field';

init();

@Directive({
  selector: 'x-currency-field[formControlName],x-currency-field[formControl],x-currency-field[ngModel]',
  standalone: true,
  providers: [{
    provide: NG_VALUE_ACCESSOR,
    useExisting: forwardRef(() => BaredomCurrencyFieldCva),
    multi: true,
  }],
})
export class BaredomCurrencyFieldCva implements ControlValueAccessor, OnInit, OnDestroy {
  private el: XCurrencyFieldElement;
  private zone: NgZone;
  private listeners: Array<() => void> = [];

  constructor(elRef: ElementRef<XCurrencyFieldElement>, zone: NgZone) {
    this.el = elRef.nativeElement;
    this.zone = zone;
  }

  private onChange: (value: string) => void = () => {};
  private onTouched: () => void = () => {};

  ngOnInit(): void {
    const handler = (e: Event) => {
      const detail = (e as CustomEvent).detail;
      this.zone.run(() => this.onChange(detail.value));
    };
    this.el.addEventListener('x-currency-field-change', handler);
    this.listeners.push(() => this.el.removeEventListener('x-currency-field-change', handler));

    const blur = () => this.zone.run(() => this.onTouched());
    this.el.addEventListener('focusout', blur);
    this.listeners.push(() => this.el.removeEventListener('focusout', blur));
  }

  ngOnDestroy(): void {
    this.listeners.forEach(fn => fn());
  }

  writeValue(v: string): void {
    if (v != null) this.el.setAttribute('value', String(v));
    else this.el.removeAttribute('value');
  }

  registerOnChange(fn: (v: string) => void): void {
    this.onChange = fn;
  }

  registerOnTouched(fn: () => void): void {
    this.onTouched = fn;
  }

  setDisabledState(d: boolean): void {
    if (d) this.el.setAttribute('disabled', '');
    else this.el.removeAttribute('disabled');
  }
}
