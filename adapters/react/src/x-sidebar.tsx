"use client";
// x-sidebar.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XSidebar as XSidebarElement } from "@vanelsas/baredom/x-sidebar";
import { init } from "@vanelsas/baredom/x-sidebar";

init();

export interface XSidebarProps {
  open?: boolean;
  collapsed?: boolean;
  onToggle?: (e: CustomEvent<{ open: boolean }>) => void;
  onDismiss?: (e: CustomEvent<{ reason: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XSidebar = forwardRef<XSidebarElement, XSidebarProps>(
  function XSidebar(props, forwardedRef) {
    const { onToggle, onDismiss, children, ...rest } = props;
    const innerRef = useRef<XSidebarElement>(null);

    const setRef = (el: XSidebarElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onToggle) {
        el.addEventListener("toggle", onToggle as EventListener);
        cleanup.push(() => el.removeEventListener("toggle", onToggle as EventListener));
      }
      if (onDismiss) {
        el.addEventListener("dismiss", onDismiss as EventListener);
        cleanup.push(() => el.removeEventListener("dismiss", onDismiss as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onToggle, onDismiss]);

    return <x-sidebar ref={setRef} {...rest}>{children}</x-sidebar>;
  }
);
