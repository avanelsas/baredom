"use client";
// x-calendar.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XCalendar as XCalendarElement } from "@vanelsas/baredom/x-calendar";
import { init } from "@vanelsas/baredom/x-calendar";

init();

export interface XCalendarProps {
  min?: string;
  disabled?: boolean;
  locale?: string;
  autoSwap?: boolean;
  value?: string;
  mode?: string;
  start?: string;
  month?: string;
  showWeekNumbers?: boolean;
  disabledDates?: string;
  max?: string;
  firstDayOfWeek?: string;
  end?: string;
  rangeAllowSameDay?: boolean;
  onChange?: (e: CustomEvent<{ value: string; start: string; end: string; mode: string }>) => void;
  onNavigate?: (e: CustomEvent<{ month: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XCalendar = forwardRef<XCalendarElement, XCalendarProps>(
  function XCalendar(props, forwardedRef) {
    const { onChange, onNavigate, children, ...rest } = props;
    const innerRef = useRef<XCalendarElement>(null);

    const setRef = (el: XCalendarElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onChange) {
        el.addEventListener("x-calendar-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-calendar-change", onChange as EventListener));
      }
      if (onNavigate) {
        el.addEventListener("x-calendar-navigate", onNavigate as EventListener);
        cleanup.push(() => el.removeEventListener("x-calendar-navigate", onNavigate as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onChange, onNavigate]);

    return <x-calendar ref={setRef} {...rest}>{children}</x-calendar>;
  }
);
