"use client";
// x-drawer.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XDrawer as XDrawerElement } from "@vanelsas/baredom/x-drawer";
import { init } from "@vanelsas/baredom/x-drawer";

init();

export interface XDrawerProps {
  open?: boolean;
  placement?: string;
  label?: string;
  onToggle?: (e: CustomEvent<{ open: boolean }>) => void;
  onDismiss?: (e: CustomEvent<{ reason: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XDrawer = forwardRef<XDrawerElement, XDrawerProps>(
  function XDrawer(props, forwardedRef) {
    const { onToggle, onDismiss, children, ...rest } = props;
    const innerRef = useRef<XDrawerElement>(null);

    const setRef = (el: XDrawerElement | null) => {
      (innerRef as React.MutableRefObject<XDrawerElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onToggle) {
        el.addEventListener("x-drawer-toggle", onToggle as EventListener);
        cleanup.push(() => el.removeEventListener("x-drawer-toggle", onToggle as EventListener));
      }
      if (onDismiss) {
        el.addEventListener("x-drawer-dismiss", onDismiss as EventListener);
        cleanup.push(() => el.removeEventListener("x-drawer-dismiss", onDismiss as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onToggle, onDismiss]);

    return <x-drawer ref={setRef} {...rest}>{children}</x-drawer>;
  }
);
