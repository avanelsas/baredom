"use client";
// x-organic-shape.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XOrganicShape as XOrganicShapeElement } from "@vanelsas/baredom/x-organic-shape";
import { init } from "@vanelsas/baredom/x-organic-shape";

init();

export interface XOrganicShapeProps {
  shape?: string;
  path?: string;
  animation?: string;
  ratio?: string;
  width?: string;
  height?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XOrganicShape = forwardRef<XOrganicShapeElement, XOrganicShapeProps>(
  function XOrganicShape(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XOrganicShapeElement>(null);

    const setRef = (el: XOrganicShapeElement | null) => {
      (innerRef as React.MutableRefObject<XOrganicShapeElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-organic-shape ref={setRef} {...rest}>{children}</x-organic-shape>;
  }
);
