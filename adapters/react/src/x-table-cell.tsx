"use client";
// x-table-cell.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XTableCell as XTableCellElement } from "@vanelsas/baredom/x-table-cell";
import { init } from "@vanelsas/baredom/x-table-cell";

init();

export interface XTableCellProps {
  align?: string;
  rowSpan?: number;
  truncate?: boolean;
  disabled?: boolean;
  valign?: string;
  type?: string;
  scope?: string;
  colSpan?: number;
  sticky?: string;
  sortable?: boolean;
  sortDirection?: string;
  onSort?: (e: CustomEvent<{ direction: string; previousDirection: string }>) => void;
  onConnected?: (e: CustomEvent<{ type: string; scope: string; colSpan: number; rowSpan: number; align: string }>) => void;
  onDisconnected?: (e: CustomEvent<{}>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XTableCell = forwardRef<XTableCellElement, XTableCellProps>(
  function XTableCell(props, forwardedRef) {
    const { onSort, onConnected, onDisconnected, children, ...rest } = props;
    const innerRef = useRef<XTableCellElement>(null);

    const setRef = (el: XTableCellElement | null) => {
      (innerRef as React.MutableRefObject<XTableCellElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onSort) {
        el.addEventListener("x-table-cell-sort", onSort as EventListener);
        cleanup.push(() => el.removeEventListener("x-table-cell-sort", onSort as EventListener));
      }
      if (onConnected) {
        el.addEventListener("x-table-cell-connected", onConnected as EventListener);
        cleanup.push(() => el.removeEventListener("x-table-cell-connected", onConnected as EventListener));
      }
      if (onDisconnected) {
        el.addEventListener("x-table-cell-disconnected", onDisconnected as EventListener);
        cleanup.push(() => el.removeEventListener("x-table-cell-disconnected", onDisconnected as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onSort, onConnected, onDisconnected]);

    return <x-table-cell ref={setRef} {...rest}>{children}</x-table-cell>;
  }
);
