"use client";
// x-divider.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XDivider as XDividerElement } from "@vanelsas/baredom/x-divider";
import { init } from "@vanelsas/baredom/x-divider";

init();

export interface XDividerProps {
  orientation?: string;
  variant?: string;
  align?: string;
  label?: string;
  thickness?: string;
  color?: string;
  inset?: string;
  length?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XDivider = forwardRef<XDividerElement, XDividerProps>(
  function XDivider(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XDividerElement>(null);

    const setRef = (el: XDividerElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-divider ref={setRef} {...rest}>{children}</x-divider>;
  }
);
