"use client";
// x-dropdown.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XDropdown as XDropdownElement } from "@vanelsas/baredom/x-dropdown";
import { init } from "@vanelsas/baredom/x-dropdown";

init();

export interface XDropdownProps {
  open?: boolean;
  disabled?: boolean;
  label?: string;
  placement?: string;
  onToggle?: (e: CustomEvent<{ open: boolean; source: string }>) => void;
  onChange?: (e: CustomEvent<{ open: boolean }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XDropdown = forwardRef<XDropdownElement, XDropdownProps>(
  function XDropdown(props, forwardedRef) {
    const { onToggle, onChange, children, ...rest } = props;
    const innerRef = useRef<XDropdownElement>(null);

    const setRef = (el: XDropdownElement | null) => {
      (innerRef as React.MutableRefObject<XDropdownElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onToggle) {
        el.addEventListener("x-dropdown-toggle", onToggle as EventListener);
        cleanup.push(() => el.removeEventListener("x-dropdown-toggle", onToggle as EventListener));
      }
      if (onChange) {
        el.addEventListener("x-dropdown-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-dropdown-change", onChange as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onToggle, onChange]);

    return <x-dropdown ref={setRef} {...rest}>{children}</x-dropdown>;
  }
);
