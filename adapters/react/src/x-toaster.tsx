"use client";
// x-toaster.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XToaster as XToasterElement } from "@vanelsas/baredom/x-toaster";
import { init } from "@vanelsas/baredom/x-toaster";

init();

export interface XToasterProps {
  position?: string;
  maxToasts?: number;
  label?: string;
  onDismiss?: (e: CustomEvent<{ type: string; reason: string; heading: string; message: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XToaster = forwardRef<XToasterElement, XToasterProps>(
  function XToaster(props, forwardedRef) {
    const { onDismiss, children, ...rest } = props;
    const innerRef = useRef<XToasterElement>(null);

    const setRef = (el: XToasterElement | null) => {
      (innerRef as React.MutableRefObject<XToasterElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onDismiss) {
        el.addEventListener("x-toaster-dismiss", onDismiss as EventListener);
        cleanup.push(() => el.removeEventListener("x-toaster-dismiss", onDismiss as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onDismiss]);

    return <x-toaster ref={setRef} {...rest}>{children}</x-toaster>;
  }
);
