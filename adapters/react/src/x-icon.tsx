"use client";
// x-icon.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XIcon as XIconElement } from "@vanelsas/baredom/x-icon";
import { init } from "@vanelsas/baredom/x-icon";

init();

export interface XIconProps {
  size?: string;
  color?: string;
  label?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XIcon = forwardRef<XIconElement, XIconProps>(
  function XIcon(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XIconElement>(null);

    const setRef = (el: XIconElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-icon ref={setRef} {...rest}>{children}</x-icon>;
  }
);
