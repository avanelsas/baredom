"use client";
// x-i18n-provider.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XI18nProvider as XI18nProviderElement } from "@vanelsas/baredom/x-i18n-provider";
import { init } from "@vanelsas/baredom/x-i18n-provider";

init();

export interface XI18nProviderProps {
  src?: string;
  locale?: string;
  fallbackLocale?: string;
  onXI18nLoading?: (e: CustomEvent<{ locale: string }>) => void;
  onXI18nChange?: (e: CustomEvent<{ locale: string }>) => void;
  onXI18nError?: (e: CustomEvent<{ locale: string; message: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XI18nProvider = forwardRef<XI18nProviderElement, XI18nProviderProps>(
  function XI18nProvider(props, forwardedRef) {
    const { onXI18nLoading, onXI18nChange, onXI18nError, children, ...rest } = props;
    const innerRef = useRef<XI18nProviderElement>(null);

    const setRef = (el: XI18nProviderElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onXI18nLoading) {
        el.addEventListener("x-i18n-loading", onXI18nLoading as EventListener);
        cleanup.push(() => el.removeEventListener("x-i18n-loading", onXI18nLoading as EventListener));
      }
      if (onXI18nChange) {
        el.addEventListener("x-i18n-change", onXI18nChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-i18n-change", onXI18nChange as EventListener));
      }
      if (onXI18nError) {
        el.addEventListener("x-i18n-error", onXI18nError as EventListener);
        cleanup.push(() => el.removeEventListener("x-i18n-error", onXI18nError as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onXI18nLoading, onXI18nChange, onXI18nError]);

    return <x-i18n-provider ref={setRef} {...rest}>{children}</x-i18n-provider>;
  }
);
