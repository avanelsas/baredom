"use client";
// x-morph-stack.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XMorphStack as XMorphStackElement } from "@vanelsas/baredom/x-morph-stack";
import { init } from "@vanelsas/baredom/x-morph-stack";

init();

export interface XMorphStackProps {
  activeState?: string;
  activeIndex?: number;
  stiffness?: number;
  damping?: number;
  mass?: number;
  variant?: string;
  duration?: number;
  disabled?: boolean;
  onChange?: (e: CustomEvent<{ from: string; to: string; reason: string }>) => void;
  onChanged?: (e: CustomEvent<{ from: string; to: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XMorphStack = forwardRef<XMorphStackElement, XMorphStackProps>(
  function XMorphStack(props, forwardedRef) {
    const { onChange, onChanged, children, ...rest } = props;
    const innerRef = useRef<XMorphStackElement>(null);

    const setRef = (el: XMorphStackElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onChange) {
        el.addEventListener("x-morph-stack-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-morph-stack-change", onChange as EventListener));
      }
      if (onChanged) {
        el.addEventListener("x-morph-stack-changed", onChanged as EventListener);
        cleanup.push(() => el.removeEventListener("x-morph-stack-changed", onChanged as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onChange, onChanged]);

    return <x-morph-stack ref={setRef} {...rest}>{children}</x-morph-stack>;
  }
);
