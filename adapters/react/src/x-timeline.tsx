"use client";
// x-timeline.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XTimeline as XTimelineElement } from "@vanelsas/baredom/x-timeline";
import { init } from "@vanelsas/baredom/x-timeline";

init();

export interface XTimelineProps {
  label?: string;
  position?: string;
  striped?: boolean;
  onSelect?: (e: CustomEvent<{ index: number; status: string; label: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XTimeline = forwardRef<XTimelineElement, XTimelineProps>(
  function XTimeline(props, forwardedRef) {
    const { onSelect, children, ...rest } = props;
    const innerRef = useRef<XTimelineElement>(null);

    const setRef = (el: XTimelineElement | null) => {
      (innerRef as React.MutableRefObject<XTimelineElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onSelect) {
        el.addEventListener("x-timeline-select", onSelect as EventListener);
        cleanup.push(() => el.removeEventListener("x-timeline-select", onSelect as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onSelect]);

    return <x-timeline ref={setRef} {...rest}>{children}</x-timeline>;
  }
);
