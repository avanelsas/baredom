"use client";
// x-container.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XContainer as XContainerElement } from "@vanelsas/baredom/x-container";
import { init } from "@vanelsas/baredom/x-container";

init();

export interface XContainerProps {
  center?: boolean;
  fluid?: boolean;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XContainer = forwardRef<XContainerElement, XContainerProps>(
  function XContainer(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XContainerElement>(null);

    const setRef = (el: XContainerElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-container ref={setRef} {...rest}>{children}</x-container>;
  }
);
