"use client";
// x-range-slider.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XRangeSlider as XRangeSliderElement } from "@vanelsas/baredom/x-range-slider";
import { init } from "@vanelsas/baredom/x-range-slider";

init();

export interface XRangeSliderProps {
  min?: string;
  disabled?: boolean;
  showValue?: boolean;
  name?: string;
  start?: string;
  minGap?: string;
  size?: string;
  readOnly?: boolean;
  max?: string;
  label?: string;
  end?: string;
  step?: string;
  onChangeRequest?: (e: CustomEvent<{ start: number; end: number; previousStart: number; previousEnd: number; min: number; max: number }>) => void;
  onInput?: (e: CustomEvent<{ start: number; end: number; min: number; max: number }>) => void;
  onChange?: (e: CustomEvent<{ start: number; end: number; min: number; max: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XRangeSlider = forwardRef<XRangeSliderElement, XRangeSliderProps>(
  function XRangeSlider(props, forwardedRef) {
    const { onChangeRequest, onInput, onChange, children, ...rest } = props;
    const innerRef = useRef<XRangeSliderElement>(null);

    const setRef = (el: XRangeSliderElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onChangeRequest) {
        el.addEventListener("x-range-slider-change-request", onChangeRequest as EventListener);
        cleanup.push(() => el.removeEventListener("x-range-slider-change-request", onChangeRequest as EventListener));
      }
      if (onInput) {
        el.addEventListener("x-range-slider-input", onInput as EventListener);
        cleanup.push(() => el.removeEventListener("x-range-slider-input", onInput as EventListener));
      }
      if (onChange) {
        el.addEventListener("x-range-slider-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-range-slider-change", onChange as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onChangeRequest, onInput, onChange]);

    return <x-range-slider ref={setRef} {...rest}>{children}</x-range-slider>;
  }
);
