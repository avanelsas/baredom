"use client";
// x-carousel.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XCarousel as XCarouselElement } from "@vanelsas/baredom/x-carousel";
import { init } from "@vanelsas/baredom/x-carousel";

init();

export interface XCarouselProps {
  arrows?: boolean;
  disabled?: boolean;
  transition?: string;
  loop?: boolean;
  interval?: number;
  autoplay?: boolean;
  peek?: string;
  dots?: boolean;
  currentSlide?: number;
  direction?: string;
  onChange?: (e: CustomEvent<{ index: number; previousIndex: number; reason: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XCarousel = forwardRef<XCarouselElement, XCarouselProps>(
  function XCarousel(props, forwardedRef) {
    const { onChange, children, ...rest } = props;
    const innerRef = useRef<XCarouselElement>(null);

    const setRef = (el: XCarouselElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onChange) {
        el.addEventListener("x-carousel-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-carousel-change", onChange as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onChange]);

    return <x-carousel ref={setRef} {...rest}>{children}</x-carousel>;
  }
);
