"use client";
// x-soft-body.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XSoftBody as XSoftBodyElement } from "@vanelsas/baredom/x-soft-body";
import { init } from "@vanelsas/baredom/x-soft-body";

init();

export interface XSoftBodyProps {
  stiffness?: number;
  damping?: number;
  radius?: number;
  intensity?: number;
  grabRadius?: number;
  disabled?: boolean;
  onGrab?: (e: CustomEvent<{}>) => void;
  onRelease?: (e: CustomEvent<{}>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XSoftBody = forwardRef<XSoftBodyElement, XSoftBodyProps>(
  function XSoftBody(props, forwardedRef) {
    const { onGrab, onRelease, children, ...rest } = props;
    const innerRef = useRef<XSoftBodyElement>(null);

    const setRef = (el: XSoftBodyElement | null) => {
      (innerRef as React.MutableRefObject<XSoftBodyElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onGrab) {
        el.addEventListener("x-soft-body-grab", onGrab as EventListener);
        cleanup.push(() => el.removeEventListener("x-soft-body-grab", onGrab as EventListener));
      }
      if (onRelease) {
        el.addEventListener("x-soft-body-release", onRelease as EventListener);
        cleanup.push(() => el.removeEventListener("x-soft-body-release", onRelease as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onGrab, onRelease]);

    return <x-soft-body ref={setRef} {...rest}>{children}</x-soft-body>;
  }
);
