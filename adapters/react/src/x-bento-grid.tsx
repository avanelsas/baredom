"use client";
// x-bento-grid.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XBentoGrid as XBentoGridElement } from "@vanelsas/baredom/x-bento-grid";
import { init } from "@vanelsas/baredom/x-bento-grid";

init();

export interface XBentoGridProps {
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XBentoGrid = forwardRef<XBentoGridElement, XBentoGridProps>(
  function XBentoGrid(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XBentoGridElement>(null);

    const setRef = (el: XBentoGridElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-bento-grid ref={setRef} {...rest}>{children}</x-bento-grid>;
  }
);
