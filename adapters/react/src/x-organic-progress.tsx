"use client";
// x-organic-progress.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XOrganicProgress as XOrganicProgressElement } from "@vanelsas/baredom/x-organic-progress";
import { init } from "@vanelsas/baredom/x-organic-progress";

init();

export interface XOrganicProgressProps {
  progress?: string;
  variant?: string;
  color?: string;
  bloom?: boolean;
  density?: string;
  seed?: number;
  label?: string;
  onComplete?: (e: CustomEvent<{ progress: number }>) => void;
  onBloomEnd?: (e: CustomEvent<{ progress: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XOrganicProgress = forwardRef<XOrganicProgressElement, XOrganicProgressProps>(
  function XOrganicProgress(props, forwardedRef) {
    const { onComplete, onBloomEnd, children, ...rest } = props;
    const innerRef = useRef<XOrganicProgressElement>(null);

    const setRef = (el: XOrganicProgressElement | null) => {
      (innerRef as React.MutableRefObject<XOrganicProgressElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onComplete) {
        el.addEventListener("x-organic-progress-complete", onComplete as EventListener);
        cleanup.push(() => el.removeEventListener("x-organic-progress-complete", onComplete as EventListener));
      }
      if (onBloomEnd) {
        el.addEventListener("x-organic-progress-bloom-end", onBloomEnd as EventListener);
        cleanup.push(() => el.removeEventListener("x-organic-progress-bloom-end", onBloomEnd as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onComplete, onBloomEnd]);

    return <x-organic-progress ref={setRef} {...rest}>{children}</x-organic-progress>;
  }
);
