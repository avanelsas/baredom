"use client";
// x-chip.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XChip as XChipElement } from "@vanelsas/baredom/x-chip";
import { init } from "@vanelsas/baredom/x-chip";

init();

export interface XChipProps {
  label?: string;
  value?: string;
  removable?: boolean;
  disabled?: boolean;
  onRemove?: (e: CustomEvent<{ value: string; label: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XChip = forwardRef<XChipElement, XChipProps>(
  function XChip(props, forwardedRef) {
    const { onRemove, children, ...rest } = props;
    const innerRef = useRef<XChipElement>(null);

    const setRef = (el: XChipElement | null) => {
      (innerRef as React.MutableRefObject<XChipElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onRemove) {
        el.addEventListener("x-chip-remove", onRemove as EventListener);
        cleanup.push(() => el.removeEventListener("x-chip-remove", onRemove as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onRemove]);

    return <x-chip ref={setRef} {...rest}>{children}</x-chip>;
  }
);
