"use client";
// x-ripple-effect.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XRippleEffect as XRippleEffectElement } from "@vanelsas/baredom/x-ripple-effect";
import { init } from "@vanelsas/baredom/x-ripple-effect";

init();

export interface XRippleEffectProps {
  intensity?: number;
  duration?: number;
  frequency?: number;
  disabled?: boolean;
  onStart?: (e: CustomEvent<{ x: number; y: number }>) => void;
  onEnd?: (e: CustomEvent<{ x: number; y: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XRippleEffect = forwardRef<XRippleEffectElement, XRippleEffectProps>(
  function XRippleEffect(props, forwardedRef) {
    const { onStart, onEnd, children, ...rest } = props;
    const innerRef = useRef<XRippleEffectElement>(null);

    const setRef = (el: XRippleEffectElement | null) => {
      (innerRef as React.MutableRefObject<XRippleEffectElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onStart) {
        el.addEventListener("x-ripple-effect-start", onStart as EventListener);
        cleanup.push(() => el.removeEventListener("x-ripple-effect-start", onStart as EventListener));
      }
      if (onEnd) {
        el.addEventListener("x-ripple-effect-end", onEnd as EventListener);
        cleanup.push(() => el.removeEventListener("x-ripple-effect-end", onEnd as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onStart, onEnd]);

    return <x-ripple-effect ref={setRef} {...rest}>{children}</x-ripple-effect>;
  }
);
