"use client";
// x-color-picker.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XColorPicker as XColorPickerElement } from "@vanelsas/baredom/x-color-picker";
import { init } from "@vanelsas/baredom/x-color-picker";

init();

export interface XColorPickerProps {
  open?: boolean;
  disabled?: boolean;
  name?: string;
  value?: string;
  mode?: string;
  swatches?: string;
  readOnly?: boolean;
  label?: string;
  alpha?: boolean;
  onInput?: (e: CustomEvent<{ value: string; h: number; s: number; l: number; a: number }>) => void;
  onChange?: (e: CustomEvent<{ value: string; h: number; s: number; l: number; a: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XColorPicker = forwardRef<XColorPickerElement, XColorPickerProps>(
  function XColorPicker(props, forwardedRef) {
    const { onInput, onChange, children, ...rest } = props;
    const innerRef = useRef<XColorPickerElement>(null);

    const setRef = (el: XColorPickerElement | null) => {
      (innerRef as React.MutableRefObject<XColorPickerElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onInput) {
        el.addEventListener("x-color-picker-input", onInput as EventListener);
        cleanup.push(() => el.removeEventListener("x-color-picker-input", onInput as EventListener));
      }
      if (onChange) {
        el.addEventListener("x-color-picker-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-color-picker-change", onChange as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onInput, onChange]);

    return <x-color-picker ref={setRef} {...rest}>{children}</x-color-picker>;
  }
);
