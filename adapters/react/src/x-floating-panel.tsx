"use client";
// x-floating-panel.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XFloatingPanel as XFloatingPanelElement } from "@vanelsas/baredom/x-floating-panel";
import { init } from "@vanelsas/baredom/x-floating-panel";

init();

export interface XFloatingPanelProps {
  open?: boolean;
  x?: number;
  y?: number;
  step?: number;
  closable?: boolean;
  resizable?: boolean;
  label?: string;
  focusOnOpen?: boolean;
  onToggle?: (e: CustomEvent<{ open: boolean }>) => void;
  onDismissRequest?: (e: CustomEvent<{ reason: string }>) => void;
  onMove?: (e: CustomEvent<{ x: number; y: number; source: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XFloatingPanel = forwardRef<XFloatingPanelElement, XFloatingPanelProps>(
  function XFloatingPanel(props, forwardedRef) {
    const { onToggle, onDismissRequest, onMove, children, ...rest } = props;
    const innerRef = useRef<XFloatingPanelElement>(null);

    const setRef = (el: XFloatingPanelElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onToggle) {
        el.addEventListener("x-floating-panel-toggle", onToggle as EventListener);
        cleanup.push(() => el.removeEventListener("x-floating-panel-toggle", onToggle as EventListener));
      }
      if (onDismissRequest) {
        el.addEventListener("x-floating-panel-dismiss-request", onDismissRequest as EventListener);
        cleanup.push(() => el.removeEventListener("x-floating-panel-dismiss-request", onDismissRequest as EventListener));
      }
      if (onMove) {
        el.addEventListener("x-floating-panel-move", onMove as EventListener);
        cleanup.push(() => el.removeEventListener("x-floating-panel-move", onMove as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onToggle, onDismissRequest, onMove]);

    return <x-floating-panel ref={setRef} {...rest}>{children}</x-floating-panel>;
  }
);
