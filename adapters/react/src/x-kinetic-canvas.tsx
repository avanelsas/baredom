"use client";
// x-kinetic-canvas.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XKineticCanvas as XKineticCanvasElement } from "@vanelsas/baredom/x-kinetic-canvas";
import { init } from "@vanelsas/baredom/x-kinetic-canvas";

init();

export interface XKineticCanvasProps {
  type?: string;
  variant?: string;
  speed?: string;
  density?: string;
  fullscreen?: boolean;
  paused?: boolean;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XKineticCanvas = forwardRef<XKineticCanvasElement, XKineticCanvasProps>(
  function XKineticCanvas(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XKineticCanvasElement>(null);

    const setRef = (el: XKineticCanvasElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-kinetic-canvas ref={setRef} {...rest}>{children}</x-kinetic-canvas>;
  }
);
