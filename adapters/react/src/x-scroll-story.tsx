"use client";
// x-scroll-story.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XScrollStory as XScrollStoryElement } from "@vanelsas/baredom/x-scroll-story";
import { init } from "@vanelsas/baredom/x-scroll-story";

init();

export interface XScrollStoryProps {
  autoplayLoop?: boolean;
  autoplaySpeed?: number;
  autoplayIndicator?: boolean;
  disabled?: boolean;
  layout?: string;
  threshold?: number;
  split?: number;
  label?: string;
  autoplay?: boolean;
  onStepChange?: (e: CustomEvent<{ index: number; id: string; previousIndex: number; previousId: string }>) => void;
  onStepEnter?: (e: CustomEvent<{ index: number; id: string; progress: number }>) => void;
  onStepLeave?: (e: CustomEvent<{ index: number; id: string; progress: number }>) => void;
  onProgress?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
  onEnter?: (e: CustomEvent<{ progress: number }>) => void;
  onLeave?: (e: CustomEvent<{ progress: number }>) => void;
  onAutoplayPause?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
  onAutoplayResume?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XScrollStory = forwardRef<XScrollStoryElement, XScrollStoryProps>(
  function XScrollStory(props, forwardedRef) {
    const { onStepChange, onStepEnter, onStepLeave, onProgress, onEnter, onLeave, onAutoplayPause, onAutoplayResume, children, ...rest } = props;
    const innerRef = useRef<XScrollStoryElement>(null);

    const setRef = (el: XScrollStoryElement | null) => {
      (innerRef as React.MutableRefObject<XScrollStoryElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onStepChange) {
        el.addEventListener("x-scroll-story-step-change", onStepChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-story-step-change", onStepChange as EventListener));
      }
      if (onStepEnter) {
        el.addEventListener("x-scroll-story-step-enter", onStepEnter as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-story-step-enter", onStepEnter as EventListener));
      }
      if (onStepLeave) {
        el.addEventListener("x-scroll-story-step-leave", onStepLeave as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-story-step-leave", onStepLeave as EventListener));
      }
      if (onProgress) {
        el.addEventListener("x-scroll-story-progress", onProgress as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-story-progress", onProgress as EventListener));
      }
      if (onEnter) {
        el.addEventListener("x-scroll-story-enter", onEnter as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-story-enter", onEnter as EventListener));
      }
      if (onLeave) {
        el.addEventListener("x-scroll-story-leave", onLeave as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-story-leave", onLeave as EventListener));
      }
      if (onAutoplayPause) {
        el.addEventListener("x-scroll-story-autoplay-pause", onAutoplayPause as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-story-autoplay-pause", onAutoplayPause as EventListener));
      }
      if (onAutoplayResume) {
        el.addEventListener("x-scroll-story-autoplay-resume", onAutoplayResume as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-story-autoplay-resume", onAutoplayResume as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onStepChange, onStepEnter, onStepLeave, onProgress, onEnter, onLeave, onAutoplayPause, onAutoplayResume]);

    return <x-scroll-story ref={setRef} {...rest}>{children}</x-scroll-story>;
  }
);
