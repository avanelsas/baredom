"use client";
// x-spinner.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XSpinner as XSpinnerElement } from "@vanelsas/baredom/x-spinner";
import { init } from "@vanelsas/baredom/x-spinner";

init();

export interface XSpinnerProps {
  size?: string;
  variant?: string;
  label?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XSpinner = forwardRef<XSpinnerElement, XSpinnerProps>(
  function XSpinner(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XSpinnerElement>(null);

    const setRef = (el: XSpinnerElement | null) => {
      (innerRef as React.MutableRefObject<XSpinnerElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-spinner ref={setRef} {...rest}>{children}</x-spinner>;
  }
);
