"use client";
// x-slider.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XSlider as XSliderElement } from "@vanelsas/baredom/x-slider";
import { init } from "@vanelsas/baredom/x-slider";

init();

export interface XSliderProps {
  min?: string;
  disabled?: boolean;
  showValue?: boolean;
  name?: string;
  value?: string;
  size?: string;
  readOnly?: boolean;
  max?: string;
  label?: string;
  step?: string;
  onInput?: (e: CustomEvent<{ value: number; min: number; max: number }>) => void;
  onChange?: (e: CustomEvent<{ value: number; min: number; max: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XSlider = forwardRef<XSliderElement, XSliderProps>(
  function XSlider(props, forwardedRef) {
    const { onInput, onChange, children, ...rest } = props;
    const innerRef = useRef<XSliderElement>(null);

    const setRef = (el: XSliderElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onInput) {
        el.addEventListener("x-slider-input", onInput as EventListener);
        cleanup.push(() => el.removeEventListener("x-slider-input", onInput as EventListener));
      }
      if (onChange) {
        el.addEventListener("x-slider-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-slider-change", onChange as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onInput, onChange]);

    return <x-slider ref={setRef} {...rest}>{children}</x-slider>;
  }
);
