"use client";
// x-organic-divider.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XOrganicDivider as XOrganicDividerElement } from "@vanelsas/baredom/x-organic-divider";
import { init } from "@vanelsas/baredom/x-organic-divider";

init();

export interface XOrganicDividerProps {
  shape?: string;
  layers?: string;
  height?: string;
  flip?: boolean;
  mirror?: boolean;
  animation?: string;
  path?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XOrganicDivider = forwardRef<XOrganicDividerElement, XOrganicDividerProps>(
  function XOrganicDivider(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XOrganicDividerElement>(null);

    const setRef = (el: XOrganicDividerElement | null) => {
      (innerRef as React.MutableRefObject<XOrganicDividerElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-organic-divider ref={setRef} {...rest}>{children}</x-organic-divider>;
  }
);
