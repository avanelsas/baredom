"use client";
// x-skeleton.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XSkeleton as XSkeletonElement } from "@vanelsas/baredom/x-skeleton";
import { init } from "@vanelsas/baredom/x-skeleton";

init();

export interface XSkeletonProps {
  variant?: string;
  animation?: string;
  width?: string;
  height?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XSkeleton = forwardRef<XSkeletonElement, XSkeletonProps>(
  function XSkeleton(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XSkeletonElement>(null);

    const setRef = (el: XSkeletonElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-skeleton ref={setRef} {...rest}>{children}</x-skeleton>;
  }
);
