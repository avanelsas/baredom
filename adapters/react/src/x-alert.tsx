"use client";
// x-alert.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XAlert as XAlertElement } from "@vanelsas/baredom/x-alert";
import { init } from "@vanelsas/baredom/x-alert";

init();

export interface XAlertProps {
  type?: string;
  text?: string;
  icon?: string;
  dismissible?: boolean;
  disabled?: boolean;
  timeoutMs?: number;
  onDismiss?: (e: CustomEvent<{ type: string; reason: string; text: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XAlert = forwardRef<XAlertElement, XAlertProps>(
  function XAlert(props, forwardedRef) {
    const { onDismiss, children, ...rest } = props;
    const innerRef = useRef<XAlertElement>(null);

    const setRef = (el: XAlertElement | null) => {
      (innerRef as React.MutableRefObject<XAlertElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onDismiss) {
        el.addEventListener("x-alert-dismiss", onDismiss as EventListener);
        cleanup.push(() => el.removeEventListener("x-alert-dismiss", onDismiss as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onDismiss]);

    return <x-alert ref={setRef} {...rest}>{children}</x-alert>;
  }
);
