"use client";
// x-liquid-dock.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XLiquidDock as XLiquidDockElement } from "@vanelsas/baredom/x-liquid-dock";
import { init } from "@vanelsas/baredom/x-liquid-dock";

init();

export interface XLiquidDockProps {
  rippleScale?: number;
  color?: string;
  magnetStrength?: number;
  disabled?: boolean;
  threshold?: string;
  magnetRadius?: number;
  bobIntensity?: number;
  blur?: number;
  rippleSpeed?: number;
  gap?: number;
  position?: string;
  onSelect?: (e: CustomEvent<{ index: number; item: any; source: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XLiquidDock = forwardRef<XLiquidDockElement, XLiquidDockProps>(
  function XLiquidDock(props, forwardedRef) {
    const { onSelect, children, ...rest } = props;
    const innerRef = useRef<XLiquidDockElement>(null);

    const setRef = (el: XLiquidDockElement | null) => {
      (innerRef as React.MutableRefObject<XLiquidDockElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onSelect) {
        el.addEventListener("x-liquid-dock-select", onSelect as EventListener);
        cleanup.push(() => el.removeEventListener("x-liquid-dock-select", onSelect as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onSelect]);

    return <x-liquid-dock ref={setRef} {...rest}>{children}</x-liquid-dock>;
  }
);
