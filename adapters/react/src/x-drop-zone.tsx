"use client";
// x-drop-zone.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XDropZone as XDropZoneElement } from "@vanelsas/baredom/x-drop-zone";
import { init } from "@vanelsas/baredom/x-drop-zone";

init();

export interface XDropZoneProps {
  value?: string;
  accepts?: string;
  max?: number;
  label?: string;
  disabled?: boolean;
  pending?: boolean;
  pendingIndex?: number;
  animateMoves?: boolean;
  onEnter?: (e: CustomEvent<{ kind: string; value: string }>) => void;
  onLeave?: (e: CustomEvent<{ kind: string; value: string }>) => void;
  onDrop?: (e: CustomEvent<{ kind: string; value: string; from: string | null; to: string; index: number; panel: HTMLElement }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XDropZone = forwardRef<XDropZoneElement, XDropZoneProps>(
  function XDropZone(props, forwardedRef) {
    const { onEnter, onLeave, onDrop, children, ...rest } = props;
    const innerRef = useRef<XDropZoneElement>(null);

    const setRef = (el: XDropZoneElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onEnter) {
        el.addEventListener("x-drop-zone-enter", onEnter as EventListener);
        cleanup.push(() => el.removeEventListener("x-drop-zone-enter", onEnter as EventListener));
      }
      if (onLeave) {
        el.addEventListener("x-drop-zone-leave", onLeave as EventListener);
        cleanup.push(() => el.removeEventListener("x-drop-zone-leave", onLeave as EventListener));
      }
      if (onDrop) {
        el.addEventListener("x-drop-zone-drop", onDrop as EventListener);
        cleanup.push(() => el.removeEventListener("x-drop-zone-drop", onDrop as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onEnter, onLeave, onDrop]);

    return <x-drop-zone ref={setRef} {...rest}>{children}</x-drop-zone>;
  }
);
