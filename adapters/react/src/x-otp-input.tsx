"use client";
// x-otp-input.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XOtpInput as XOtpInputElement } from "@vanelsas/baredom/x-otp-input";
import { init } from "@vanelsas/baredom/x-otp-input";

init();

export interface XOtpInputProps {
  mask?: boolean;
  placeholder?: string;
  disabled?: boolean;
  name?: string;
  value?: string;
  readonly?: boolean;
  type?: string;
  label?: string;
  autofocus?: boolean;
  length?: number;
  error?: string;
  required?: boolean;
  onInput?: (e: CustomEvent<{ name: string; value: string; complete: boolean }>) => void;
  onChange?: (e: CustomEvent<{ name: string; value: string; complete: boolean }>) => void;
  onComplete?: (e: CustomEvent<{ name: string; value: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XOtpInput = forwardRef<XOtpInputElement, XOtpInputProps>(
  function XOtpInput(props, forwardedRef) {
    const { onInput, onChange, onComplete, children, ...rest } = props;
    const innerRef = useRef<XOtpInputElement>(null);

    const setRef = (el: XOtpInputElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onInput) {
        el.addEventListener("x-otp-input-input", onInput as EventListener);
        cleanup.push(() => el.removeEventListener("x-otp-input-input", onInput as EventListener));
      }
      if (onChange) {
        el.addEventListener("x-otp-input-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-otp-input-change", onChange as EventListener));
      }
      if (onComplete) {
        el.addEventListener("x-otp-input-complete", onComplete as EventListener);
        cleanup.push(() => el.removeEventListener("x-otp-input-complete", onComplete as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onInput, onChange, onComplete]);

    return <x-otp-input ref={setRef} {...rest}>{children}</x-otp-input>;
  }
);
