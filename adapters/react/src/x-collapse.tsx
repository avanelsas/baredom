"use client";
// x-collapse.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XCollapse as XCollapseElement } from "@vanelsas/baredom/x-collapse";
import { init } from "@vanelsas/baredom/x-collapse";

init();

export interface XCollapseProps {
  open?: boolean;
  disabled?: boolean;
  header?: string;
  durationMs?: number;
  onToggle?: (e: CustomEvent<{ open: boolean; source: string }>) => void;
  onChange?: (e: CustomEvent<{ open: boolean }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XCollapse = forwardRef<XCollapseElement, XCollapseProps>(
  function XCollapse(props, forwardedRef) {
    const { onToggle, onChange, children, ...rest } = props;
    const innerRef = useRef<XCollapseElement>(null);

    const setRef = (el: XCollapseElement | null) => {
      (innerRef as React.MutableRefObject<XCollapseElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onToggle) {
        el.addEventListener("x-collapse-toggle", onToggle as EventListener);
        cleanup.push(() => el.removeEventListener("x-collapse-toggle", onToggle as EventListener));
      }
      if (onChange) {
        el.addEventListener("x-collapse-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-collapse-change", onChange as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onToggle, onChange]);

    return <x-collapse ref={setRef} {...rest}>{children}</x-collapse>;
  }
);
