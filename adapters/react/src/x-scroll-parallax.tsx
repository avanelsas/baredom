"use client";
// x-scroll-parallax.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XScrollParallax as XScrollParallaxElement } from "@vanelsas/baredom/x-scroll-parallax";
import { init } from "@vanelsas/baredom/x-scroll-parallax";

init();

export interface XScrollParallaxProps {
  direction?: string;
  source?: string;
  easing?: string;
  disabled?: boolean;
  label?: string;
  onEnter?: (e: CustomEvent<{ progress: number }>) => void;
  onLeave?: (e: CustomEvent<{ progress: number }>) => void;
  onProgress?: (e: CustomEvent<{ progress: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XScrollParallax = forwardRef<XScrollParallaxElement, XScrollParallaxProps>(
  function XScrollParallax(props, forwardedRef) {
    const { onEnter, onLeave, onProgress, children, ...rest } = props;
    const innerRef = useRef<XScrollParallaxElement>(null);

    const setRef = (el: XScrollParallaxElement | null) => {
      (innerRef as React.MutableRefObject<XScrollParallaxElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onEnter) {
        el.addEventListener("x-scroll-parallax-enter", onEnter as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-parallax-enter", onEnter as EventListener));
      }
      if (onLeave) {
        el.addEventListener("x-scroll-parallax-leave", onLeave as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-parallax-leave", onLeave as EventListener));
      }
      if (onProgress) {
        el.addEventListener("x-scroll-parallax-progress", onProgress as EventListener);
        cleanup.push(() => el.removeEventListener("x-scroll-parallax-progress", onProgress as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onEnter, onLeave, onProgress]);

    return <x-scroll-parallax ref={setRef} {...rest}>{children}</x-scroll-parallax>;
  }
);
