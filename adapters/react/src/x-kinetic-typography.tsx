"use client";
// x-kinetic-typography.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XKineticTypography as XKineticTypographyElement } from "@vanelsas/baredom/x-kinetic-typography";
import { init } from "@vanelsas/baredom/x-kinetic-typography";

init();

export interface XKineticTypographyProps {
  path?: string;
  startSize?: string;
  animation?: string;
  speed?: string;
  echoCount?: string;
  echoScale?: string;
  echoOpacity?: string;
  preset?: string;
  repeat?: string;
  fontSize?: string;
  effect?: string;
  echoDelay?: string;
  endSize?: string;
  direction?: string;
  text?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XKineticTypography = forwardRef<XKineticTypographyElement, XKineticTypographyProps>(
  function XKineticTypography(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XKineticTypographyElement>(null);

    const setRef = (el: XKineticTypographyElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-kinetic-typography ref={setRef} {...rest}>{children}</x-kinetic-typography>;
  }
);
