"use client";
// x-stepper.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XStepper as XStepperElement } from "@vanelsas/baredom/x-stepper";
import { init } from "@vanelsas/baredom/x-stepper";

init();

export interface XStepperProps {
  steps?: string;
  current?: number;
  orientation?: string;
  size?: string;
  disabled?: boolean;
  onChange?: (e: CustomEvent<{ from: number; to: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XStepper = forwardRef<XStepperElement, XStepperProps>(
  function XStepper(props, forwardedRef) {
    const { onChange, children, ...rest } = props;
    const innerRef = useRef<XStepperElement>(null);

    const setRef = (el: XStepperElement | null) => {
      (innerRef as React.MutableRefObject<XStepperElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onChange) {
        el.addEventListener("x-stepper-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-stepper-change", onChange as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onChange]);

    return <x-stepper ref={setRef} {...rest}>{children}</x-stepper>;
  }
);
