"use client";
// x-gaussian-blur.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XGaussianBlur as XGaussianBlurElement } from "@vanelsas/baredom/x-gaussian-blur";
import { init } from "@vanelsas/baredom/x-gaussian-blur";

init();

export interface XGaussianBlurProps {
  paused?: boolean;
  animation?: string;
  speed?: string;
  colors?: string;
  size?: string;
  blur?: string;
  opacity?: string;
  count?: string;
  blend?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XGaussianBlur = forwardRef<XGaussianBlurElement, XGaussianBlurProps>(
  function XGaussianBlur(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XGaussianBlurElement>(null);

    const setRef = (el: XGaussianBlurElement | null) => {
      (innerRef as React.MutableRefObject<XGaussianBlurElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-gaussian-blur ref={setRef} {...rest}>{children}</x-gaussian-blur>;
  }
);
