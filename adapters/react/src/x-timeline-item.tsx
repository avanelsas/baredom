"use client";
// x-timeline-item.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XTimelineItem as XTimelineItemElement } from "@vanelsas/baredom/x-timeline-item";
import { init } from "@vanelsas/baredom/x-timeline-item";

init();

export interface XTimelineItemProps {
  label?: string;
  title?: string;
  status?: string;
  icon?: string;
  connector?: string;
  position?: string;
  disabled?: boolean;
  onConnected?: (e: CustomEvent<{ status: string; label: string; position: string; disabled: boolean }>) => void;
  onDisconnected?: (e: CustomEvent<{ status: string; label: string }>) => void;
  onClick?: (e: CustomEvent<{ status: string; label: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XTimelineItem = forwardRef<XTimelineItemElement, XTimelineItemProps>(
  function XTimelineItem(props, forwardedRef) {
    const { onConnected, onDisconnected, onClick, children, ...rest } = props;
    const innerRef = useRef<XTimelineItemElement>(null);

    const setRef = (el: XTimelineItemElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onConnected) {
        el.addEventListener("x-timeline-item-connected", onConnected as EventListener);
        cleanup.push(() => el.removeEventListener("x-timeline-item-connected", onConnected as EventListener));
      }
      if (onDisconnected) {
        el.addEventListener("x-timeline-item-disconnected", onDisconnected as EventListener);
        cleanup.push(() => el.removeEventListener("x-timeline-item-disconnected", onDisconnected as EventListener));
      }
      if (onClick) {
        el.addEventListener("x-timeline-item-click", onClick as EventListener);
        cleanup.push(() => el.removeEventListener("x-timeline-item-click", onClick as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onConnected, onDisconnected, onClick]);

    return <x-timeline-item ref={setRef} {...rest}>{children}</x-timeline-item>;
  }
);
