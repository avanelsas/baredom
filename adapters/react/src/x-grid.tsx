"use client";
// x-grid.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XGrid as XGridElement } from "@vanelsas/baredom/x-grid";
import { init } from "@vanelsas/baredom/x-grid";

init();

export interface XGridProps {
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XGrid = forwardRef<XGridElement, XGridProps>(
  function XGrid(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XGridElement>(null);

    const setRef = (el: XGridElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-grid ref={setRef} {...rest}>{children}</x-grid>;
  }
);
