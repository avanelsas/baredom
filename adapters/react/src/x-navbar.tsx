"use client";
// x-navbar.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XNavbar as XNavbarElement } from "@vanelsas/baredom/x-navbar";
import { init } from "@vanelsas/baredom/x-navbar";

init();

export interface XNavbarProps {
  sticky?: boolean;
  elevated?: boolean;
  label?: string;
  variant?: string;
  orientation?: string;
  alignment?: string;
  breakpoint?: string;
  onFocusVisible?: (e: CustomEvent<{}>) => void;
  onNavigate?: (e: CustomEvent<{ href: string; source: string }>) => void;
  onBrandActivate?: (e: CustomEvent<{ source: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XNavbar = forwardRef<XNavbarElement, XNavbarProps>(
  function XNavbar(props, forwardedRef) {
    const { onFocusVisible, onNavigate, onBrandActivate, children, ...rest } = props;
    const innerRef = useRef<XNavbarElement>(null);

    const setRef = (el: XNavbarElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onFocusVisible) {
        el.addEventListener("focus-visible", onFocusVisible as EventListener);
        cleanup.push(() => el.removeEventListener("focus-visible", onFocusVisible as EventListener));
      }
      if (onNavigate) {
        el.addEventListener("navigate", onNavigate as EventListener);
        cleanup.push(() => el.removeEventListener("navigate", onNavigate as EventListener));
      }
      if (onBrandActivate) {
        el.addEventListener("brand-activate", onBrandActivate as EventListener);
        cleanup.push(() => el.removeEventListener("brand-activate", onBrandActivate as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onFocusVisible, onNavigate, onBrandActivate]);

    return <x-navbar ref={setRef} {...rest}>{children}</x-navbar>;
  }
);
