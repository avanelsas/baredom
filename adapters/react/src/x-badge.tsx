"use client";
// x-badge.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XBadge as XBadgeElement } from "@vanelsas/baredom/x-badge";
import { init } from "@vanelsas/baredom/x-badge";

init();

export interface XBadgeProps {
  variant?: string;
  size?: string;
  pill?: boolean;
  dot?: boolean;
  count?: number;
  max?: number;
  text?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XBadge = forwardRef<XBadgeElement, XBadgeProps>(
  function XBadge(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XBadgeElement>(null);

    const setRef = (el: XBadgeElement | null) => {
      (innerRef as React.MutableRefObject<XBadgeElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-badge ref={setRef} {...rest}>{children}</x-badge>;
  }
);
