"use client";
// x-table-row.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XTableRow as XTableRowElement } from "@vanelsas/baredom/x-table-row";
import { init } from "@vanelsas/baredom/x-table-row";

init();

export interface XTableRowProps {
  selected?: boolean;
  disabled?: boolean;
  interactive?: boolean;
  rowIndex?: number;
  onClick?: (e: CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean }>) => void;
  onConnected?: (e: CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean; interactive: boolean }>) => void;
  onDisconnected?: (e: CustomEvent<{}>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XTableRow = forwardRef<XTableRowElement, XTableRowProps>(
  function XTableRow(props, forwardedRef) {
    const { onClick, onConnected, onDisconnected, children, ...rest } = props;
    const innerRef = useRef<XTableRowElement>(null);

    const setRef = (el: XTableRowElement | null) => {
      (innerRef as React.MutableRefObject<XTableRowElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onClick) {
        el.addEventListener("x-table-row-click", onClick as EventListener);
        cleanup.push(() => el.removeEventListener("x-table-row-click", onClick as EventListener));
      }
      if (onConnected) {
        el.addEventListener("x-table-row-connected", onConnected as EventListener);
        cleanup.push(() => el.removeEventListener("x-table-row-connected", onConnected as EventListener));
      }
      if (onDisconnected) {
        el.addEventListener("x-table-row-disconnected", onDisconnected as EventListener);
        cleanup.push(() => el.removeEventListener("x-table-row-disconnected", onDisconnected as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onClick, onConnected, onDisconnected]);

    return <x-table-row ref={setRef} {...rest}>{children}</x-table-row>;
  }
);
