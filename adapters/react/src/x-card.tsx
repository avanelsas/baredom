"use client";
// x-card.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XCard as XCardElement } from "@vanelsas/baredom/x-card";
import { init } from "@vanelsas/baredom/x-card";

init();

export interface XCardProps {
  interactive?: boolean;
  disabled?: boolean;
  onPress?: (e: CustomEvent<{}>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XCard = forwardRef<XCardElement, XCardProps>(
  function XCard(props, forwardedRef) {
    const { onPress, children, ...rest } = props;
    const innerRef = useRef<XCardElement>(null);

    const setRef = (el: XCardElement | null) => {
      (innerRef as React.MutableRefObject<XCardElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onPress) {
        el.addEventListener("press", onPress as EventListener);
        cleanup.push(() => el.removeEventListener("press", onPress as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onPress]);

    return <x-card ref={setRef} {...rest}>{children}</x-card>;
  }
);
