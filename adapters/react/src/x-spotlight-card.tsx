"use client";
// x-spotlight-card.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XSpotlightCard as XSpotlightCardElement } from "@vanelsas/baredom/x-spotlight-card";
import { init } from "@vanelsas/baredom/x-spotlight-card";

init();

export interface XSpotlightCardProps {
  variant?: string;
  radius?: string;
  padding?: string;
  color?: string;
  intensity?: string;
  size?: string;
  static?: boolean;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XSpotlightCard = forwardRef<XSpotlightCardElement, XSpotlightCardProps>(
  function XSpotlightCard(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XSpotlightCardElement>(null);

    const setRef = (el: XSpotlightCardElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-spotlight-card ref={setRef} {...rest}>{children}</x-spotlight-card>;
  }
);
