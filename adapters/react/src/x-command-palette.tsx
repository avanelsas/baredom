"use client";
// x-command-palette.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XCommandPalette as XCommandPaletteElement } from "@vanelsas/baredom/x-command-palette";
import { init } from "@vanelsas/baredom/x-command-palette";

init();

export interface XCommandPaletteProps {
  items?: any;
  open?: boolean;
  disabled?: boolean;
  onOpenRequest?: (e: CustomEvent<{}>) => void;
  onOpen?: (e: CustomEvent<{}>) => void;
  onCloseRequest?: (e: CustomEvent<{}>) => void;
  onClose?: (e: CustomEvent<{}>) => void;
  onSelectRequest?: (e: CustomEvent<{ item: Record<string, any> }>) => void;
  onSelect?: (e: CustomEvent<{ item: Record<string, any> }>) => void;
  onQueryChange?: (e: CustomEvent<{ query: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XCommandPalette = forwardRef<XCommandPaletteElement, XCommandPaletteProps>(
  function XCommandPalette(props, forwardedRef) {
    const { onOpenRequest, onOpen, onCloseRequest, onClose, onSelectRequest, onSelect, onQueryChange, children, ...rest } = props;
    const innerRef = useRef<XCommandPaletteElement>(null);

    const setRef = (el: XCommandPaletteElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onOpenRequest) {
        el.addEventListener("x-command-palette-open-request", onOpenRequest as EventListener);
        cleanup.push(() => el.removeEventListener("x-command-palette-open-request", onOpenRequest as EventListener));
      }
      if (onOpen) {
        el.addEventListener("x-command-palette-open", onOpen as EventListener);
        cleanup.push(() => el.removeEventListener("x-command-palette-open", onOpen as EventListener));
      }
      if (onCloseRequest) {
        el.addEventListener("x-command-palette-close-request", onCloseRequest as EventListener);
        cleanup.push(() => el.removeEventListener("x-command-palette-close-request", onCloseRequest as EventListener));
      }
      if (onClose) {
        el.addEventListener("x-command-palette-close", onClose as EventListener);
        cleanup.push(() => el.removeEventListener("x-command-palette-close", onClose as EventListener));
      }
      if (onSelectRequest) {
        el.addEventListener("x-command-palette-select-request", onSelectRequest as EventListener);
        cleanup.push(() => el.removeEventListener("x-command-palette-select-request", onSelectRequest as EventListener));
      }
      if (onSelect) {
        el.addEventListener("x-command-palette-select", onSelect as EventListener);
        cleanup.push(() => el.removeEventListener("x-command-palette-select", onSelect as EventListener));
      }
      if (onQueryChange) {
        el.addEventListener("x-command-palette-query-change", onQueryChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-command-palette-query-change", onQueryChange as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onOpenRequest, onOpen, onCloseRequest, onClose, onSelectRequest, onSelect, onQueryChange]);

    return <x-command-palette ref={setRef} {...rest}>{children}</x-command-palette>;
  }
);
