"use client";
// x-search-field.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XSearchField as XSearchFieldElement } from "@vanelsas/baredom/x-search-field";
import { init } from "@vanelsas/baredom/x-search-field";

init();

export interface XSearchFieldProps {
  value?: string;
  name?: string;
  placeholder?: string;
  label?: string;
  autocomplete?: string;
  disabled?: boolean;
  required?: boolean;
  onInput?: (e: CustomEvent<{ name: string; value: string }>) => void;
  onChange?: (e: CustomEvent<{ name: string; value: string }>) => void;
  onSearch?: (e: CustomEvent<{ name: string; value: string }>) => void;
  onClear?: (e: CustomEvent<{ name: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XSearchField = forwardRef<XSearchFieldElement, XSearchFieldProps>(
  function XSearchField(props, forwardedRef) {
    const { onInput, onChange, onSearch, onClear, children, ...rest } = props;
    const innerRef = useRef<XSearchFieldElement>(null);

    const setRef = (el: XSearchFieldElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onInput) {
        el.addEventListener("x-search-field-input", onInput as EventListener);
        cleanup.push(() => el.removeEventListener("x-search-field-input", onInput as EventListener));
      }
      if (onChange) {
        el.addEventListener("x-search-field-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-search-field-change", onChange as EventListener));
      }
      if (onSearch) {
        el.addEventListener("x-search-field-search", onSearch as EventListener);
        cleanup.push(() => el.removeEventListener("x-search-field-search", onSearch as EventListener));
      }
      if (onClear) {
        el.addEventListener("x-search-field-clear", onClear as EventListener);
        cleanup.push(() => el.removeEventListener("x-search-field-clear", onClear as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onInput, onChange, onSearch, onClear]);

    return <x-search-field ref={setRef} {...rest}>{children}</x-search-field>;
  }
);
