"use client";
// x-menu-item.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XMenuItem as XMenuItemElement } from "@vanelsas/baredom/x-menu-item";
import { init } from "@vanelsas/baredom/x-menu-item";

init();

export interface XMenuItemProps {
  value?: string;
  disabled?: boolean;
  variant?: string;
  type?: string;
  onSelect?: (e: CustomEvent<{ value: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XMenuItem = forwardRef<XMenuItemElement, XMenuItemProps>(
  function XMenuItem(props, forwardedRef) {
    const { onSelect, children, ...rest } = props;
    const innerRef = useRef<XMenuItemElement>(null);

    const setRef = (el: XMenuItemElement | null) => {
      (innerRef as React.MutableRefObject<XMenuItemElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onSelect) {
        el.addEventListener("x-menu-item-select", onSelect as EventListener);
        cleanup.push(() => el.removeEventListener("x-menu-item-select", onSelect as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onSelect]);

    return <x-menu-item ref={setRef} {...rest}>{children}</x-menu-item>;
  }
);
