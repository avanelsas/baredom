"use client";
// x-progress.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XProgress as XProgressElement } from "@vanelsas/baredom/x-progress";
import { init } from "@vanelsas/baredom/x-progress";

init();

export interface XProgressProps {
  value?: string;
  max?: string;
  variant?: string;
  size?: string;
  label?: string;
  indeterminate?: boolean;
  showValue?: boolean;
  onComplete?: (e: CustomEvent<{ value: number; max: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XProgress = forwardRef<XProgressElement, XProgressProps>(
  function XProgress(props, forwardedRef) {
    const { onComplete, children, ...rest } = props;
    const innerRef = useRef<XProgressElement>(null);

    const setRef = (el: XProgressElement | null) => {
      (innerRef as React.MutableRefObject<XProgressElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onComplete) {
        el.addEventListener("x-progress-complete", onComplete as EventListener);
        cleanup.push(() => el.removeEventListener("x-progress-complete", onComplete as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onComplete]);

    return <x-progress ref={setRef} {...rest}>{children}</x-progress>;
  }
);
