"use client";
// x-confetti.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XConfetti as XConfettiElement } from "@vanelsas/baredom/x-confetti";
import { init } from "@vanelsas/baredom/x-confetti";

init();

export interface XConfettiProps {
  disabled?: boolean;
  spread?: number;
  mode?: string;
  colors?: string;
  duration?: number;
  autoFire?: boolean;
  gravity?: number;
  count?: number;
  origin?: string;
  velocity?: number;
  shapes?: string;
  onFire?: (e: CustomEvent<{ count: number; origin: string }>) => void;
  onEnd?: (e: CustomEvent<{ duration: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XConfetti = forwardRef<XConfettiElement, XConfettiProps>(
  function XConfetti(props, forwardedRef) {
    const { onFire, onEnd, children, ...rest } = props;
    const innerRef = useRef<XConfettiElement>(null);

    const setRef = (el: XConfettiElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onFire) {
        el.addEventListener("x-confetti-fire", onFire as EventListener);
        cleanup.push(() => el.removeEventListener("x-confetti-fire", onFire as EventListener));
      }
      if (onEnd) {
        el.addEventListener("x-confetti-end", onEnd as EventListener);
        cleanup.push(() => el.removeEventListener("x-confetti-end", onEnd as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onFire, onEnd]);

    return <x-confetti ref={setRef} {...rest}>{children}</x-confetti>;
  }
);
