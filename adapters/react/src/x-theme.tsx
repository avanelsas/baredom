"use client";
// x-theme.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XTheme as XThemeElement } from "@vanelsas/baredom/x-theme";
import { init } from "@vanelsas/baredom/x-theme";

init();

export interface XThemeProps {
  preset?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XTheme = forwardRef<XThemeElement, XThemeProps>(
  function XTheme(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XThemeElement>(null);

    const setRef = (el: XThemeElement | null) => {
      (innerRef as React.MutableRefObject<XThemeElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-theme ref={setRef} {...rest}>{children}</x-theme>;
  }
);
