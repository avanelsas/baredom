"use client";
// x-stat.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef } from "react";
import type { XStat as XStatElement } from "@vanelsas/baredom/x-stat";
import { init } from "@vanelsas/baredom/x-stat";

init();

export interface XStatProps {
  trend?: string;
  align?: string;
  variant?: string;
  value?: string;
  hint?: string;
  size?: string;
  loading?: boolean;
  label?: string;
  emphasis?: string;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XStat = forwardRef<XStatElement, XStatProps>(
  function XStat(props, forwardedRef) {
    const { children, ...rest } = props;
    const innerRef = useRef<XStatElement>(null);

    const setRef = (el: XStatElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    return <x-stat ref={setRef} {...rest}>{children}</x-stat>;
  }
);
