"use client";
// x-popover.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XPopover as XPopoverElement } from "@vanelsas/baredom/x-popover";
import { init } from "@vanelsas/baredom/x-popover";

init();

export interface XPopoverProps {
  open?: boolean;
  placement?: string;
  heading?: string;
  closeLabel?: string;
  noClose?: boolean;
  disabled?: boolean;
  portal?: boolean;
  onToggle?: (e: CustomEvent<{ open: boolean; source: string }>) => void;
  onChange?: (e: CustomEvent<{ open: boolean }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XPopover = forwardRef<XPopoverElement, XPopoverProps>(
  function XPopover(props, forwardedRef) {
    const { onToggle, onChange, children, ...rest } = props;
    const innerRef = useRef<XPopoverElement>(null);

    const setRef = (el: XPopoverElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onToggle) {
        el.addEventListener("x-popover-toggle", onToggle as EventListener);
        cleanup.push(() => el.removeEventListener("x-popover-toggle", onToggle as EventListener));
      }
      if (onChange) {
        el.addEventListener("x-popover-change", onChange as EventListener);
        cleanup.push(() => el.removeEventListener("x-popover-change", onChange as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onToggle, onChange]);

    return <x-popover ref={setRef} {...rest}>{children}</x-popover>;
  }
);
