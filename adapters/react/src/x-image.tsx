"use client";
// x-image.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XImage as XImageElement } from "@vanelsas/baredom/x-image";
import { init } from "@vanelsas/baredom/x-image";

init();

export interface XImageProps {
  alt?: string;
  fit?: string;
  src?: string;
  decorative?: boolean;
  loading?: string;
  ratio?: string;
  position?: string;
  onLoad?: (e: CustomEvent<{ src: string; naturalWidth: number; naturalHeight: number }>) => void;
  onError?: (e: CustomEvent<{ src: string }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XImage = forwardRef<XImageElement, XImageProps>(
  function XImage(props, forwardedRef) {
    const { onLoad, onError, children, ...rest } = props;
    const innerRef = useRef<XImageElement>(null);

    const setRef = (el: XImageElement | null) => {
      (innerRef as React.MutableRefObject<XImageElement | null>).current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onLoad) {
        el.addEventListener("x-image-load", onLoad as EventListener);
        cleanup.push(() => el.removeEventListener("x-image-load", onLoad as EventListener));
      }
      if (onError) {
        el.addEventListener("x-image-error", onError as EventListener);
        cleanup.push(() => el.removeEventListener("x-image-error", onError as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onLoad, onError]);

    return <x-image ref={setRef} {...rest}>{children}</x-image>;
  }
);
