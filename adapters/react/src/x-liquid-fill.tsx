"use client";
// x-liquid-fill.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XLiquidFill as XLiquidFillElement } from "@vanelsas/baredom/x-liquid-fill";
import { init } from "@vanelsas/baredom/x-liquid-fill";

init();

export interface XLiquidFillProps {
  waveIntensity?: number;
  disabled?: boolean;
  splashIntensity?: number;
  mode?: string;
  orientation?: string;
  theme?: string;
  layers?: number;
  target?: string;
  onProgress?: (e: CustomEvent<{ progress: number; velocity: number }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XLiquidFill = forwardRef<XLiquidFillElement, XLiquidFillProps>(
  function XLiquidFill(props, forwardedRef) {
    const { onProgress, children, ...rest } = props;
    const innerRef = useRef<XLiquidFillElement>(null);

    const setRef = (el: XLiquidFillElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onProgress) {
        el.addEventListener("x-liquid-fill-progress", onProgress as EventListener);
        cleanup.push(() => el.removeEventListener("x-liquid-fill-progress", onProgress as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onProgress]);

    return <x-liquid-fill ref={setRef} {...rest}>{children}</x-liquid-fill>;
  }
);
