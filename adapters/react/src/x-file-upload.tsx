"use client";
// x-file-upload.tsx — auto-generated by generate_react.bb, do not edit

import React, { forwardRef, useRef, useEffect } from "react";
import type { XFileUpload as XFileUploadElement } from "@vanelsas/baredom/x-file-upload";
import { init } from "@vanelsas/baredom/x-file-upload";

init();

export interface XFileUploadProps {
  accept?: string;
  multiple?: boolean;
  maxSize?: number;
  maxFiles?: number;
  disabled?: boolean;
  required?: boolean;
  name?: string;
  onSelect?: (e: CustomEvent<{ files: any; rejected: any }>) => void;
  onRemove?: (e: CustomEvent<{ file: Record<string, any>; remaining: any }>) => void;
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
  id?: string;
  slot?: string;
}

export const XFileUpload = forwardRef<XFileUploadElement, XFileUploadProps>(
  function XFileUpload(props, forwardedRef) {
    const { onSelect, onRemove, children, ...rest } = props;
    const innerRef = useRef<XFileUploadElement>(null);

    const setRef = (el: XFileUploadElement | null) => {
      innerRef.current = el;
      if (typeof forwardedRef === "function") forwardedRef(el);
      else if (forwardedRef) forwardedRef.current = el;
    };

    useEffect(() => {
      const el = innerRef.current;
      if (!el) return;
      const cleanup: Array<() => void> = [];

      if (onSelect) {
        el.addEventListener("x-file-upload-select", onSelect as EventListener);
        cleanup.push(() => el.removeEventListener("x-file-upload-select", onSelect as EventListener));
      }
      if (onRemove) {
        el.addEventListener("x-file-upload-remove", onRemove as EventListener);
        cleanup.push(() => el.removeEventListener("x-file-upload-remove", onRemove as EventListener));
      }

      return () => cleanup.forEach(fn => fn());
    }, [onSelect, onRemove]);

    return <x-file-upload ref={setRef} {...rest}>{children}</x-file-upload>;
  }
);
