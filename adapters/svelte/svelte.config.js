// svelte.config.js — Svelte 5 handles `<script lang="ts">` natively;
// no preprocessor is required for this adapter (no scoped TS imports
// outside script tags, no PostCSS, no Less/Sass).

export default {};
