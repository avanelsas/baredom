<!-- x-select.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XSelect as XSelectElement } from "@vanelsas/baredom/x-select";
  import { init } from "@vanelsas/baredom/x-select";
  init();
  export type { XSelectElement };
  export interface XSelectProps {
    disabled?: boolean;
    required?: boolean;
    /** Two-way bindable form value — `bind:value={...}`. */
    value?: string;
    onchangerequest?: (e: CustomEvent<{ value: string; label: string; previousValue: string }>) => void;
    onselectchange?: (e: CustomEvent<{ value: string; label: string }>) => void;
    el?: XSelectElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    disabled,
    required,
    value = $bindable(undefined),
    onchangerequest,
    onselectchange,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XSelectProps = $props();

  $effect(() => {
    const node = el;
    if (!node || value === undefined) return;
    if (value != null) node.setAttribute("value", String(value));
    else node.removeAttribute("value");
  });

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangerequestHandler = (e: Event) => onchangerequest?.(e as CustomEvent<{ value: string; label: string; previousValue: string }>);
    node.addEventListener("x-select-change-request", onchangerequestHandler);
    cleanups.push(() => node.removeEventListener("x-select-change-request", onchangerequestHandler));
    const onselectchangeHandler = (e: Event) => {
      const detail = (e as CustomEvent<{ value: string; label: string }>).detail;
      value = String(detail.value);
      onselectchange?.(e as CustomEvent<{ value: string; label: string }>);
    };
    node.addEventListener("select-change", onselectchangeHandler);
    cleanups.push(() => node.removeEventListener("select-change", onselectchangeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-select
  bind:this={el}
  {disabled}
  {required}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-select>
