<!-- x-otp-input.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XOtpInput as XOtpInputElement } from "@vanelsas/baredom/x-otp-input";
  import { init } from "@vanelsas/baredom/x-otp-input";
  init();
  export type { XOtpInputElement };
  export interface XOtpInputProps {
    mask?: boolean;
    placeholder?: string;
    disabled?: boolean;
    name?: string;
    value?: string;
    readonly?: boolean;
    type?: string;
    label?: string;
    autofocus?: boolean;
    length?: number;
    error?: string;
    required?: boolean;
    oninput?: (e: CustomEvent<{ name: string; value: string; complete: boolean }>) => void;
    onchange?: (e: CustomEvent<{ name: string; value: string; complete: boolean }>) => void;
    oncomplete?: (e: CustomEvent<{ name: string; value: string }>) => void;
    el?: XOtpInputElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    mask,
    placeholder,
    disabled,
    name,
    value,
    readonly,
    type,
    label,
    autofocus,
    length,
    error,
    required,
    oninput,
    onchange,
    oncomplete,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XOtpInputProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const oninputHandler = (e: Event) => oninput?.(e as CustomEvent<{ name: string; value: string; complete: boolean }>);
    node.addEventListener("x-otp-input-input", oninputHandler);
    cleanups.push(() => node.removeEventListener("x-otp-input-input", oninputHandler));
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ name: string; value: string; complete: boolean }>);
    node.addEventListener("x-otp-input-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-otp-input-change", onchangeHandler));
    const oncompleteHandler = (e: Event) => oncomplete?.(e as CustomEvent<{ name: string; value: string }>);
    node.addEventListener("x-otp-input-complete", oncompleteHandler);
    cleanups.push(() => node.removeEventListener("x-otp-input-complete", oncompleteHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<!-- svelte-ignore a11y_autofocus -->
<x-otp-input
  bind:this={el}
  {mask}
  {placeholder}
  {disabled}
  {name}
  {value}
  {readonly}
  {type}
  {label}
  {autofocus}
  {length}
  {error}
  {required}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-otp-input>
