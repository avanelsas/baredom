<!-- x-slider.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XSlider as XSliderElement } from "@vanelsas/baredom/x-slider";
  import { init } from "@vanelsas/baredom/x-slider";
  init();
  export type { XSliderElement };
  export interface XSliderProps {
    min?: string;
    disabled?: boolean;
    showValue?: boolean;
    name?: string;
    size?: string;
    readOnly?: boolean;
    max?: string;
    label?: string;
    step?: string;
    /** Two-way bindable form value — `bind:value={...}`. */
    value?: string;
    onchangerequest?: (e: CustomEvent<{ value: number; previousValue: number; min: number; max: number }>) => void;
    oninput?: (e: CustomEvent<{ value: number; min: number; max: number }>) => void;
    onchange?: (e: CustomEvent<{ value: number; min: number; max: number }>) => void;
    el?: XSliderElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    min,
    disabled,
    showValue,
    name,
    size,
    readOnly,
    max,
    label,
    step,
    value = $bindable(undefined),
    onchangerequest,
    oninput,
    onchange,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XSliderProps = $props();

  $effect(() => {
    const node = el;
    if (!node || value === undefined) return;
    if (value != null) node.setAttribute("value", String(value));
    else node.removeAttribute("value");
  });

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangerequestHandler = (e: Event) => onchangerequest?.(e as CustomEvent<{ value: number; previousValue: number; min: number; max: number }>);
    node.addEventListener("x-slider-change-request", onchangerequestHandler);
    cleanups.push(() => node.removeEventListener("x-slider-change-request", onchangerequestHandler));
    const oninputHandler = (e: Event) => oninput?.(e as CustomEvent<{ value: number; min: number; max: number }>);
    node.addEventListener("x-slider-input", oninputHandler);
    cleanups.push(() => node.removeEventListener("x-slider-input", oninputHandler));
    const onchangeHandler = (e: Event) => {
      const detail = (e as CustomEvent<{ value: number; min: number; max: number }>).detail;
      value = String(detail.value);
      onchange?.(e as CustomEvent<{ value: number; min: number; max: number }>);
    };
    node.addEventListener("x-slider-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-slider-change", onchangeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-slider
  bind:this={el}
  {min}
  {disabled}
  {showValue}
  {name}
  {size}
  {readOnly}
  {max}
  {label}
  {step}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-slider>
