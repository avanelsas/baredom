<!-- x-chart.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XChart as XChartElement } from "@vanelsas/baredom/x-chart";
  import { init } from "@vanelsas/baredom/x-chart";
  init();
  export type { XChartElement };
  export interface XChartProps {
    selected?: string;
    disabled?: boolean;
    axes?: boolean;
    grid?: boolean;
    cursor?: string;
    type?: string;
    loading?: boolean;
    padding?: number;
    tooltip?: boolean;
    height?: number;
    data?: any;
    onselect?: (e: CustomEvent<{ seriesId: string; index: number; x: number; y: number; value: number }>) => void;
    onhover?: (e: CustomEvent<{ seriesId: string; index: number; x: number; y: number; value: number }>) => void;
    el?: XChartElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    selected,
    disabled,
    axes,
    grid,
    cursor,
    type,
    loading,
    padding,
    tooltip,
    height,
    data,
    onselect,
    onhover,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XChartProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onselectHandler = (e: Event) => onselect?.(e as CustomEvent<{ seriesId: string; index: number; x: number; y: number; value: number }>);
    node.addEventListener("x-chart-select", onselectHandler);
    cleanups.push(() => node.removeEventListener("x-chart-select", onselectHandler));
    const onhoverHandler = (e: Event) => onhover?.(e as CustomEvent<{ seriesId: string; index: number; x: number; y: number; value: number }>);
    node.addEventListener("x-chart-hover", onhoverHandler);
    cleanups.push(() => node.removeEventListener("x-chart-hover", onhoverHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-chart
  bind:this={el}
  {selected}
  {disabled}
  {axes}
  {grid}
  {cursor}
  {type}
  {loading}
  {padding}
  {tooltip}
  {height}
  {data}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-chart>
