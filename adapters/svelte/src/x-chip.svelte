<!-- x-chip.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XChip as XChipElement } from "@vanelsas/baredom/x-chip";
  import { init } from "@vanelsas/baredom/x-chip";
  init();
  export type { XChipElement };
  export interface XChipProps {
    label?: string;
    value?: string;
    removable?: boolean;
    disabled?: boolean;
    onremove?: (e: CustomEvent<{ value: string; label: string }>) => void;
    el?: XChipElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    label,
    value,
    removable,
    disabled,
    onremove,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XChipProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onremoveHandler = (e: Event) => onremove?.(e as CustomEvent<{ value: string; label: string }>);
    node.addEventListener("x-chip-remove", onremoveHandler);
    cleanups.push(() => node.removeEventListener("x-chip-remove", onremoveHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-chip
  bind:this={el}
  {label}
  {value}
  {removable}
  {disabled}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-chip>
