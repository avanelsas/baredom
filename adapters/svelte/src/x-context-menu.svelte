<!-- x-context-menu.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XContextMenu as XContextMenuElement } from "@vanelsas/baredom/x-context-menu";
  import { init } from "@vanelsas/baredom/x-context-menu";
  init();
  export type { XContextMenuElement };
  export interface XContextMenuProps {
    open?: boolean;
    disabled?: boolean;
    placement?: string;
    offset?: string;
    zIndex?: string;
    onopenrequest?: (e: CustomEvent<{ reason: string }>) => void;
    onopen?: (e: CustomEvent<{ reason: string }>) => void;
    oncloserequest?: (e: CustomEvent<{ reason: string }>) => void;
    onclose?: (e: CustomEvent<{ reason: string }>) => void;
    onselect?: (e: CustomEvent<{ item: Record<string, any> }>) => void;
    el?: XContextMenuElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    open,
    disabled,
    placement,
    offset,
    zIndex,
    onopenrequest,
    onopen,
    oncloserequest,
    onclose,
    onselect,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XContextMenuProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onopenrequestHandler = (e: Event) => onopenrequest?.(e as CustomEvent<{ reason: string }>);
    node.addEventListener("x-context-menu-open-request", onopenrequestHandler);
    cleanups.push(() => node.removeEventListener("x-context-menu-open-request", onopenrequestHandler));
    const onopenHandler = (e: Event) => onopen?.(e as CustomEvent<{ reason: string }>);
    node.addEventListener("x-context-menu-open", onopenHandler);
    cleanups.push(() => node.removeEventListener("x-context-menu-open", onopenHandler));
    const oncloserequestHandler = (e: Event) => oncloserequest?.(e as CustomEvent<{ reason: string }>);
    node.addEventListener("x-context-menu-close-request", oncloserequestHandler);
    cleanups.push(() => node.removeEventListener("x-context-menu-close-request", oncloserequestHandler));
    const oncloseHandler = (e: Event) => onclose?.(e as CustomEvent<{ reason: string }>);
    node.addEventListener("x-context-menu-close", oncloseHandler);
    cleanups.push(() => node.removeEventListener("x-context-menu-close", oncloseHandler));
    const onselectHandler = (e: Event) => onselect?.(e as CustomEvent<{ item: Record<string, any> }>);
    node.addEventListener("x-context-menu-select", onselectHandler);
    cleanups.push(() => node.removeEventListener("x-context-menu-select", onselectHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-context-menu
  bind:this={el}
  {open}
  {disabled}
  {placement}
  {offset}
  {zIndex}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-context-menu>
