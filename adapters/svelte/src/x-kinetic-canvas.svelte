<!-- x-kinetic-canvas.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XKineticCanvas as XKineticCanvasElement } from "@vanelsas/baredom/x-kinetic-canvas";
  import { init } from "@vanelsas/baredom/x-kinetic-canvas";
  init();
  export type { XKineticCanvasElement };
  export interface XKineticCanvasProps {
    type?: string;
    variant?: string;
    speed?: string;
    density?: string;
    fullscreen?: boolean;
    paused?: boolean;
    el?: XKineticCanvasElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    type,
    variant,
    speed,
    density,
    fullscreen,
    paused,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XKineticCanvasProps = $props();
</script>

<x-kinetic-canvas
  bind:this={el}
  {type}
  {variant}
  {speed}
  {density}
  {fullscreen}
  {paused}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-kinetic-canvas>
