<!-- x-code.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XCode as XCodeElement } from "@vanelsas/baredom/x-code";
  import { init } from "@vanelsas/baredom/x-code";
  init();
  export type { XCodeElement };
  export interface XCodeProps {
    language?: string;
    filename?: string;
    showCopy?: boolean;
    lineNumbers?: boolean;
    wrap?: boolean;
    maxLines?: number;
    expanded?: boolean;
    code?: string;
    oncopy?: (e: CustomEvent<{ code: string }>) => void;
    ontoggle?: (e: CustomEvent<{ expanded: boolean }>) => void;
    el?: XCodeElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    language,
    filename,
    showCopy,
    lineNumbers,
    wrap,
    maxLines,
    expanded,
    code,
    oncopy,
    ontoggle,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XCodeProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const oncopyHandler = (e: Event) => oncopy?.(e as CustomEvent<{ code: string }>);
    node.addEventListener("x-code-copy", oncopyHandler);
    cleanups.push(() => node.removeEventListener("x-code-copy", oncopyHandler));
    const ontoggleHandler = (e: Event) => ontoggle?.(e as CustomEvent<{ expanded: boolean }>);
    node.addEventListener("x-code-toggle", ontoggleHandler);
    cleanups.push(() => node.removeEventListener("x-code-toggle", ontoggleHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-code
  bind:this={el}
  {language}
  {filename}
  {showCopy}
  {lineNumbers}
  {wrap}
  {maxLines}
  {expanded}
  {code}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-code>
