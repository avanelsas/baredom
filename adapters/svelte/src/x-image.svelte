<!-- x-image.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XImage as XImageElement } from "@vanelsas/baredom/x-image";
  import { init } from "@vanelsas/baredom/x-image";
  init();
  export type { XImageElement };
  export interface XImageProps {
    alt?: string;
    fit?: string;
    src?: string;
    decorative?: boolean;
    loading?: string;
    ratio?: string;
    position?: string;
    onload?: (e: CustomEvent<{ src: string; naturalWidth: number; naturalHeight: number }>) => void;
    onerror?: (e: CustomEvent<{ src: string }>) => void;
    el?: XImageElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    alt,
    fit,
    src,
    decorative,
    loading,
    ratio,
    position,
    onload,
    onerror,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XImageProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onloadHandler = (e: Event) => onload?.(e as CustomEvent<{ src: string; naturalWidth: number; naturalHeight: number }>);
    node.addEventListener("x-image-load", onloadHandler);
    cleanups.push(() => node.removeEventListener("x-image-load", onloadHandler));
    const onerrorHandler = (e: Event) => onerror?.(e as CustomEvent<{ src: string }>);
    node.addEventListener("x-image-error", onerrorHandler);
    cleanups.push(() => node.removeEventListener("x-image-error", onerrorHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-image
  bind:this={el}
  {alt}
  {fit}
  {src}
  {decorative}
  {loading}
  {ratio}
  {position}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-image>
