<!-- x-alert.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XAlert as XAlertElement } from "@vanelsas/baredom/x-alert";
  import { init } from "@vanelsas/baredom/x-alert";
  init();
  export type { XAlertElement };
  export interface XAlertProps {
    type?: string;
    text?: string;
    icon?: string;
    dismissible?: boolean;
    disabled?: boolean;
    timeoutMs?: number;
    ondismiss?: (e: CustomEvent<{ type: string; reason: string; text: string }>) => void;
    el?: XAlertElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    type,
    text,
    icon,
    dismissible,
    disabled,
    timeoutMs,
    ondismiss,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XAlertProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const ondismissHandler = (e: Event) => ondismiss?.(e as CustomEvent<{ type: string; reason: string; text: string }>);
    node.addEventListener("x-alert-dismiss", ondismissHandler);
    cleanups.push(() => node.removeEventListener("x-alert-dismiss", ondismissHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-alert
  bind:this={el}
  {type}
  {text}
  {icon}
  {dismissible}
  {disabled}
  {timeoutMs}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-alert>
