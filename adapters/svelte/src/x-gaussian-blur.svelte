<!-- x-gaussian-blur.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XGaussianBlur as XGaussianBlurElement } from "@vanelsas/baredom/x-gaussian-blur";
  import { init } from "@vanelsas/baredom/x-gaussian-blur";
  init();
  export type { XGaussianBlurElement };
  export interface XGaussianBlurProps {
    paused?: boolean;
    animation?: string;
    speed?: string;
    colors?: string;
    size?: string;
    blur?: string;
    opacity?: string;
    count?: string;
    blend?: string;
    el?: XGaussianBlurElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    paused,
    animation,
    speed,
    colors,
    size,
    blur,
    opacity,
    count,
    blend,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XGaussianBlurProps = $props();
</script>

<x-gaussian-blur
  bind:this={el}
  {paused}
  {animation}
  {speed}
  {colors}
  {size}
  {blur}
  {opacity}
  {count}
  {blend}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-gaussian-blur>
