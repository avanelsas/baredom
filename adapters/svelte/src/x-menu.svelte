<!-- x-menu.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XMenu as XMenuElement } from "@vanelsas/baredom/x-menu";
  import { init } from "@vanelsas/baredom/x-menu";
  init();
  export type { XMenuElement };
  export interface XMenuProps {
    open?: boolean;
    placement?: string;
    label?: string;
    onopen?: (e: CustomEvent<{}>) => void;
    onclose?: (e: CustomEvent<{}>) => void;
    onselect?: (e: CustomEvent<{ value: string }>) => void;
    el?: XMenuElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    open,
    placement,
    label,
    onopen,
    onclose,
    onselect,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XMenuProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onopenHandler = (e: Event) => onopen?.(e as CustomEvent<{}>);
    node.addEventListener("x-menu-open", onopenHandler);
    cleanups.push(() => node.removeEventListener("x-menu-open", onopenHandler));
    const oncloseHandler = (e: Event) => onclose?.(e as CustomEvent<{}>);
    node.addEventListener("x-menu-close", oncloseHandler);
    cleanups.push(() => node.removeEventListener("x-menu-close", oncloseHandler));
    const onselectHandler = (e: Event) => onselect?.(e as CustomEvent<{ value: string }>);
    node.addEventListener("x-menu-select", onselectHandler);
    cleanups.push(() => node.removeEventListener("x-menu-select", onselectHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-menu
  bind:this={el}
  {open}
  {placement}
  {label}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-menu>
