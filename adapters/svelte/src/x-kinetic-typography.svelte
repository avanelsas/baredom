<!-- x-kinetic-typography.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XKineticTypography as XKineticTypographyElement } from "@vanelsas/baredom/x-kinetic-typography";
  import { init } from "@vanelsas/baredom/x-kinetic-typography";
  init();
  export type { XKineticTypographyElement };
  export interface XKineticTypographyProps {
    path?: string;
    startSize?: string;
    animation?: string;
    speed?: string;
    echoCount?: string;
    echoScale?: string;
    echoOpacity?: string;
    preset?: string;
    repeat?: string;
    fontSize?: string;
    effect?: string;
    echoDelay?: string;
    endSize?: string;
    direction?: string;
    text?: string;
    el?: XKineticTypographyElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    path,
    startSize,
    animation,
    speed,
    echoCount,
    echoScale,
    echoOpacity,
    preset,
    repeat,
    fontSize,
    effect,
    echoDelay,
    endSize,
    direction,
    text,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XKineticTypographyProps = $props();
</script>

<x-kinetic-typography
  bind:this={el}
  {path}
  {startSize}
  {animation}
  {speed}
  {echoCount}
  {echoScale}
  {echoOpacity}
  {preset}
  {repeat}
  {fontSize}
  {effect}
  {echoDelay}
  {endSize}
  {direction}
  {text}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-kinetic-typography>
