<!-- x-popover.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XPopover as XPopoverElement } from "@vanelsas/baredom/x-popover";
  import { init } from "@vanelsas/baredom/x-popover";
  init();
  export type { XPopoverElement };
  export interface XPopoverProps {
    open?: boolean;
    placement?: string;
    heading?: string;
    closeLabel?: string;
    noClose?: boolean;
    disabled?: boolean;
    portal?: boolean;
    ontoggle?: (e: CustomEvent<{ open: boolean; source: string }>) => void;
    onchange?: (e: CustomEvent<{ open: boolean }>) => void;
    el?: XPopoverElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    open,
    placement,
    heading,
    closeLabel,
    noClose,
    disabled,
    portal,
    ontoggle,
    onchange,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XPopoverProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const ontoggleHandler = (e: Event) => ontoggle?.(e as CustomEvent<{ open: boolean; source: string }>);
    node.addEventListener("x-popover-toggle", ontoggleHandler);
    cleanups.push(() => node.removeEventListener("x-popover-toggle", ontoggleHandler));
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ open: boolean }>);
    node.addEventListener("x-popover-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-popover-change", onchangeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-popover
  bind:this={el}
  {open}
  {placement}
  {heading}
  {closeLabel}
  {noClose}
  {disabled}
  {portal}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-popover>
