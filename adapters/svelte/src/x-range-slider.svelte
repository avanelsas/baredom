<!-- x-range-slider.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XRangeSlider as XRangeSliderElement } from "@vanelsas/baredom/x-range-slider";
  import { init } from "@vanelsas/baredom/x-range-slider";
  init();
  export type { XRangeSliderElement };
  export interface XRangeSliderProps {
    min?: string;
    disabled?: boolean;
    showValue?: boolean;
    name?: string;
    start?: string;
    minGap?: string;
    size?: string;
    readOnly?: boolean;
    max?: string;
    label?: string;
    end?: string;
    step?: string;
    onchangerequest?: (e: CustomEvent<{ start: number; end: number; previousStart: number; previousEnd: number; min: number; max: number }>) => void;
    oninput?: (e: CustomEvent<{ start: number; end: number; min: number; max: number }>) => void;
    onchange?: (e: CustomEvent<{ start: number; end: number; min: number; max: number }>) => void;
    el?: XRangeSliderElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    min,
    disabled,
    showValue,
    name,
    start,
    minGap,
    size,
    readOnly,
    max,
    label,
    end,
    step,
    onchangerequest,
    oninput,
    onchange,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XRangeSliderProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangerequestHandler = (e: Event) => onchangerequest?.(e as CustomEvent<{ start: number; end: number; previousStart: number; previousEnd: number; min: number; max: number }>);
    node.addEventListener("x-range-slider-change-request", onchangerequestHandler);
    cleanups.push(() => node.removeEventListener("x-range-slider-change-request", onchangerequestHandler));
    const oninputHandler = (e: Event) => oninput?.(e as CustomEvent<{ start: number; end: number; min: number; max: number }>);
    node.addEventListener("x-range-slider-input", oninputHandler);
    cleanups.push(() => node.removeEventListener("x-range-slider-input", oninputHandler));
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ start: number; end: number; min: number; max: number }>);
    node.addEventListener("x-range-slider-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-range-slider-change", onchangeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-range-slider
  bind:this={el}
  {min}
  {disabled}
  {showValue}
  {name}
  {start}
  {minGap}
  {size}
  {readOnly}
  {max}
  {label}
  {end}
  {step}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-range-slider>
