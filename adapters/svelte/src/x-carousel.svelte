<!-- x-carousel.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XCarousel as XCarouselElement } from "@vanelsas/baredom/x-carousel";
  import { init } from "@vanelsas/baredom/x-carousel";
  init();
  export type { XCarouselElement };
  export interface XCarouselProps {
    arrows?: boolean;
    disabled?: boolean;
    transition?: string;
    loop?: boolean;
    interval?: number;
    autoplay?: boolean;
    peek?: string;
    dots?: boolean;
    currentSlide?: number;
    direction?: string;
    onchange?: (e: CustomEvent<{ index: number; previousIndex: number; reason: string }>) => void;
    el?: XCarouselElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    arrows,
    disabled,
    transition,
    loop,
    interval,
    autoplay,
    peek,
    dots,
    currentSlide,
    direction,
    onchange,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XCarouselProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ index: number; previousIndex: number; reason: string }>);
    node.addEventListener("x-carousel-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-carousel-change", onchangeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-carousel
  bind:this={el}
  {arrows}
  {disabled}
  {transition}
  {loop}
  {interval}
  {autoplay}
  {peek}
  {dots}
  {currentSlide}
  {direction}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-carousel>
