<!-- x-kinetic-font.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XKineticFont as XKineticFontElement } from "@vanelsas/baredom/x-kinetic-font";
  import { init } from "@vanelsas/baredom/x-kinetic-font";
  init();
  export type { XKineticFontElement };
  export interface XKineticFontProps {
    mass?: number;
    tension?: number;
    mode?: string;
    perChar?: boolean;
    radius?: number;
    intensity?: number;
    fontFamily?: string;
    friction?: number;
    trigger?: string;
    text?: string;
    onspringactivate?: (e: CustomEvent<{}>) => void;
    onspringsettle?: (e: CustomEvent<{}>) => void;
    el?: XKineticFontElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    mass,
    tension,
    mode,
    perChar,
    radius,
    intensity,
    fontFamily,
    friction,
    trigger,
    text,
    onspringactivate,
    onspringsettle,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XKineticFontProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onspringactivateHandler = (e: Event) => onspringactivate?.(e as CustomEvent<{}>);
    node.addEventListener("x-kinetic-font-spring-activate", onspringactivateHandler);
    cleanups.push(() => node.removeEventListener("x-kinetic-font-spring-activate", onspringactivateHandler));
    const onspringsettleHandler = (e: Event) => onspringsettle?.(e as CustomEvent<{}>);
    node.addEventListener("x-kinetic-font-spring-settle", onspringsettleHandler);
    cleanups.push(() => node.removeEventListener("x-kinetic-font-spring-settle", onspringsettleHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-kinetic-font
  bind:this={el}
  {mass}
  {tension}
  {mode}
  {perChar}
  {radius}
  {intensity}
  {fontFamily}
  {friction}
  {trigger}
  {text}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-kinetic-font>
