<!-- x-icon.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XIcon as XIconElement } from "@vanelsas/baredom/x-icon";
  import { init } from "@vanelsas/baredom/x-icon";
  init();
  export type { XIconElement };
  export interface XIconProps {
    size?: string;
    color?: string;
    label?: string;
    el?: XIconElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    size,
    color,
    label,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XIconProps = $props();
</script>

<x-icon
  bind:this={el}
  {size}
  {color}
  {label}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-icon>
