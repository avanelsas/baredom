<!-- x-spinner.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XSpinner as XSpinnerElement } from "@vanelsas/baredom/x-spinner";
  import { init } from "@vanelsas/baredom/x-spinner";
  init();
  export type { XSpinnerElement };
  export interface XSpinnerProps {
    size?: string;
    variant?: string;
    label?: string;
    el?: XSpinnerElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    size,
    variant,
    label,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XSpinnerProps = $props();
</script>

<x-spinner
  bind:this={el}
  {size}
  {variant}
  {label}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-spinner>
