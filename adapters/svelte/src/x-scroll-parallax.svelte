<!-- x-scroll-parallax.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XScrollParallax as XScrollParallaxElement } from "@vanelsas/baredom/x-scroll-parallax";
  import { init } from "@vanelsas/baredom/x-scroll-parallax";
  init();
  export type { XScrollParallaxElement };
  export interface XScrollParallaxProps {
    direction?: string;
    source?: string;
    easing?: string;
    disabled?: boolean;
    label?: string;
    onenter?: (e: CustomEvent<{ progress: number }>) => void;
    onleave?: (e: CustomEvent<{ progress: number }>) => void;
    onprogress?: (e: CustomEvent<{ progress: number }>) => void;
    el?: XScrollParallaxElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    direction,
    source,
    easing,
    disabled,
    label,
    onenter,
    onleave,
    onprogress,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XScrollParallaxProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onenterHandler = (e: Event) => onenter?.(e as CustomEvent<{ progress: number }>);
    node.addEventListener("x-scroll-parallax-enter", onenterHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-parallax-enter", onenterHandler));
    const onleaveHandler = (e: Event) => onleave?.(e as CustomEvent<{ progress: number }>);
    node.addEventListener("x-scroll-parallax-leave", onleaveHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-parallax-leave", onleaveHandler));
    const onprogressHandler = (e: Event) => onprogress?.(e as CustomEvent<{ progress: number }>);
    node.addEventListener("x-scroll-parallax-progress", onprogressHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-parallax-progress", onprogressHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-scroll-parallax
  bind:this={el}
  {direction}
  {source}
  {easing}
  {disabled}
  {label}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-scroll-parallax>
