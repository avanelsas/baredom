<!-- x-divider.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XDivider as XDividerElement } from "@vanelsas/baredom/x-divider";
  import { init } from "@vanelsas/baredom/x-divider";
  init();
  export type { XDividerElement };
  export interface XDividerProps {
    orientation?: string;
    variant?: string;
    align?: string;
    label?: string;
    thickness?: string;
    color?: string;
    inset?: string;
    length?: string;
    el?: XDividerElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    orientation,
    variant,
    align,
    label,
    thickness,
    color,
    inset,
    length,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XDividerProps = $props();
</script>

<x-divider
  bind:this={el}
  {orientation}
  {variant}
  {align}
  {label}
  {thickness}
  {color}
  {inset}
  {length}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-divider>
