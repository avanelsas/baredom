<!-- x-file-download.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XFileDownload as XFileDownloadElement } from "@vanelsas/baredom/x-file-download";
  import { init } from "@vanelsas/baredom/x-file-download";
  init();
  export type { XFileDownloadElement };
  export interface XFileDownloadProps {
    href?: string;
    filename?: string;
    disabled?: boolean;
    onclick?: (e: CustomEvent<{ href: string; filename: string }>) => void;
    el?: XFileDownloadElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    href,
    filename,
    disabled,
    onclick,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XFileDownloadProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onclickHandler = (e: Event) => onclick?.(e as CustomEvent<{ href: string; filename: string }>);
    node.addEventListener("x-file-download-click", onclickHandler);
    cleanups.push(() => node.removeEventListener("x-file-download-click", onclickHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-file-download
  bind:this={el}
  {href}
  {filename}
  {disabled}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-file-download>
