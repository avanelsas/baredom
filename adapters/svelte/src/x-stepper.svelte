<!-- x-stepper.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XStepper as XStepperElement } from "@vanelsas/baredom/x-stepper";
  import { init } from "@vanelsas/baredom/x-stepper";
  init();
  export type { XStepperElement };
  export interface XStepperProps {
    steps?: string;
    current?: number;
    orientation?: string;
    size?: string;
    disabled?: boolean;
    onchange?: (e: CustomEvent<{ from: number; to: number }>) => void;
    el?: XStepperElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    steps,
    current,
    orientation,
    size,
    disabled,
    onchange,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XStepperProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ from: number; to: number }>);
    node.addEventListener("x-stepper-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-stepper-change", onchangeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-stepper
  bind:this={el}
  {steps}
  {current}
  {orientation}
  {size}
  {disabled}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-stepper>
