<!-- x-copy.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XCopy as XCopyElement } from "@vanelsas/baredom/x-copy";
  import { init } from "@vanelsas/baredom/x-copy";
  init();
  export type { XCopyElement };
  export interface XCopyProps {
    errorMessage?: string;
    disabled?: boolean;
    textValue?: string;
    mode?: string;
    fromAttr?: string;
    tooltipMs?: number;
    hotkey?: string;
    from?: string;
    showTooltip?: boolean;
    successMessage?: string;
    text?: string;
    onrequest?: (e: CustomEvent<{ text: string; mode: string; from: string; fromAttr: string }>) => void;
    onsuccess?: (e: CustomEvent<{ text: string }>) => void;
    onerror?: (e: CustomEvent<{ error: string }>) => void;
    el?: XCopyElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    errorMessage,
    disabled,
    textValue,
    mode,
    fromAttr,
    tooltipMs,
    hotkey,
    from,
    showTooltip,
    successMessage,
    text,
    onrequest,
    onsuccess,
    onerror,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XCopyProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onrequestHandler = (e: Event) => onrequest?.(e as CustomEvent<{ text: string; mode: string; from: string; fromAttr: string }>);
    node.addEventListener("x-copy-request", onrequestHandler);
    cleanups.push(() => node.removeEventListener("x-copy-request", onrequestHandler));
    const onsuccessHandler = (e: Event) => onsuccess?.(e as CustomEvent<{ text: string }>);
    node.addEventListener("x-copy-success", onsuccessHandler);
    cleanups.push(() => node.removeEventListener("x-copy-success", onsuccessHandler));
    const onerrorHandler = (e: Event) => onerror?.(e as CustomEvent<{ error: string }>);
    node.addEventListener("x-copy-error", onerrorHandler);
    cleanups.push(() => node.removeEventListener("x-copy-error", onerrorHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-copy
  bind:this={el}
  {errorMessage}
  {disabled}
  {textValue}
  {mode}
  {fromAttr}
  {tooltipMs}
  {hotkey}
  {from}
  {showTooltip}
  {successMessage}
  {text}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-copy>
