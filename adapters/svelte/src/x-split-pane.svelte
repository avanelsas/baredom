<!-- x-split-pane.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XSplitPane as XSplitPaneElement } from "@vanelsas/baredom/x-split-pane";
  import { init } from "@vanelsas/baredom/x-split-pane";
  init();
  export type { XSplitPaneElement };
  export interface XSplitPaneProps {
    orientation?: string;
    position?: number;
    minStart?: number;
    minEnd?: number;
    disabled?: boolean;
    dividerLabel?: string;
    onresize?: (e: CustomEvent<{ position: number; orientation: string }>) => void;
    onresizeend?: (e: CustomEvent<{ position: number; orientation: string }>) => void;
    el?: XSplitPaneElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    orientation,
    position,
    minStart,
    minEnd,
    disabled,
    dividerLabel,
    onresize,
    onresizeend,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XSplitPaneProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onresizeHandler = (e: Event) => onresize?.(e as CustomEvent<{ position: number; orientation: string }>);
    node.addEventListener("x-split-pane-resize", onresizeHandler);
    cleanups.push(() => node.removeEventListener("x-split-pane-resize", onresizeHandler));
    const onresizeendHandler = (e: Event) => onresizeend?.(e as CustomEvent<{ position: number; orientation: string }>);
    node.addEventListener("x-split-pane-resize-end", onresizeendHandler);
    cleanups.push(() => node.removeEventListener("x-split-pane-resize-end", onresizeendHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-split-pane
  bind:this={el}
  {orientation}
  {position}
  {minStart}
  {minEnd}
  {disabled}
  {dividerLabel}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-split-pane>
