<!-- x-table-cell.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XTableCell as XTableCellElement } from "@vanelsas/baredom/x-table-cell";
  import { init } from "@vanelsas/baredom/x-table-cell";
  init();
  export type { XTableCellElement };
  export interface XTableCellProps {
    align?: string;
    rowSpan?: number;
    truncate?: boolean;
    disabled?: boolean;
    valign?: string;
    type?: string;
    scope?: string;
    colSpan?: number;
    sticky?: string;
    sortable?: boolean;
    sortDirection?: string;
    onsort?: (e: CustomEvent<{ direction: string; previousDirection: string }>) => void;
    onconnected?: (e: CustomEvent<{ type: string; scope: string; colSpan: number; rowSpan: number; align: string }>) => void;
    ondisconnected?: (e: CustomEvent<{}>) => void;
    el?: XTableCellElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    align,
    rowSpan,
    truncate,
    disabled,
    valign,
    type,
    scope,
    colSpan,
    sticky,
    sortable,
    sortDirection,
    onsort,
    onconnected,
    ondisconnected,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XTableCellProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onsortHandler = (e: Event) => onsort?.(e as CustomEvent<{ direction: string; previousDirection: string }>);
    node.addEventListener("x-table-cell-sort", onsortHandler);
    cleanups.push(() => node.removeEventListener("x-table-cell-sort", onsortHandler));
    const onconnectedHandler = (e: Event) => onconnected?.(e as CustomEvent<{ type: string; scope: string; colSpan: number; rowSpan: number; align: string }>);
    node.addEventListener("x-table-cell-connected", onconnectedHandler);
    cleanups.push(() => node.removeEventListener("x-table-cell-connected", onconnectedHandler));
    const ondisconnectedHandler = (e: Event) => ondisconnected?.(e as CustomEvent<{}>);
    node.addEventListener("x-table-cell-disconnected", ondisconnectedHandler);
    cleanups.push(() => node.removeEventListener("x-table-cell-disconnected", ondisconnectedHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<!-- svelte-ignore a11y_misplaced_scope -->
<x-table-cell
  bind:this={el}
  {align}
  {rowSpan}
  {truncate}
  {disabled}
  {valign}
  {type}
  {scope}
  {colSpan}
  {sticky}
  {sortable}
  {sortDirection}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-table-cell>
