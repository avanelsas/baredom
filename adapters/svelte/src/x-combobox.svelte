<!-- x-combobox.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XCombobox as XComboboxElement } from "@vanelsas/baredom/x-combobox";
  import { init } from "@vanelsas/baredom/x-combobox";
  init();
  export type { XComboboxElement };
  export interface XComboboxProps {
    placeholder?: string;
    name?: string;
    disabled?: boolean;
    required?: boolean;
    open?: boolean;
    placement?: string;
    error?: string;
    /** Two-way bindable form value — `bind:value={...}`. */
    value?: string;
    onchangerequest?: (e: CustomEvent<{ value: string; label: string; previousValue: string }>) => void;
    onchange?: (e: CustomEvent<{ value: string; label: string }>) => void;
    oninput?: (e: CustomEvent<{ query: string }>) => void;
    ontoggle?: (e: CustomEvent<{ open: boolean; source: string }>) => void;
    el?: XComboboxElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    placeholder,
    name,
    disabled,
    required,
    open,
    placement,
    error,
    value = $bindable(undefined),
    onchangerequest,
    onchange,
    oninput,
    ontoggle,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XComboboxProps = $props();

  $effect(() => {
    const node = el;
    if (!node || value === undefined) return;
    if (value != null) node.setAttribute("value", String(value));
    else node.removeAttribute("value");
  });

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangerequestHandler = (e: Event) => onchangerequest?.(e as CustomEvent<{ value: string; label: string; previousValue: string }>);
    node.addEventListener("x-combobox-change-request", onchangerequestHandler);
    cleanups.push(() => node.removeEventListener("x-combobox-change-request", onchangerequestHandler));
    const onchangeHandler = (e: Event) => {
      const detail = (e as CustomEvent<{ value: string; label: string }>).detail;
      value = String(detail.value);
      onchange?.(e as CustomEvent<{ value: string; label: string }>);
    };
    node.addEventListener("x-combobox-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-combobox-change", onchangeHandler));
    const oninputHandler = (e: Event) => oninput?.(e as CustomEvent<{ query: string }>);
    node.addEventListener("x-combobox-input", oninputHandler);
    cleanups.push(() => node.removeEventListener("x-combobox-input", oninputHandler));
    const ontoggleHandler = (e: Event) => ontoggle?.(e as CustomEvent<{ open: boolean; source: string }>);
    node.addEventListener("x-combobox-toggle", ontoggleHandler);
    cleanups.push(() => node.removeEventListener("x-combobox-toggle", ontoggleHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-combobox
  bind:this={el}
  {placeholder}
  {name}
  {disabled}
  {required}
  {open}
  {placement}
  {error}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-combobox>
