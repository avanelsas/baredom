<!-- x-toast.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XToast as XToastElement } from "@vanelsas/baredom/x-toast";
  import { init } from "@vanelsas/baredom/x-toast";
  init();
  export type { XToastElement };
  export interface XToastProps {
    type?: string;
    heading?: string;
    message?: string;
    icon?: string;
    dismissible?: boolean;
    disabled?: boolean;
    timeoutMs?: number;
    showProgress?: boolean;
    ondismiss?: (e: CustomEvent<{ type: string; reason: string; heading: string; message: string }>) => void;
    el?: XToastElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    type,
    heading,
    message,
    icon,
    dismissible,
    disabled,
    timeoutMs,
    showProgress,
    ondismiss,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XToastProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const ondismissHandler = (e: Event) => ondismiss?.(e as CustomEvent<{ type: string; reason: string; heading: string; message: string }>);
    node.addEventListener("x-toast-dismiss", ondismissHandler);
    cleanups.push(() => node.removeEventListener("x-toast-dismiss", ondismissHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-toast
  bind:this={el}
  {type}
  {heading}
  {message}
  {icon}
  {dismissible}
  {disabled}
  {timeoutMs}
  {showProgress}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-toast>
