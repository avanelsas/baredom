<!-- x-splash.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XSplash as XSplashElement } from "@vanelsas/baredom/x-splash";
  import { init } from "@vanelsas/baredom/x-splash";
  init();
  export type { XSplashElement };
  export interface XSplashProps {
    active?: boolean;
    variant?: string;
    progress?: number;
    spinner?: boolean;
    overlay?: string;
    onhidden?: (e: CustomEvent<{}>) => void;
    el?: XSplashElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    active,
    variant,
    progress,
    spinner,
    overlay,
    onhidden,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XSplashProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onhiddenHandler = (e: Event) => onhidden?.(e as CustomEvent<{}>);
    node.addEventListener("x-splash-hidden", onhiddenHandler);
    cleanups.push(() => node.removeEventListener("x-splash-hidden", onhiddenHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-splash
  bind:this={el}
  {active}
  {variant}
  {progress}
  {spinner}
  {overlay}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-splash>
