<!-- x-soft-body.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XSoftBody as XSoftBodyElement } from "@vanelsas/baredom/x-soft-body";
  import { init } from "@vanelsas/baredom/x-soft-body";
  init();
  export type { XSoftBodyElement };
  export interface XSoftBodyProps {
    stiffness?: number;
    damping?: number;
    radius?: number;
    intensity?: number;
    grabRadius?: number;
    disabled?: boolean;
    ongrab?: (e: CustomEvent<{}>) => void;
    onrelease?: (e: CustomEvent<{}>) => void;
    el?: XSoftBodyElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    stiffness,
    damping,
    radius,
    intensity,
    grabRadius,
    disabled,
    ongrab,
    onrelease,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XSoftBodyProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const ongrabHandler = (e: Event) => ongrab?.(e as CustomEvent<{}>);
    node.addEventListener("x-soft-body-grab", ongrabHandler);
    cleanups.push(() => node.removeEventListener("x-soft-body-grab", ongrabHandler));
    const onreleaseHandler = (e: Event) => onrelease?.(e as CustomEvent<{}>);
    node.addEventListener("x-soft-body-release", onreleaseHandler);
    cleanups.push(() => node.removeEventListener("x-soft-body-release", onreleaseHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-soft-body
  bind:this={el}
  {stiffness}
  {damping}
  {radius}
  {intensity}
  {grabRadius}
  {disabled}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-soft-body>
