<!-- x-drop-zone.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XDropZone as XDropZoneElement } from "@vanelsas/baredom/x-drop-zone";
  import { init } from "@vanelsas/baredom/x-drop-zone";
  init();
  export type { XDropZoneElement };
  export interface XDropZoneProps {
    value?: string;
    accepts?: string;
    max?: number;
    label?: string;
    disabled?: boolean;
    pending?: boolean;
    pendingIndex?: number;
    animateMoves?: boolean;
    onenter?: (e: CustomEvent<{ kind: string; value: string }>) => void;
    onleave?: (e: CustomEvent<{ kind: string; value: string }>) => void;
    ondrop?: (e: CustomEvent<{ kind: string; value: string; from: string | null; to: string; index: number; panel: HTMLElement }>) => void;
    el?: XDropZoneElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    value,
    accepts,
    max,
    label,
    disabled,
    pending,
    pendingIndex,
    animateMoves,
    onenter,
    onleave,
    ondrop,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XDropZoneProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onenterHandler = (e: Event) => onenter?.(e as CustomEvent<{ kind: string; value: string }>);
    node.addEventListener("x-drop-zone-enter", onenterHandler);
    cleanups.push(() => node.removeEventListener("x-drop-zone-enter", onenterHandler));
    const onleaveHandler = (e: Event) => onleave?.(e as CustomEvent<{ kind: string; value: string }>);
    node.addEventListener("x-drop-zone-leave", onleaveHandler);
    cleanups.push(() => node.removeEventListener("x-drop-zone-leave", onleaveHandler));
    const ondropHandler = (e: Event) => ondrop?.(e as CustomEvent<{ kind: string; value: string; from: string | null; to: string; index: number; panel: HTMLElement }>);
    node.addEventListener("x-drop-zone-drop", ondropHandler);
    cleanups.push(() => node.removeEventListener("x-drop-zone-drop", ondropHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-drop-zone
  bind:this={el}
  {value}
  {accepts}
  {max}
  {label}
  {disabled}
  {pending}
  {pendingIndex}
  {animateMoves}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-drop-zone>
