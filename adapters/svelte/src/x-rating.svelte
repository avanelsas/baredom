<!-- x-rating.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XRating as XRatingElement } from "@vanelsas/baredom/x-rating";
  import { init } from "@vanelsas/baredom/x-rating";
  init();
  export type { XRatingElement };
  export interface XRatingProps {
    allowClear?: boolean;
    disabled?: boolean;
    name?: string;
    value?: string;
    precision?: string;
    size?: string;
    readOnly?: boolean;
    max?: string;
    label?: string;
    shape?: string;
    onchangerequest?: (e: CustomEvent<{ value: number; previousValue: number; max: number }>) => void;
    onchange?: (e: CustomEvent<{ value: number; max: number }>) => void;
    onhover?: (e: CustomEvent<{ value: number; max: number }>) => void;
    el?: XRatingElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    allowClear,
    disabled,
    name,
    value,
    precision,
    size,
    readOnly,
    max,
    label,
    shape,
    onchangerequest,
    onchange,
    onhover,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XRatingProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangerequestHandler = (e: Event) => onchangerequest?.(e as CustomEvent<{ value: number; previousValue: number; max: number }>);
    node.addEventListener("x-rating-change-request", onchangerequestHandler);
    cleanups.push(() => node.removeEventListener("x-rating-change-request", onchangerequestHandler));
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ value: number; max: number }>);
    node.addEventListener("x-rating-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-rating-change", onchangeHandler));
    const onhoverHandler = (e: Event) => onhover?.(e as CustomEvent<{ value: number; max: number }>);
    node.addEventListener("x-rating-hover", onhoverHandler);
    cleanups.push(() => node.removeEventListener("x-rating-hover", onhoverHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-rating
  bind:this={el}
  {allowClear}
  {disabled}
  {name}
  {value}
  {precision}
  {size}
  {readOnly}
  {max}
  {label}
  {shape}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-rating>
