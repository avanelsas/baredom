<!-- x-form.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XForm as XFormElement } from "@vanelsas/baredom/x-form";
  import { init } from "@vanelsas/baredom/x-form";
  init();
  export type { XFormElement };
  export interface XFormProps {
    loading?: boolean;
    novalidate?: boolean;
    autocomplete?: string;
    onsubmit?: (e: CustomEvent<{ values: Record<string, any> }>) => void;
    onreset?: (e: CustomEvent<{}>) => void;
    el?: XFormElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    loading,
    novalidate,
    autocomplete,
    onsubmit,
    onreset,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XFormProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onsubmitHandler = (e: Event) => onsubmit?.(e as CustomEvent<{ values: Record<string, any> }>);
    node.addEventListener("x-form-submit", onsubmitHandler);
    cleanups.push(() => node.removeEventListener("x-form-submit", onsubmitHandler));
    const onresetHandler = (e: Event) => onreset?.(e as CustomEvent<{}>);
    node.addEventListener("x-form-reset", onresetHandler);
    cleanups.push(() => node.removeEventListener("x-form-reset", onresetHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-form
  bind:this={el}
  {loading}
  {novalidate}
  {autocomplete}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-form>
