<!-- x-toaster.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XToaster as XToasterElement } from "@vanelsas/baredom/x-toaster";
  import { init } from "@vanelsas/baredom/x-toaster";
  init();
  export type { XToasterElement };
  export interface XToasterProps {
    position?: string;
    maxToasts?: number;
    label?: string;
    ondismiss?: (e: CustomEvent<{ type: string; reason: string; heading: string; message: string }>) => void;
    el?: XToasterElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    position,
    maxToasts,
    label,
    ondismiss,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XToasterProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const ondismissHandler = (e: Event) => ondismiss?.(e as CustomEvent<{ type: string; reason: string; heading: string; message: string }>);
    node.addEventListener("x-toaster-dismiss", ondismissHandler);
    cleanups.push(() => node.removeEventListener("x-toaster-dismiss", ondismissHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-toaster
  bind:this={el}
  {position}
  {maxToasts}
  {label}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-toaster>
