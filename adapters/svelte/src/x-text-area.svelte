<!-- x-text-area.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XTextArea as XTextAreaElement } from "@vanelsas/baredom/x-text-area";
  import { init } from "@vanelsas/baredom/x-text-area";
  init();
  export type { XTextAreaElement };
  export interface XTextAreaProps {
    disabled?: boolean;
    name?: string;
    minLength?: number;
    rows?: number;
    readOnly?: boolean;
    maxLength?: number;
    autocomplete?: string;
    required?: boolean;
    /** Two-way bindable form value — `bind:value={...}`. */
    value?: string;
    onchangerequest?: (e: CustomEvent<{ name: string; value: string; previousValue: string }>) => void;
    oninput?: (e: CustomEvent<{ name: string; value: string }>) => void;
    onchange?: (e: CustomEvent<{ name: string; value: string }>) => void;
    el?: XTextAreaElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    disabled,
    name,
    minLength,
    rows,
    readOnly,
    maxLength,
    autocomplete,
    required,
    value = $bindable(undefined),
    onchangerequest,
    oninput,
    onchange,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XTextAreaProps = $props();

  $effect(() => {
    const node = el;
    if (!node || value === undefined) return;
    if (value != null) node.setAttribute("value", String(value));
    else node.removeAttribute("value");
  });

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangerequestHandler = (e: Event) => onchangerequest?.(e as CustomEvent<{ name: string; value: string; previousValue: string }>);
    node.addEventListener("x-text-area-change-request", onchangerequestHandler);
    cleanups.push(() => node.removeEventListener("x-text-area-change-request", onchangerequestHandler));
    const oninputHandler = (e: Event) => oninput?.(e as CustomEvent<{ name: string; value: string }>);
    node.addEventListener("x-text-area-input", oninputHandler);
    cleanups.push(() => node.removeEventListener("x-text-area-input", oninputHandler));
    const onchangeHandler = (e: Event) => {
      const detail = (e as CustomEvent<{ name: string; value: string }>).detail;
      value = String(detail.value);
      onchange?.(e as CustomEvent<{ name: string; value: string }>);
    };
    node.addEventListener("x-text-area-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-text-area-change", onchangeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-text-area
  bind:this={el}
  {disabled}
  {name}
  {minLength}
  {rows}
  {readOnly}
  {maxLength}
  {autocomplete}
  {required}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-text-area>
