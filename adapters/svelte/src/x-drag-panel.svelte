<!-- x-drag-panel.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XDragPanel as XDragPanelElement } from "@vanelsas/baredom/x-drag-panel";
  import { init } from "@vanelsas/baredom/x-drag-panel";
  init();
  export type { XDragPanelElement };
  export interface XDragPanelProps {
    kind?: string;
    value?: string;
    label?: string;
    grab?: string;
    autoScroll?: string;
    disabled?: boolean;
    pending?: boolean;
    ondragstart?: (e: CustomEvent<{ kind: string; value: string }>) => void;
    ondragcancel?: (e: CustomEvent<{ kind: string; value: string; reason: string }>) => void;
    el?: XDragPanelElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    kind,
    value,
    label,
    grab,
    autoScroll,
    disabled,
    pending,
    ondragstart,
    ondragcancel,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XDragPanelProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const ondragstartHandler = (e: Event) => ondragstart?.(e as CustomEvent<{ kind: string; value: string }>);
    node.addEventListener("x-drag-panel-drag-start", ondragstartHandler);
    cleanups.push(() => node.removeEventListener("x-drag-panel-drag-start", ondragstartHandler));
    const ondragcancelHandler = (e: Event) => ondragcancel?.(e as CustomEvent<{ kind: string; value: string; reason: string }>);
    node.addEventListener("x-drag-panel-drag-cancel", ondragcancelHandler);
    cleanups.push(() => node.removeEventListener("x-drag-panel-drag-cancel", ondragcancelHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-drag-panel
  bind:this={el}
  {kind}
  {value}
  {label}
  {grab}
  {autoScroll}
  {disabled}
  {pending}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-drag-panel>
