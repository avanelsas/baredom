<!-- x-morph-stack.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XMorphStack as XMorphStackElement } from "@vanelsas/baredom/x-morph-stack";
  import { init } from "@vanelsas/baredom/x-morph-stack";
  init();
  export type { XMorphStackElement };
  export interface XMorphStackProps {
    activeState?: string;
    activeIndex?: number;
    stiffness?: number;
    damping?: number;
    mass?: number;
    variant?: string;
    duration?: number;
    disabled?: boolean;
    onchange?: (e: CustomEvent<{ from: string; to: string; reason: string }>) => void;
    onchanged?: (e: CustomEvent<{ from: string; to: string }>) => void;
    el?: XMorphStackElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    activeState,
    activeIndex,
    stiffness,
    damping,
    mass,
    variant,
    duration,
    disabled,
    onchange,
    onchanged,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XMorphStackProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ from: string; to: string; reason: string }>);
    node.addEventListener("x-morph-stack-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-morph-stack-change", onchangeHandler));
    const onchangedHandler = (e: Event) => onchanged?.(e as CustomEvent<{ from: string; to: string }>);
    node.addEventListener("x-morph-stack-changed", onchangedHandler);
    cleanups.push(() => node.removeEventListener("x-morph-stack-changed", onchangedHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-morph-stack
  bind:this={el}
  {activeState}
  {activeIndex}
  {stiffness}
  {damping}
  {mass}
  {variant}
  {duration}
  {disabled}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-morph-stack>
