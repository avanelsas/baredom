<!-- x-scroll-stack.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XScrollStack as XScrollStackElement } from "@vanelsas/baredom/x-scroll-stack";
  import { init } from "@vanelsas/baredom/x-scroll-stack";
  init();
  export type { XScrollStackElement };
  export interface XScrollStackProps {
    peek?: number;
    rotation?: number;
    scrollDistance?: number;
    align?: string;
    disabled?: boolean;
    onchange?: (e: CustomEvent<{ stackedCount: number; totalCount: number; progress: number }>) => void;
    onprogress?: (e: CustomEvent<{ progress: number; stackedCount: number; totalCount: number }>) => void;
    el?: XScrollStackElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    peek,
    rotation,
    scrollDistance,
    align,
    disabled,
    onchange,
    onprogress,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XScrollStackProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ stackedCount: number; totalCount: number; progress: number }>);
    node.addEventListener("x-scroll-stack-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-stack-change", onchangeHandler));
    const onprogressHandler = (e: Event) => onprogress?.(e as CustomEvent<{ progress: number; stackedCount: number; totalCount: number }>);
    node.addEventListener("x-scroll-stack-progress", onprogressHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-stack-progress", onprogressHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-scroll-stack
  bind:this={el}
  {peek}
  {rotation}
  {scrollDistance}
  {align}
  {disabled}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-scroll-stack>
