<!-- x-navbar.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XNavbar as XNavbarElement } from "@vanelsas/baredom/x-navbar";
  import { init } from "@vanelsas/baredom/x-navbar";
  init();
  export type { XNavbarElement };
  export interface XNavbarProps {
    sticky?: boolean;
    elevated?: boolean;
    label?: string;
    variant?: string;
    orientation?: string;
    alignment?: string;
    breakpoint?: string;
    onfocusvisible?: (e: CustomEvent<{}>) => void;
    onnavigate?: (e: CustomEvent<{ href: string; source: string }>) => void;
    onbrandactivate?: (e: CustomEvent<{ source: string }>) => void;
    el?: XNavbarElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    sticky,
    elevated,
    label,
    variant,
    orientation,
    alignment,
    breakpoint,
    onfocusvisible,
    onnavigate,
    onbrandactivate,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XNavbarProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onfocusvisibleHandler = (e: Event) => onfocusvisible?.(e as CustomEvent<{}>);
    node.addEventListener("focus-visible", onfocusvisibleHandler);
    cleanups.push(() => node.removeEventListener("focus-visible", onfocusvisibleHandler));
    const onnavigateHandler = (e: Event) => onnavigate?.(e as CustomEvent<{ href: string; source: string }>);
    node.addEventListener("navigate", onnavigateHandler);
    cleanups.push(() => node.removeEventListener("navigate", onnavigateHandler));
    const onbrandactivateHandler = (e: Event) => onbrandactivate?.(e as CustomEvent<{ source: string }>);
    node.addEventListener("brand-activate", onbrandactivateHandler);
    cleanups.push(() => node.removeEventListener("brand-activate", onbrandactivateHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-navbar
  bind:this={el}
  {sticky}
  {elevated}
  {label}
  {variant}
  {orientation}
  {alignment}
  {breakpoint}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-navbar>
