<!-- x-scroll-timeline.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XScrollTimeline as XScrollTimelineElement } from "@vanelsas/baredom/x-scroll-timeline";
  import { init } from "@vanelsas/baredom/x-scroll-timeline";
  init();
  export type { XScrollTimelineElement };
  export interface XScrollTimelineProps {
    autoplayLoop?: boolean;
    autoplaySpeed?: number;
    autoplayIndicator?: boolean;
    disabled?: boolean;
    layout?: string;
    marker?: string;
    threshold?: number;
    label?: string;
    autoplay?: boolean;
    noProgress?: boolean;
    track?: string;
    onentrychange?: (e: CustomEvent<{ index: number; id: string; previousIndex: number; previousId: string }>) => void;
    onentryenter?: (e: CustomEvent<{ index: number; id: string; progress: number }>) => void;
    onentryleave?: (e: CustomEvent<{ index: number; id: string; progress: number }>) => void;
    onprogress?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
    onenter?: (e: CustomEvent<{ progress: number }>) => void;
    onleave?: (e: CustomEvent<{ progress: number }>) => void;
    onautoplaypause?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
    onautoplayresume?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
    el?: XScrollTimelineElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    autoplayLoop,
    autoplaySpeed,
    autoplayIndicator,
    disabled,
    layout,
    marker,
    threshold,
    label,
    autoplay,
    noProgress,
    track,
    onentrychange,
    onentryenter,
    onentryleave,
    onprogress,
    onenter,
    onleave,
    onautoplaypause,
    onautoplayresume,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XScrollTimelineProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onentrychangeHandler = (e: Event) => onentrychange?.(e as CustomEvent<{ index: number; id: string; previousIndex: number; previousId: string }>);
    node.addEventListener("x-scroll-timeline-entry-change", onentrychangeHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-timeline-entry-change", onentrychangeHandler));
    const onentryenterHandler = (e: Event) => onentryenter?.(e as CustomEvent<{ index: number; id: string; progress: number }>);
    node.addEventListener("x-scroll-timeline-entry-enter", onentryenterHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-timeline-entry-enter", onentryenterHandler));
    const onentryleaveHandler = (e: Event) => onentryleave?.(e as CustomEvent<{ index: number; id: string; progress: number }>);
    node.addEventListener("x-scroll-timeline-entry-leave", onentryleaveHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-timeline-entry-leave", onentryleaveHandler));
    const onprogressHandler = (e: Event) => onprogress?.(e as CustomEvent<{ progress: number; activeIndex: number; activeId: string }>);
    node.addEventListener("x-scroll-timeline-progress", onprogressHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-timeline-progress", onprogressHandler));
    const onenterHandler = (e: Event) => onenter?.(e as CustomEvent<{ progress: number }>);
    node.addEventListener("x-scroll-timeline-enter", onenterHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-timeline-enter", onenterHandler));
    const onleaveHandler = (e: Event) => onleave?.(e as CustomEvent<{ progress: number }>);
    node.addEventListener("x-scroll-timeline-leave", onleaveHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-timeline-leave", onleaveHandler));
    const onautoplaypauseHandler = (e: Event) => onautoplaypause?.(e as CustomEvent<{ progress: number; activeIndex: number; activeId: string }>);
    node.addEventListener("x-scroll-timeline-autoplay-pause", onautoplaypauseHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-timeline-autoplay-pause", onautoplaypauseHandler));
    const onautoplayresumeHandler = (e: Event) => onautoplayresume?.(e as CustomEvent<{ progress: number; activeIndex: number; activeId: string }>);
    node.addEventListener("x-scroll-timeline-autoplay-resume", onautoplayresumeHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-timeline-autoplay-resume", onautoplayresumeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-scroll-timeline
  bind:this={el}
  {autoplayLoop}
  {autoplaySpeed}
  {autoplayIndicator}
  {disabled}
  {layout}
  {marker}
  {threshold}
  {label}
  {autoplay}
  {noProgress}
  {track}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-scroll-timeline>
