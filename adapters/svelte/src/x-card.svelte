<!-- x-card.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XCard as XCardElement } from "@vanelsas/baredom/x-card";
  import { init } from "@vanelsas/baredom/x-card";
  init();
  export type { XCardElement };
  export interface XCardProps {
    interactive?: boolean;
    disabled?: boolean;
    onpress?: (e: CustomEvent<{}>) => void;
    el?: XCardElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    interactive,
    disabled,
    onpress,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XCardProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onpressHandler = (e: Event) => onpress?.(e as CustomEvent<{}>);
    node.addEventListener("press", onpressHandler);
    cleanups.push(() => node.removeEventListener("press", onpressHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-card
  bind:this={el}
  {interactive}
  {disabled}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-card>
