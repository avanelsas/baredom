<!-- x-organic-progress.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XOrganicProgress as XOrganicProgressElement } from "@vanelsas/baredom/x-organic-progress";
  import { init } from "@vanelsas/baredom/x-organic-progress";
  init();
  export type { XOrganicProgressElement };
  export interface XOrganicProgressProps {
    progress?: string;
    variant?: string;
    color?: string;
    bloom?: boolean;
    density?: string;
    seed?: number;
    label?: string;
    oncomplete?: (e: CustomEvent<{ progress: number }>) => void;
    onbloomend?: (e: CustomEvent<{ progress: number }>) => void;
    el?: XOrganicProgressElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    progress,
    variant,
    color,
    bloom,
    density,
    seed,
    label,
    oncomplete,
    onbloomend,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XOrganicProgressProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const oncompleteHandler = (e: Event) => oncomplete?.(e as CustomEvent<{ progress: number }>);
    node.addEventListener("x-organic-progress-complete", oncompleteHandler);
    cleanups.push(() => node.removeEventListener("x-organic-progress-complete", oncompleteHandler));
    const onbloomendHandler = (e: Event) => onbloomend?.(e as CustomEvent<{ progress: number }>);
    node.addEventListener("x-organic-progress-bloom-end", onbloomendHandler);
    cleanups.push(() => node.removeEventListener("x-organic-progress-bloom-end", onbloomendHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-organic-progress
  bind:this={el}
  {progress}
  {variant}
  {color}
  {bloom}
  {density}
  {seed}
  {label}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-organic-progress>
