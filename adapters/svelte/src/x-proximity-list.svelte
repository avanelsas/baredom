<!-- x-proximity-list.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XProximityList as XProximityListElement } from "@vanelsas/baredom/x-proximity-list";
  import { init } from "@vanelsas/baredom/x-proximity-list";
  init();
  export type { XProximityListElement };
  export interface XProximityListProps {
    direction?: string;
    radius?: number;
    maxScale?: number;
    lift?: number;
    gap?: number;
    disabled?: boolean;
    onselect?: (e: CustomEvent<{ index: number; item: any; source: string }>) => void;
    el?: XProximityListElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    direction,
    radius,
    maxScale,
    lift,
    gap,
    disabled,
    onselect,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XProximityListProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onselectHandler = (e: Event) => onselect?.(e as CustomEvent<{ index: number; item: any; source: string }>);
    node.addEventListener("x-proximity-list-select", onselectHandler);
    cleanups.push(() => node.removeEventListener("x-proximity-list-select", onselectHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-proximity-list
  bind:this={el}
  {direction}
  {radius}
  {maxScale}
  {lift}
  {gap}
  {disabled}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-proximity-list>
