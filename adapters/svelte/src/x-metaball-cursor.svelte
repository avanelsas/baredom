<!-- x-metaball-cursor.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XMetaballCursor as XMetaballCursorElement } from "@vanelsas/baredom/x-metaball-cursor";
  import { init } from "@vanelsas/baredom/x-metaball-cursor";
  init();
  export type { XMetaballCursorElement };
  export interface XMetaballCursorProps {
    color?: string;
    blobSize?: number;
    noiseIntensity?: number;
    palette?: string;
    noiseScale?: number;
    noise?: boolean;
    threshold?: string;
    blur?: number;
    noiseSpeed?: number;
    blobCount?: number;
    el?: XMetaballCursorElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    color,
    blobSize,
    noiseIntensity,
    palette,
    noiseScale,
    noise,
    threshold,
    blur,
    noiseSpeed,
    blobCount,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XMetaballCursorProps = $props();
</script>

<x-metaball-cursor
  bind:this={el}
  {color}
  {blobSize}
  {noiseIntensity}
  {palette}
  {noiseScale}
  {noise}
  {threshold}
  {blur}
  {noiseSpeed}
  {blobCount}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-metaball-cursor>
