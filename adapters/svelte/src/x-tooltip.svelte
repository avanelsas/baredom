<!-- x-tooltip.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XTooltip as XTooltipElement } from "@vanelsas/baredom/x-tooltip";
  import { init } from "@vanelsas/baredom/x-tooltip";
  init();
  export type { XTooltipElement };
  export interface XTooltipProps {
    text?: string;
    placement?: string;
    delay?: number;
    disabled?: boolean;
    open?: boolean;
    onshow?: (e: CustomEvent<{}>) => void;
    onhide?: (e: CustomEvent<{}>) => void;
    el?: XTooltipElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    text,
    placement,
    delay,
    disabled,
    open,
    onshow,
    onhide,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XTooltipProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onshowHandler = (e: Event) => onshow?.(e as CustomEvent<{}>);
    node.addEventListener("x-tooltip-show", onshowHandler);
    cleanups.push(() => node.removeEventListener("x-tooltip-show", onshowHandler));
    const onhideHandler = (e: Event) => onhide?.(e as CustomEvent<{}>);
    node.addEventListener("x-tooltip-hide", onhideHandler);
    cleanups.push(() => node.removeEventListener("x-tooltip-hide", onhideHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-tooltip
  bind:this={el}
  {text}
  {placement}
  {delay}
  {disabled}
  {open}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-tooltip>
