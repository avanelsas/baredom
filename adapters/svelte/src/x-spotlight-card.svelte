<!-- x-spotlight-card.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XSpotlightCard as XSpotlightCardElement } from "@vanelsas/baredom/x-spotlight-card";
  import { init } from "@vanelsas/baredom/x-spotlight-card";
  init();
  export type { XSpotlightCardElement };
  export interface XSpotlightCardProps {
    variant?: string;
    radius?: string;
    padding?: string;
    color?: string;
    intensity?: string;
    size?: string;
    static?: boolean;
    el?: XSpotlightCardElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    variant,
    radius,
    padding,
    color,
    intensity,
    size,
    static: staticAttr,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XSpotlightCardProps = $props();
</script>

<x-spotlight-card
  bind:this={el}
  {variant}
  {radius}
  {padding}
  {color}
  {intensity}
  {size}
  static={staticAttr}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-spotlight-card>
