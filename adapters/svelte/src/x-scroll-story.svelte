<!-- x-scroll-story.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XScrollStory as XScrollStoryElement } from "@vanelsas/baredom/x-scroll-story";
  import { init } from "@vanelsas/baredom/x-scroll-story";
  init();
  export type { XScrollStoryElement };
  export interface XScrollStoryProps {
    autoplayLoop?: boolean;
    autoplaySpeed?: number;
    autoplayIndicator?: boolean;
    disabled?: boolean;
    layout?: string;
    threshold?: number;
    split?: number;
    label?: string;
    autoplay?: boolean;
    onstepchange?: (e: CustomEvent<{ index: number; id: string; previousIndex: number; previousId: string }>) => void;
    onstepenter?: (e: CustomEvent<{ index: number; id: string; progress: number }>) => void;
    onstepleave?: (e: CustomEvent<{ index: number; id: string; progress: number }>) => void;
    onprogress?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
    onenter?: (e: CustomEvent<{ progress: number }>) => void;
    onleave?: (e: CustomEvent<{ progress: number }>) => void;
    onautoplaypause?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
    onautoplayresume?: (e: CustomEvent<{ progress: number; activeIndex: number; activeId: string }>) => void;
    el?: XScrollStoryElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    autoplayLoop,
    autoplaySpeed,
    autoplayIndicator,
    disabled,
    layout,
    threshold,
    split,
    label,
    autoplay,
    onstepchange,
    onstepenter,
    onstepleave,
    onprogress,
    onenter,
    onleave,
    onautoplaypause,
    onautoplayresume,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XScrollStoryProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onstepchangeHandler = (e: Event) => onstepchange?.(e as CustomEvent<{ index: number; id: string; previousIndex: number; previousId: string }>);
    node.addEventListener("x-scroll-story-step-change", onstepchangeHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-story-step-change", onstepchangeHandler));
    const onstepenterHandler = (e: Event) => onstepenter?.(e as CustomEvent<{ index: number; id: string; progress: number }>);
    node.addEventListener("x-scroll-story-step-enter", onstepenterHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-story-step-enter", onstepenterHandler));
    const onstepleaveHandler = (e: Event) => onstepleave?.(e as CustomEvent<{ index: number; id: string; progress: number }>);
    node.addEventListener("x-scroll-story-step-leave", onstepleaveHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-story-step-leave", onstepleaveHandler));
    const onprogressHandler = (e: Event) => onprogress?.(e as CustomEvent<{ progress: number; activeIndex: number; activeId: string }>);
    node.addEventListener("x-scroll-story-progress", onprogressHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-story-progress", onprogressHandler));
    const onenterHandler = (e: Event) => onenter?.(e as CustomEvent<{ progress: number }>);
    node.addEventListener("x-scroll-story-enter", onenterHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-story-enter", onenterHandler));
    const onleaveHandler = (e: Event) => onleave?.(e as CustomEvent<{ progress: number }>);
    node.addEventListener("x-scroll-story-leave", onleaveHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-story-leave", onleaveHandler));
    const onautoplaypauseHandler = (e: Event) => onautoplaypause?.(e as CustomEvent<{ progress: number; activeIndex: number; activeId: string }>);
    node.addEventListener("x-scroll-story-autoplay-pause", onautoplaypauseHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-story-autoplay-pause", onautoplaypauseHandler));
    const onautoplayresumeHandler = (e: Event) => onautoplayresume?.(e as CustomEvent<{ progress: number; activeIndex: number; activeId: string }>);
    node.addEventListener("x-scroll-story-autoplay-resume", onautoplayresumeHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-story-autoplay-resume", onautoplayresumeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-scroll-story
  bind:this={el}
  {autoplayLoop}
  {autoplaySpeed}
  {autoplayIndicator}
  {disabled}
  {layout}
  {threshold}
  {split}
  {label}
  {autoplay}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-scroll-story>
