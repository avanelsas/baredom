<!-- x-liquid-dock.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XLiquidDock as XLiquidDockElement } from "@vanelsas/baredom/x-liquid-dock";
  import { init } from "@vanelsas/baredom/x-liquid-dock";
  init();
  export type { XLiquidDockElement };
  export interface XLiquidDockProps {
    rippleScale?: number;
    color?: string;
    magnetStrength?: number;
    disabled?: boolean;
    threshold?: string;
    magnetRadius?: number;
    bobIntensity?: number;
    blur?: number;
    rippleSpeed?: number;
    gap?: number;
    position?: string;
    onselect?: (e: CustomEvent<{ index: number; item: any; source: string }>) => void;
    el?: XLiquidDockElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    rippleScale,
    color,
    magnetStrength,
    disabled,
    threshold,
    magnetRadius,
    bobIntensity,
    blur,
    rippleSpeed,
    gap,
    position,
    onselect,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XLiquidDockProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onselectHandler = (e: Event) => onselect?.(e as CustomEvent<{ index: number; item: any; source: string }>);
    node.addEventListener("x-liquid-dock-select", onselectHandler);
    cleanups.push(() => node.removeEventListener("x-liquid-dock-select", onselectHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-liquid-dock
  bind:this={el}
  {rippleScale}
  {color}
  {magnetStrength}
  {disabled}
  {threshold}
  {magnetRadius}
  {bobIntensity}
  {blur}
  {rippleSpeed}
  {gap}
  {position}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-liquid-dock>
