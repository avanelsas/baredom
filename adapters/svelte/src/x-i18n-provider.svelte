<!-- x-i18n-provider.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XI18nProvider as XI18nProviderElement } from "@vanelsas/baredom/x-i18n-provider";
  import { init } from "@vanelsas/baredom/x-i18n-provider";
  init();
  export type { XI18nProviderElement };
  export interface XI18nProviderProps {
    src?: string;
    locale?: string;
    fallbackLocale?: string;
    onxi18nloading?: (e: CustomEvent<{ locale: string }>) => void;
    onxi18nchange?: (e: CustomEvent<{ locale: string }>) => void;
    onxi18nerror?: (e: CustomEvent<{ locale: string; message: string }>) => void;
    el?: XI18nProviderElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    src,
    locale,
    fallbackLocale,
    onxi18nloading,
    onxi18nchange,
    onxi18nerror,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XI18nProviderProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onxi18nloadingHandler = (e: Event) => onxi18nloading?.(e as CustomEvent<{ locale: string }>);
    node.addEventListener("x-i18n-loading", onxi18nloadingHandler);
    cleanups.push(() => node.removeEventListener("x-i18n-loading", onxi18nloadingHandler));
    const onxi18nchangeHandler = (e: Event) => onxi18nchange?.(e as CustomEvent<{ locale: string }>);
    node.addEventListener("x-i18n-change", onxi18nchangeHandler);
    cleanups.push(() => node.removeEventListener("x-i18n-change", onxi18nchangeHandler));
    const onxi18nerrorHandler = (e: Event) => onxi18nerror?.(e as CustomEvent<{ locale: string; message: string }>);
    node.addEventListener("x-i18n-error", onxi18nerrorHandler);
    cleanups.push(() => node.removeEventListener("x-i18n-error", onxi18nerrorHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-i18n-provider
  bind:this={el}
  {src}
  {locale}
  {fallbackLocale}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-i18n-provider>
