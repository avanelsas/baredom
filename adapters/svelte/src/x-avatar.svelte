<!-- x-avatar.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XAvatar as XAvatarElement } from "@vanelsas/baredom/x-avatar";
  import { init } from "@vanelsas/baredom/x-avatar";
  init();
  export type { XAvatarElement };
  export interface XAvatarProps {
    initials?: string;
    disabled?: boolean;
    variant?: string;
    alt?: string;
    name?: string;
    src?: string;
    size?: string;
    status?: string;
    shape?: string;
    el?: XAvatarElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    initials,
    disabled,
    variant,
    alt,
    name,
    src,
    size,
    status,
    shape,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XAvatarProps = $props();
</script>

<x-avatar
  bind:this={el}
  {initials}
  {disabled}
  {variant}
  {alt}
  {name}
  {src}
  {size}
  {status}
  {shape}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-avatar>
