<!-- x-stat.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XStat as XStatElement } from "@vanelsas/baredom/x-stat";
  import { init } from "@vanelsas/baredom/x-stat";
  init();
  export type { XStatElement };
  export interface XStatProps {
    trend?: string;
    align?: string;
    variant?: string;
    value?: string;
    hint?: string;
    size?: string;
    loading?: boolean;
    label?: string;
    emphasis?: string;
    el?: XStatElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    trend,
    align,
    variant,
    value,
    hint,
    size,
    loading,
    label,
    emphasis,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XStatProps = $props();
</script>

<x-stat
  bind:this={el}
  {trend}
  {align}
  {variant}
  {value}
  {hint}
  {size}
  {loading}
  {label}
  {emphasis}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-stat>
