<!-- x-form-field.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XFormField as XFormFieldElement } from "@vanelsas/baredom/x-form-field";
  import { init } from "@vanelsas/baredom/x-form-field";
  init();
  export type { XFormFieldElement };
  export interface XFormFieldProps {
    placeholder?: string;
    disabled?: boolean;
    name?: string;
    value?: string;
    readonly?: boolean;
    type?: string;
    label?: string;
    autocomplete?: string;
    required?: boolean;
    oninput?: (e: CustomEvent<{ name: string; value: string }>) => void;
    onchange?: (e: CustomEvent<{ name: string; value: string }>) => void;
    el?: XFormFieldElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    placeholder,
    disabled,
    name,
    value,
    readonly,
    type,
    label,
    autocomplete,
    required,
    oninput,
    onchange,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XFormFieldProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const oninputHandler = (e: Event) => oninput?.(e as CustomEvent<{ name: string; value: string }>);
    node.addEventListener("x-form-field-input", oninputHandler);
    cleanups.push(() => node.removeEventListener("x-form-field-input", oninputHandler));
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ name: string; value: string }>);
    node.addEventListener("x-form-field-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-form-field-change", onchangeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-form-field
  bind:this={el}
  {placeholder}
  {disabled}
  {name}
  {value}
  {readonly}
  {type}
  {label}
  {autocomplete}
  {required}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-form-field>
