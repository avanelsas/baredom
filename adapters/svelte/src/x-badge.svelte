<!-- x-badge.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XBadge as XBadgeElement } from "@vanelsas/baredom/x-badge";
  import { init } from "@vanelsas/baredom/x-badge";
  init();
  export type { XBadgeElement };
  export interface XBadgeProps {
    variant?: string;
    size?: string;
    pill?: boolean;
    dot?: boolean;
    count?: number;
    max?: number;
    text?: string;
    el?: XBadgeElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    variant,
    size,
    pill,
    dot,
    count,
    max,
    text,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XBadgeProps = $props();
</script>

<x-badge
  bind:this={el}
  {variant}
  {size}
  {pill}
  {dot}
  {count}
  {max}
  {text}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-badge>
