<!-- x-table-row.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XTableRow as XTableRowElement } from "@vanelsas/baredom/x-table-row";
  import { init } from "@vanelsas/baredom/x-table-row";
  init();
  export type { XTableRowElement };
  export interface XTableRowProps {
    selected?: boolean;
    disabled?: boolean;
    interactive?: boolean;
    rowIndex?: number;
    onclick?: (e: CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean }>) => void;
    onconnected?: (e: CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean; interactive: boolean }>) => void;
    ondisconnected?: (e: CustomEvent<{}>) => void;
    el?: XTableRowElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    selected,
    disabled,
    interactive,
    rowIndex,
    onclick,
    onconnected,
    ondisconnected,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XTableRowProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onclickHandler = (e: Event) => onclick?.(e as CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean }>);
    node.addEventListener("x-table-row-click", onclickHandler);
    cleanups.push(() => node.removeEventListener("x-table-row-click", onclickHandler));
    const onconnectedHandler = (e: Event) => onconnected?.(e as CustomEvent<{ rowIndex: number; selected: boolean; disabled: boolean; interactive: boolean }>);
    node.addEventListener("x-table-row-connected", onconnectedHandler);
    cleanups.push(() => node.removeEventListener("x-table-row-connected", onconnectedHandler));
    const ondisconnectedHandler = (e: Event) => ondisconnected?.(e as CustomEvent<{}>);
    node.addEventListener("x-table-row-disconnected", ondisconnectedHandler);
    cleanups.push(() => node.removeEventListener("x-table-row-disconnected", ondisconnectedHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-table-row
  bind:this={el}
  {selected}
  {disabled}
  {interactive}
  {rowIndex}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-table-row>
