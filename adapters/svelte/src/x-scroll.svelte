<!-- x-scroll.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XScroll as XScrollElement } from "@vanelsas/baredom/x-scroll";
  import { init } from "@vanelsas/baredom/x-scroll";
  init();
  export type { XScrollElement };
  export interface XScrollProps {
    disabled?: boolean;
    showIndicators?: boolean;
    snap?: string;
    activeIndex?: number;
    mode?: string;
    showControls?: boolean;
    loop?: boolean;
    label?: string;
    gap?: number;
    autoPlay?: boolean;
    interval?: number;
    onchange?: (e: CustomEvent<{ activeIndex: number; previousIndex: number }>) => void;
    onstart?: (e: CustomEvent<{ direction: string; activeIndex: number }>) => void;
    onend?: (e: CustomEvent<{ activeIndex: number }>) => void;
    onloop?: (e: CustomEvent<{ direction: string }>) => void;
    el?: XScrollElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    disabled,
    showIndicators,
    snap,
    activeIndex,
    mode,
    showControls,
    loop,
    label,
    gap,
    autoPlay,
    interval,
    onchange,
    onstart,
    onend,
    onloop,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XScrollProps = $props();

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangeHandler = (e: Event) => onchange?.(e as CustomEvent<{ activeIndex: number; previousIndex: number }>);
    node.addEventListener("x-scroll-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-change", onchangeHandler));
    const onstartHandler = (e: Event) => onstart?.(e as CustomEvent<{ direction: string; activeIndex: number }>);
    node.addEventListener("x-scroll-start", onstartHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-start", onstartHandler));
    const onendHandler = (e: Event) => onend?.(e as CustomEvent<{ activeIndex: number }>);
    node.addEventListener("x-scroll-end", onendHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-end", onendHandler));
    const onloopHandler = (e: Event) => onloop?.(e as CustomEvent<{ direction: string }>);
    node.addEventListener("x-scroll-loop", onloopHandler);
    cleanups.push(() => node.removeEventListener("x-scroll-loop", onloopHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-scroll
  bind:this={el}
  {disabled}
  {showIndicators}
  {snap}
  {activeIndex}
  {mode}
  {showControls}
  {loop}
  {label}
  {gap}
  {autoPlay}
  {interval}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-scroll>
