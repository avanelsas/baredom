<!-- x-currency-field.svelte — auto-generated by generate_svelte.bb, do not edit -->
<script lang="ts" module>
  import type { XCurrencyField as XCurrencyFieldElement } from "@vanelsas/baredom/x-currency-field";
  import { init } from "@vanelsas/baredom/x-currency-field";
  init();
  export type { XCurrencyFieldElement };
  export interface XCurrencyFieldProps {
    min?: string;
    placeholder?: string;
    disabled?: boolean;
    locale?: string;
    name?: string;
    hint?: string;
    currency?: string;
    readOnly?: boolean;
    max?: string;
    label?: string;
    error?: string;
    required?: boolean;
    /** Two-way bindable form value — `bind:value={...}`. */
    value?: string;
    onchangerequest?: (e: CustomEvent<{ name: string; value: string; previousValue: string }>) => void;
    oninput?: (e: CustomEvent<{ name: string; value: string }>) => void;
    onchange?: (e: CustomEvent<{ name: string; value: string }>) => void;
    el?: XCurrencyFieldElement | null;
    children?: import("svelte").Snippet;
    class?: string;
    id?: string;
    slot?: string;
  }
</script>

<script lang="ts">
  let {
    min,
    placeholder,
    disabled,
    locale,
    name,
    hint,
    currency,
    readOnly,
    max,
    label,
    error,
    required,
    value = $bindable(undefined),
    onchangerequest,
    oninput,
    onchange,
    el = $bindable(null),
    children,
    class: className,
    id,
    ...rest
  }: XCurrencyFieldProps = $props();

  $effect(() => {
    const node = el;
    if (!node || value === undefined) return;
    if (value != null) node.setAttribute("value", String(value));
    else node.removeAttribute("value");
  });

  $effect(() => {
    const node = el;
    if (!node) return;
    const cleanups: Array<() => void> = [];
    const onchangerequestHandler = (e: Event) => onchangerequest?.(e as CustomEvent<{ name: string; value: string; previousValue: string }>);
    node.addEventListener("x-currency-field-change-request", onchangerequestHandler);
    cleanups.push(() => node.removeEventListener("x-currency-field-change-request", onchangerequestHandler));
    const oninputHandler = (e: Event) => oninput?.(e as CustomEvent<{ name: string; value: string }>);
    node.addEventListener("x-currency-field-input", oninputHandler);
    cleanups.push(() => node.removeEventListener("x-currency-field-input", oninputHandler));
    const onchangeHandler = (e: Event) => {
      const detail = (e as CustomEvent<{ name: string; value: string }>).detail;
      value = String(detail.value);
      onchange?.(e as CustomEvent<{ name: string; value: string }>);
    };
    node.addEventListener("x-currency-field-change", onchangeHandler);
    cleanups.push(() => node.removeEventListener("x-currency-field-change", onchangeHandler));
    return () => cleanups.forEach((fn) => fn());
  });
</script>

<x-currency-field
  bind:this={el}
  {min}
  {placeholder}
  {disabled}
  {locale}
  {name}
  {hint}
  {currency}
  {readOnly}
  {max}
  {label}
  {error}
  {required}
  class={className}
  {id}
  {...rest}
>
  {@render children?.()}
</x-currency-field>
